import * as Redacted from "effect/Redacted";
export { OAuthCredentials, OAuthError, type Authorization, } from "../Auth/OAuthFlow.ts";
/**
 * Registered PlanetScale OAuth application credentials.
 *
 * Unlike Cloudflare, PlanetScale OAuth has **no public-client flow** — the
 * token endpoint requires client authentication for every grant (PKCE is
 * advertised in their discovery doc but does not lift that requirement), so
 * exchanging the authorization code (and refreshing the token) requires the
 * application's `client_secret`. There is no way to keep that secret out of
 * a distributed CLI, so it ships here: the exposure is the same posture as a
 * public `client_id` (a stolen refresh token is usable, exactly like
 * Cloudflare's secret-less refresh), and it can be rotated by cutting a new
 * release. PlanetScale's own CLI ships its OAuth `client_secret` in source
 * the same way:
 * https://github.com/planetscale/cli/blob/main/internal/auth/authenticator.go
 *
 * Registered at https://app.planetscale.com with redirect URI
 * {@link OAUTH_REDIRECT_URI}. Scopes are configured on the application
 * itself, not requested per-authorization (so `authorize()` is called with
 * no scopes). Rotate by registering a new secret and cutting a release.
 */
export declare const OAUTH_CLIENT_ID = "pscale_app_aa12e3938baebb788aac443f66e422da";
export declare const OAUTH_CLIENT_SECRET: Redacted.Redacted<string>;
export declare const OAUTH_REDIRECT_URI = "https://alchemy.run/auth/callback";
export declare const OAUTH_LOCAL_CALLBACK_URI = "http://localhost:9976/auth/callback";
export declare const OAUTH_ENDPOINTS: {
    authorize: string;
    token: string;
};
export declare const authorize: (scopes?: ReadonlyArray<string>) => import("effect/Effect").Effect<import("./OAuthClient.ts").Authorization, import("./OAuthClient.ts").OAuthError, import("effect/Crypto").Crypto>, callback: (authorization: import("./OAuthClient.ts").Authorization) => import("effect/Effect").Effect<import("./OAuthClient.ts").OAuthCredentials, import("./OAuthClient.ts").OAuthError | import("../Auth/OAuthFlow.ts").CallbackServerStartError, never>, exchange: (code: string, authorization?: import("./OAuthClient.ts").Authorization) => import("effect/Effect").Effect<import("./OAuthClient.ts").OAuthCredentials, import("./OAuthClient.ts").OAuthError, never>, exchangeCallbackInput: (input: string, authorization: import("./OAuthClient.ts").Authorization) => import("effect/Effect").Effect<import("./OAuthClient.ts").OAuthCredentials, import("./OAuthClient.ts").OAuthError, never>, refresh: (credentials: import("./OAuthClient.ts").OAuthCredentials) => import("effect/Effect").Effect<import("./OAuthClient.ts").OAuthCredentials, import("./OAuthClient.ts").OAuthError, never>, usesCurrentClient: (credentials: {
    readonly clientId?: unknown;
}) => boolean;
//# sourceMappingURL=OAuthClient.d.ts.map