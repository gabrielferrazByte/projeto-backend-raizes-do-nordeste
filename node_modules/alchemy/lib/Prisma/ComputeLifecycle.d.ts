import * as Effect from "effect/Effect";
import { PrismaPaginationError } from "./Internal/Pagination.ts";
export { isConflict } from "./Client.ts";
declare const PrismaDeploymentWaitTimeout_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "PrismaDeploymentWaitTimeout";
} & Readonly<A>;
/**
 * A wait exceeded its `timeoutSeconds` budget before the deployment reached
 * the requested status.
 */
export declare class PrismaDeploymentWaitTimeout extends PrismaDeploymentWaitTimeout_base<{
    message: string;
}> {
}
declare const PrismaDeploymentWaitInvalidOptions_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "PrismaDeploymentWaitInvalidOptions";
} & Readonly<A>;
/** `waitForDeploymentStatus` was called with a non-positive timeout or poll interval. */
export declare class PrismaDeploymentWaitInvalidOptions extends PrismaDeploymentWaitInvalidOptions_base<{
    message: string;
}> {
}
declare const PrismaDeploymentFailed_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "PrismaDeploymentFailed";
} & Readonly<A>;
/** The deployment reached the terminal `failed` status while being waited on. */
export declare class PrismaDeploymentFailed extends PrismaDeploymentFailed_base<{
    message: string;
}> {
}
export interface WaitForDeploymentStatusOptions {
    /**
     * Maximum time to wait for a deployment to reach the requested status.
     */
    timeoutSeconds?: number;
    /**
     * Poll interval used while waiting for Prisma deployment status changes.
     */
    pollIntervalMs?: number;
}
/**
 * Result returned after attempting to stop and delete a Prisma deployment.
 */
export interface DestroyDeploymentResult {
    /**
     * Prisma deployment ID that was targeted.
     */
    deploymentId: string;
    /**
     * Status observed before cleanup started, or undefined if the deployment was gone.
     */
    previousStatus: string | undefined;
    /**
     * True when Alchemy requested a stop before deletion.
     */
    stopped: boolean;
    /**
     * True when the delete call completed or the deployment vanished during cleanup.
     */
    deleted: boolean;
}
/** Result returned after deleting a Prisma App. */
export interface DestroyAppResult {
    /** Prisma App ID that was targeted. */
    appId: string;
    /** True when App deletion completed or the app was already gone. */
    appDeleted: boolean;
}
/**
 * Result returned after deleting Apps under a Prisma project.
 */
export interface DestroyProjectAppsResult {
    /**
     * Prisma project ID that was targeted.
     */
    projectId: string;
    /**
     * App IDs deleted by this cleanup pass.
     */
    deletedAppIds: string[];
    /**
     * True when the project delete call completed or the project vanished.
     */
    projectDeleted: boolean;
}
export declare const waitForDeploymentStatus: (deploymentId: string, targetStatus: "running" | "stopped", options?: WaitForDeploymentStatusOptions | undefined) => Effect.Effect<import("@distilled.cloud/prisma/management").GetDeploymentResponseData, PrismaDeploymentFailed | PrismaDeploymentWaitInvalidOptions | PrismaDeploymentWaitTimeout | import("@distilled.cloud/prisma/management").GetDeploymentError, import("@distilled.cloud/prisma").PrismaOpContext>;
/**
 * Stops a running or provisioning deployment, then deletes it.
 *
 * Uses the canonical deployment lifecycle routes. Errors include the observed
 * status and exact manual route for cleanup.
 */
export declare const destroyDeployment: (deploymentId: string, options?: WaitForDeploymentStatusOptions | undefined) => Effect.Effect<{
    deploymentId: string;
    previousStatus: undefined;
    stopped: false;
    deleted: true;
} | {
    deploymentId: string;
    previousStatus: string;
    stopped: boolean;
    deleted: true;
}, Error | import("effect/unstable/http/HttpClientError").HttpClientError, import("@distilled.cloud/prisma").PrismaOpContext>;
/**
 * Deletes an App. The canonical App delete endpoint cascades its deployments.
 */
export declare const destroyApp: (appId: string, options?: (WaitForDeploymentStatusOptions & {
    keepApp?: boolean;
}) | undefined) => Effect.Effect<{
    appId: string;
    appDeleted: boolean;
}, import("@distilled.cloud/prisma/management").Conflict | import("@distilled.cloud/prisma/management").Forbidden | import("@distilled.cloud/prisma").PrismaOpError, import("@distilled.cloud/prisma").PrismaOpContext>;
/**
 * Deletes every App under a project, then deletes the project.
 *
 * Callers can start from the project ID and let Alchemy discover the App IDs.
 */
export declare const destroyProjectApps: (projectId: string, options?: (WaitForDeploymentStatusOptions & {
    keepProject?: boolean;
    keepApp?: boolean;
}) | undefined) => Effect.Effect<{
    projectId: string;
    deletedAppIds: string[];
    projectDeleted: boolean;
}, import("@distilled.cloud/prisma/management").BadRequest | import("@distilled.cloud/prisma/management").Conflict | import("@distilled.cloud/prisma/management").Forbidden | PrismaPaginationError | import("@distilled.cloud/prisma/management").UnprocessableEntity | import("@distilled.cloud/prisma").PrismaOpError, import("@distilled.cloud/prisma").PrismaOpContext>;
export declare const toDeploymentUrl: (domain: string | null | undefined) => string | undefined;
//# sourceMappingURL=ComputeLifecycle.d.ts.map