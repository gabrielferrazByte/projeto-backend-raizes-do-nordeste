import * as Effect from "effect/Effect";
/** Prove that a deployment belongs to an App before mutating or deleting it. */
export declare const ensureDeploymentMembership: (appId: string, deployment: {
    id: string;
    foundryVersionId: string;
}, knownLatestDeploymentId?: string | null | undefined) => Effect.Effect<undefined, Error | import("effect/unstable/http/HttpClientError").HttpClientError, import("@distilled.cloud/prisma").PrismaOpContext>;
//# sourceMappingURL=DeploymentIdentity.d.ts.map