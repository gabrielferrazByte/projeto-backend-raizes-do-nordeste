import * as Effect from "effect/Effect";
import { getDeployment } from "@distilled.cloud/prisma/management";
export const observeDeployment = (deploymentId) => getDeployment({ deploymentId }).pipe(Effect.map((response) => response.data));
//# sourceMappingURL=DeploymentObserve.js.map