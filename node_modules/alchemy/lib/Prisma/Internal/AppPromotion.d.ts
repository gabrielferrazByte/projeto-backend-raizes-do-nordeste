import * as Effect from "effect/Effect";
export interface AppDeploymentTargetObservationOptions {
    readonly timeoutSeconds?: number;
    readonly pollIntervalMs?: number;
}
export declare const waitForAppDeploymentTarget: (appId: string, deploymentId: string, options?: AppDeploymentTargetObservationOptions | undefined) => Effect.Effect<import("@distilled.cloud/prisma/management").CreateServiceResponseData, Error, import("@distilled.cloud/prisma").PrismaOpContext>;
/**
 * Promote an App and recover response loss by observing the canonical App.
 * Every successful mutation is followed by bounded observation of the App
 * row before callers may probe stable readiness or delete another deployment.
 * If the promotion response is lost, the canonical rollback primitive heals
 * an already-flipped Foundry endpoint or safely completes the target
 * promotion. Any non-converged result is ambiguous and must never trigger
 * target deletion.
 */
export declare const promoteAppObserved: (appId: string, deploymentId: string, options?: AppDeploymentTargetObservationOptions | undefined) => Effect.Effect<{
    reassignedDomains: number;
    appEndpointDomain: string;
}, AggregateError, import("@distilled.cloud/prisma").PrismaOpContext>;
//# sourceMappingURL=AppPromotion.d.ts.map