import { GetTaxSettings } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveTaxSettings, } from "./RetrieveTaxSettings.js";
import { attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveTaxSettings}. Provide it on the
 * Function or Worker Effect. Tax Settings has no Stripe id — only the API
 * key is bound.
 *
 * @layer
 * @provides Stripe.RetrieveTaxSettings
 */
export const RetrieveTaxSettingsHttp = Layer.effect(RetrieveTaxSettings, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (settings) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(settings, ["tax_read"], "Stripe.RetrieveTaxSettings");
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.RetrieveTaxSettings(${settings.LogicalId})`)(function* (request) {
            return yield* auth(GetTaxSettings(request ?? {}));
        });
    });
}));
//# sourceMappingURL=RetrieveTaxSettingsHttp.js.map