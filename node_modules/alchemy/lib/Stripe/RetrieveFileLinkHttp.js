import { GetFileLink } from "@distilled.cloud/stripe/stripe";
import * as Layer from "effect/Layer";
import { RetrieveFileLink } from "./RetrieveFileLink.js";
import { makeHttpStripeIdBinding } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveFileLink}. Provide it on the
 * Function or Worker Effect.
 *
 * @layer
 * @provides Stripe.RetrieveFileLink
 */
export const RetrieveFileLinkHttp = Layer.effect(RetrieveFileLink, makeHttpStripeIdBinding({
    tag: "Stripe.RetrieveFileLink",
    operation: GetFileLink,
    idField: "link",
    permissions: ["files_read"],
}));
//# sourceMappingURL=RetrieveFileLinkHttp.js.map