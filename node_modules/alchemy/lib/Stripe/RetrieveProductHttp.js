import { GetProduct } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveProduct } from "./RetrieveProduct.js";
import { asStringEffect, attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveProduct}. Provide it on the
 * Function or Worker Effect. Accepts a Product resource, a `prod_…` id
 * string, or an Effect resolving to a Product.
 *
 * @layer
 * @provides Stripe.RetrieveProduct
 */
export const RetrieveProductHttp = Layer.effect(RetrieveProduct, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (product) {
        const host = yield* Binding.Host;
        const isId = typeof product === "string";
        const resource = isId ? undefined : product;
        const bound = yield* attachStripeToken(resource, ["products_read"], "Stripe.RetrieveProduct");
        const id = yield* asStringEffect(isId ? product : product.id);
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        const label = isId ? product : product.LogicalId;
        return Effect.fn(`Stripe.RetrieveProduct(${label})`)(function* (request) {
            return yield* auth(GetProduct({
                ...(request ?? {}),
                id: yield* id,
            }));
        });
    });
}));
//# sourceMappingURL=RetrieveProductHttp.js.map