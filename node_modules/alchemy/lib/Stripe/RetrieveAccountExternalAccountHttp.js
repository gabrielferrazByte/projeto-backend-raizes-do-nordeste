import { GetAccountExternalAccount } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveAccountExternalAccount } from "./RetrieveAccountExternalAccount.js";
import { asStringEffect, attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveAccountExternalAccount}. The
 * nested retrieve takes both `account` and `id`.
 *
 * @layer
 * @provides Stripe.RetrieveAccountExternalAccount
 */
export const RetrieveAccountExternalAccountHttp = Layer.effect(RetrieveAccountExternalAccount, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (externalAccount) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(externalAccount, ["accounts_read"], "Stripe.RetrieveAccountExternalAccount");
        const id = yield* asStringEffect(externalAccount.id);
        const account = yield* asStringEffect(externalAccount.account);
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.RetrieveAccountExternalAccount(${externalAccount.LogicalId})`)(function* (request) {
            return yield* auth(GetAccountExternalAccount({
                ...(request ?? {}),
                id: yield* id,
                account: yield* account,
            }));
        });
    });
}));
//# sourceMappingURL=RetrieveAccountExternalAccountHttp.js.map