import { GetCustomerTaxIdsById } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveCustomerTaxId } from "./RetrieveCustomerTaxId.js";
import { asStringEffect, attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveCustomerTaxId}. The nested
 * retrieve takes both `customer` and `id`.
 *
 * @layer
 * @provides Stripe.RetrieveCustomerTaxId
 */
export const RetrieveCustomerTaxIdHttp = Layer.effect(RetrieveCustomerTaxId, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (taxId) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(taxId, ["customers_read"], "Stripe.RetrieveCustomerTaxId");
        const id = yield* asStringEffect(taxId.id);
        const customer = yield* asStringEffect(taxId.customer);
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.RetrieveCustomerTaxId(${taxId.LogicalId})`)(function* (request) {
            return yield* auth(GetCustomerTaxIdsById({
                ...(request ?? {}),
                id: yield* id,
                customer: yield* customer,
            }));
        });
    });
}));
//# sourceMappingURL=RetrieveCustomerTaxIdHttp.js.map