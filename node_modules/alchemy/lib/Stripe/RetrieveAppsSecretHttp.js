import { GetAppsSecretsFind } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveAppsSecret } from "./RetrieveAppsSecret.js";
import { asOptionalStringEffect, asStringEffect, attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveAppsSecret}. Find is keyed by
 * `name` and `scope`.
 *
 * @layer
 * @provides Stripe.RetrieveAppsSecret
 */
export const RetrieveAppsSecretHttp = Layer.effect(RetrieveAppsSecret, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (secret) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(secret, ["apps_secrets_read"], "Stripe.RetrieveAppsSecret");
        const name = yield* asStringEffect(secret.name);
        const scopeType = yield* asStringEffect(secret.scope.type);
        const scopeUser = yield* asOptionalStringEffect(secret.scope.user);
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.RetrieveAppsSecret(${secret.LogicalId})`)(function* (request) {
            const user = yield* scopeUser;
            return yield* auth(GetAppsSecretsFind({
                ...(request ?? {}),
                name: yield* name,
                scope: {
                    type: (yield* scopeType),
                    ...(user !== undefined && user.length > 0 ? { user } : {}),
                },
            }));
        });
    });
}));
//# sourceMappingURL=RetrieveAppsSecretHttp.js.map