import { GetProductFeature } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { RetrieveProductFeature } from "./RetrieveProductFeature.js";
import { asStringEffect, attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
/**
 * HTTP implementation of {@link RetrieveProductFeature}. The list-item
 * retrieve takes both `product` and `id`.
 *
 * @layer
 * @provides Stripe.RetrieveProductFeature
 */
export const RetrieveProductFeatureHttp = Layer.effect(RetrieveProductFeature, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (feature) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(feature, ["products_read"], "Stripe.RetrieveProductFeature");
        const id = yield* asStringEffect(feature.id);
        const product = yield* asStringEffect(feature.product);
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.RetrieveProductFeature(${feature.LogicalId})`)(function* (request) {
            return yield* auth(GetProductFeature({
                ...(request ?? {}),
                id: yield* id,
                product: yield* product,
            }));
        });
    });
}));
//# sourceMappingURL=RetrieveProductFeatureHttp.js.map