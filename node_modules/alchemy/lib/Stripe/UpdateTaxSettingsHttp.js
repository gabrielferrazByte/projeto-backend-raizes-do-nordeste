import { CreateTaxSettings } from "@distilled.cloud/stripe/stripe";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Binding from "../Binding.js";
import { attachStripeToken, authorizeWith, resolveStripeAuth, } from "./StripeHttp.js";
import { UpdateTaxSettings, } from "./UpdateTaxSettings.js";
/**
 * HTTP implementation of {@link UpdateTaxSettings}. Provide it on the
 * Function or Worker Effect. Tax Settings has no Stripe id — only the API
 * key is bound.
 *
 * @layer
 * @provides Stripe.UpdateTaxSettings
 */
export const UpdateTaxSettingsHttp = Layer.effect(UpdateTaxSettings, Effect.gen(function* () {
    const ambient = yield* resolveStripeAuth;
    return Effect.fn(function* (settings) {
        const host = yield* Binding.Host;
        const bound = yield* attachStripeToken(settings, ["tax_write"], "Stripe.UpdateTaxSettings");
        const auth = host !== undefined ? authorizeWith(bound) : ambient.authorize;
        return Effect.fn(`Stripe.UpdateTaxSettings(${settings.LogicalId})`)(function* (request) {
            return yield* auth(CreateTaxSettings(request ?? {}));
        });
    });
}));
//# sourceMappingURL=UpdateTaxSettingsHttp.js.map