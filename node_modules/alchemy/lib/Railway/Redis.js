import { environmentServiceInstances, waitUntilDeleted, projectServices, } from "./GraphQL.js";
import { randomBytes } from "node:crypto";
import * as railway from "@distilled.cloud/railway";
import * as Data from "effect/Data";
import * as Effect from "effect/Effect";
import * as Redacted from "effect/Redacted";
import * as Schedule from "effect/Schedule";
import { Unowned } from "../AdoptPolicy.js";
import { isResolved } from "../Diff.js";
import * as Provider from "../Provider.js";
import { Resource } from "../Resource.js";
import { createRailwayName, matchesAlchemyPhysicalName } from "./Metadata.js";
import { ownedProjects, projectEnvironmentIds, } from "./Project.js";
export const DEFAULT_REDIS_IMAGE = "redis:7";
export const REDIS_PORT = 6379;
import { REDIS_URL_ENV } from "./RedisBinding.js";
const serviceSelection = {
    id: true,
    name: true,
    deletedAt: true,
};
const attributeInstanceSelection = {
    source: { image: true },
    region: true,
    latestDeployment: { id: true, status: true },
};
const instanceSelection = {
    ...attributeInstanceSelection,
    deletedAt: true,
    sleepApplication: true,
    startCommand: true,
};
export { REDIS_URL_ENV };
export const REDIS_PASSWORD_ENV = "REDISPASSWORD";
const resolveRedisProps = (props) => Effect.gen(function* () {
    const resolved = Effect.isEffect(props) ? yield* props : props;
    if (globalThis.__ALCHEMY_RUNTIME__)
        return resolved;
    const project = Effect.isEffect(resolved.project)
        ? yield* resolved.project
        : resolved.project;
    const environment = resolved.environment === undefined
        ? undefined
        : Effect.isEffect(resolved.environment)
            ? yield* resolved.environment
            : resolved.environment;
    return { ...resolved, project, environment };
});
const RedisResource = Resource("Railway.Redis");
/**
 * A Railway.Redis is a Redis container in a Project (`redis:7` or
 * `bitnami/redis`). Alchemy writes `REDIS_URL` / `REDISPASSWORD` on the
 * service. Bind {@link ReadRedis}, {@link WriteRedis}, or
 * {@link ReadWriteRedis} on a {@link Service}. Reach it from a laptop
 * through {@link TcpProxy} on port `6379`.
 *
 * @see https://docs.railway.com/guides/redis
 *
 * ### Create Redis
 * Pass a Project. Alchemy generates a unique name and password unless
 * you pass them. Default image is `redis:7`.
 *
 * **Example:** Generated name
 * ```typescript
 * const site = yield* Railway.Project("Site");
 * const cache = yield* Railway.Redis("Cache", { project: site });
 * ```
 *
 * :::note
 * Prefer omitting `name` in tests and CI so names stay unique and
 * reclaimable.
 * :::
 *
 * ### A stable name
 * Pass `name` when you want a stable service name. Changing it later
 * updates the service in place.
 *
 * **Example:** Explicit name
 * ```typescript
 * const cache = yield* Railway.Redis("Cache", {
 *   project: site,
 *   name: "cache",
 * });
 * ```
 *
 * ### Image
 * Official Redis (`redis:7`) is started with `--requirepass`. Bitnami
 * reads `REDIS_PASSWORD`. Updating the image is in place.
 *
 * **Example:** Bitnami
 * ```typescript
 * const cache = yield* Railway.Redis("Cache", {
 *   project: site,
 *   image: "bitnami/redis",
 * });
 * ```
 *
 * ### Public TCP
 * Attach a {@link TcpProxy} on `6379` to reach Redis from outside the
 * private network.
 *
 * **Example:** Laptop access
 * ```typescript
 * const cache = yield* Railway.Redis("Cache", { project: site });
 * const proxy = yield* Railway.TcpProxy("CacheProxy", {
 *   redis: cache,
 *   environment: site,
 *   applicationPort: 6379,
 * });
 * ```
 *
 * ### Bind from a Service
 * Yield {@link ReadWriteRedis} (or {@link ReadRedis} / {@link WriteRedis})
 * in Service init. Provide the matching `*Http` layer. Runtime commands
 * use `REDIS_URL` (`{name}.railway.internal`).
 *
 * **Example:** Read and write
 * ```typescript
 * import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
 *
 * export const Site = Railway.Project("Site");
 * export const Cache = Railway.Redis("Cache", { project: Site });
 *
 * export default class Api extends Railway.Service<Api>()(
 *   "Api",
 *   { project: Site, main: import.meta.url },
 *   Effect.gen(function* () {
 *     const cache = yield* Railway.ReadWriteRedis(Cache);
 *     return {
 *       fetch: Effect.gen(function* () {
 *         yield* cache.set("marker", "hello");
 *         const value = yield* cache.get("marker");
 *         return HttpServerResponse.json({ value });
 *       }),
 *     };
 *   }).pipe(Effect.provide(Railway.ReadWriteRedisHttp)),
 * ) {}
 * ```
 *
 * ### Module-scope declarations
 * Resource-valued props accept the resource or an Effect producing it.
 *
 * **Example:** Module-scope Redis
 * ```typescript
 * // src/cache.ts
 * import * as Railway from "alchemy/Railway";
 *
 * export const Site = Railway.Project("Site");
 * export const Cache = Railway.Redis("Cache", { project: Site });
 * ```
 *
 * @resource
 */
export const Redis = Object.assign((id, props) => RedisResource(id, resolveRedisProps(props)), RedisResource);
export { CommandError as RedisCommandError, UrlMissing as RedisUrlMissing, } from "../Redis/index.js";
export class RedisNotCreated extends Data.TaggedError("Railway.RedisNotCreated") {
}
export class RedisProjectRequired extends Data.TaggedError("Railway.RedisProjectRequired") {
}
export class RedisDeployFailed extends Data.TaggedError("Railway.RedisDeployFailed") {
}
class RedisPending extends Data.TaggedError("Railway.RedisPending") {
}
class RedisDeployPending extends Data.TaggedError("Railway.RedisDeployPending") {
}
const projectIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.projectId === "string" && rec.projectId.length > 0
        ? rec.projectId
        : undefined;
};
const environmentIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.environmentId === "string" && rec.environmentId.length > 0
        ? rec.environmentId
        : undefined;
};
const isGoneService = (service) => service === undefined || service.deletedAt != null;
const isGoneInstance = (instance) => instance === undefined || instance.deletedAt != null;
const resolveName = (id, name, existing) => Effect.gen(function* () {
    if (name !== undefined)
        return name;
    if (existing !== undefined)
        return existing;
    return yield* createRailwayName(id);
});
const unwrapSecret = (value) => Redacted.isRedacted(value) ? Redacted.value(value) : value;
const generatePassword = Effect.sync(() => {
    const bytes = randomBytes(24);
    return Array.from(bytes, (b) => b.toString(16).padStart(2, "0")).join("");
});
const isBitnamiImage = (image) => /bitnami/i.test(image);
export const isRedisImage = (image) => image != null && /redis/i.test(image);
const startCommandFor = (image, password) => isBitnamiImage(image) ? undefined : `redis-server --requirepass ${password}`;
const privateHostOf = (name) => `${name}.railway.internal`;
const sameImage = (observed, desired) => {
    if (observed == null || observed.length === 0)
        return false;
    if (observed === desired)
        return true;
    if (observed === `${desired}:latest` || desired === `${observed}:latest`) {
        return true;
    }
    return (observed.endsWith(`/${desired}`) || observed.endsWith(`/${desired}:latest`));
};
const deployReady = (status) => status === "SUCCESS" || status === "SLEEPING";
const deployFailed = (status) => status === "FAILED" || status === "CRASHED" || status === "REMOVED";
const getById = (serviceId) => railway.service({ id: serviceId }, serviceSelection).pipe(Effect.map((service) => (isGoneService(service) ? undefined : service)), railway.catchTags(["RailwayNotFound"], () => Effect.succeed(undefined)));
const getInstance = (environmentId, serviceId) => railway.serviceInstance({ environmentId, serviceId }, instanceSelection).pipe(Effect.map((instance) => (isGoneInstance(instance) ? undefined : instance)), railway.catchTags(["RailwayNotFound"], () => Effect.succeed(undefined)));
const listProjectServices = (projectId) => projectServices(projectId, serviceSelection).pipe(Effect.map((services) => services.filter((node) => !isGoneService(node))), railway.catchTags(["RailwayNotFound"], () => Effect.succeed([])));
const findByName = (projectId, name) => listProjectServices(projectId).pipe(Effect.map((services) => services.find((service) => service.name === name)));
const waitForInstance = (environmentId, serviceId) => getInstance(environmentId, serviceId).pipe(Effect.flatMap((instance) => {
    if (instance === undefined) {
        return Effect.fail(new RedisPending({ serviceId, status: "creating" }));
    }
    return Effect.succeed(instance);
}), Effect.retry({
    while: (e) => e._tag === "Railway.RedisPending",
    // serviceCreate fans the instance out to each environment
    // asynchronously; wait a bounded interval for it to appear.
    times: 10,
    schedule: Schedule.spaced("2 seconds"),
}), Effect.catchTag("Railway.RedisPending", () => getInstance(environmentId, serviceId)));
const waitForDeployment = (environmentId, serviceId) => Effect.gen(function* () {
    const instance = yield* getInstance(environmentId, serviceId);
    const latest = instance?.latestDeployment;
    const status = latest?.status;
    if (status !== undefined && deployFailed(status)) {
        return yield* new RedisDeployFailed({
            serviceId,
            status,
            deploymentId: latest?.id,
        });
    }
    if (instance !== undefined && deployReady(status)) {
        return instance;
    }
    return yield* new RedisDeployPending({
        serviceId,
        status: status ?? "pending",
    });
}).pipe(Effect.retry({
    while: (e) => e._tag === "Railway.RedisDeployPending",
    times: 10,
    schedule: Schedule.spaced("5 seconds"),
}), Effect.catchTag("Railway.RedisDeployPending", () => getInstance(environmentId, serviceId)));
const asVariableMap = (value) => {
    if (value === null || typeof value !== "object" || Array.isArray(value)) {
        return {};
    }
    const out = {};
    for (const [key, item] of Object.entries(value)) {
        if (typeof item === "string") {
            out[key] = item;
        }
    }
    return out;
};
const listVariableMap = (projectId, environmentId, serviceId) => railway
    .variables({
    projectId,
    environmentId,
    serviceId,
    unrendered: true,
})
    .pipe(Effect.map(asVariableMap), railway.catchTags(["RailwayNotFound"], () => Effect.succeed({})));
const upsertVariable = (input) => railway.upsertVariable({
    input: {
        projectId: input.projectId,
        environmentId: input.environmentId,
        serviceId: input.serviceId,
        name: input.name,
        value: input.value,
        skipDeploys: true,
    },
});
const redisUrlTemplate = `redis://default:\${{${REDIS_PASSWORD_ENV}}}@\${{RAILWAY_PRIVATE_DOMAIN}}:${REDIS_PORT}`;
const desiredVariables = (password) => ({
    [REDIS_PASSWORD_ENV]: password,
    REDIS_PASSWORD: password,
    REDISUSER: "default",
    REDISPORT: String(REDIS_PORT),
    REDISHOST: "${{RAILWAY_PRIVATE_DOMAIN}}",
    [REDIS_URL_ENV]: redisUrlTemplate,
});
const syncVariables = Effect.fn(function* (input) {
    const observed = yield* listVariableMap(input.projectId, input.environmentId, input.serviceId);
    let changed = false;
    for (const [name, value] of Object.entries(input.desired)) {
        if (observed[name] !== value) {
            yield* upsertVariable({
                projectId: input.projectId,
                environmentId: input.environmentId,
                serviceId: input.serviceId,
                name,
                value,
            });
            changed = true;
        }
    }
    return changed;
});
const toAttrs = (input) => ({
    serviceId: input.service.id,
    name: input.service.name,
    projectId: input.projectId,
    environmentId: input.environmentId,
    image: input.instance?.source?.image ?? input.image,
    region: input.instance?.region ?? undefined,
    port: REDIS_PORT,
    privateHost: privateHostOf(input.service.name),
    deploymentId: input.instance?.latestDeployment?.id,
    deploymentStatus: input.instance?.latestDeployment?.status,
});
export { command as redisCommand, connectionUrl as redisConnectionUrl, run as runRedisCommand, } from "../Redis/index.js";
export const RedisProvider = () => Provider.succeed(Redis, {
    stables: ["serviceId", "projectId", "environmentId"],
    nuke: { dependsOn: ["Railway.Project"] },
    diff: Effect.fn(function* ({ news, output }) {
        if (news === undefined || !isResolved(news))
            return undefined;
        if (output === undefined)
            return undefined;
        const nextProject = projectIdOf(news.project);
        const projectChanged = nextProject !== undefined && nextProject !== output.projectId;
        const nextEnv = environmentIdOf(news.environment);
        const environmentChanged = nextEnv !== undefined && nextEnv !== output.environmentId;
        if (projectChanged || environmentChanged) {
            return { action: "replace" };
        }
        return undefined;
    }),
    read: Effect.fn(function* ({ id, olds, output }) {
        const projectId = output?.projectId ??
            (olds !== undefined ? projectIdOf(olds.project) : undefined);
        const environmentId = output?.environmentId ??
            (olds !== undefined
                ? (environmentIdOf(olds.environment) ?? environmentIdOf(olds.project))
                : undefined);
        const name = yield* resolveName(id, olds?.name, output?.name);
        const byId = output?.serviceId !== undefined && output.serviceId.length > 0
            ? yield* getById(output.serviceId)
            : undefined;
        const found = byId ??
            (projectId !== undefined
                ? yield* findByName(projectId, name)
                : undefined);
        if (found === undefined)
            return undefined;
        const resolvedProjectId = projectIdOf(found) ?? projectId ?? "";
        const resolvedEnvId = environmentId ??
            environmentIdOf(olds?.project) ??
            output?.environmentId ??
            "";
        const instance = resolvedEnvId.length > 0
            ? yield* getInstance(resolvedEnvId, found.id)
            : undefined;
        const attrs = toAttrs({
            service: found,
            instance,
            projectId: resolvedProjectId,
            environmentId: resolvedEnvId,
            image: instance?.source?.image ?? olds?.image ?? DEFAULT_REDIS_IMAGE,
        });
        if (output !== undefined)
            return attrs;
        return matchesAlchemyPhysicalName(found.name) ? attrs : Unowned(attrs);
    }),
    list: Effect.fn(function* () {
        const projects = yield* ownedProjects();
        const rows = yield* Effect.forEach(projects, (project) => Effect.gen(function* () {
            const services = new Map((yield* listProjectServices(project.projectId))
                .filter((service) => matchesAlchemyPhysicalName(service.name))
                .map((service) => [service.id, service]));
            if (services.size === 0)
                return [];
            const envIds = yield* projectEnvironmentIds(project);
            const items = yield* Effect.forEach(envIds, (environmentId) => environmentServiceInstances(environmentId, project.projectId, {
                ...attributeInstanceSelection,
                serviceId: true,
                deletedAt: true,
            }).pipe(Effect.map((instances) => instances.flatMap((instance) => {
                const service = services.get(instance.serviceId);
                const image = instance.source?.image;
                if (instance.deletedAt != null ||
                    service === undefined ||
                    !isRedisImage(image))
                    return [];
                return [
                    toAttrs({
                        service,
                        instance,
                        projectId: project.projectId,
                        environmentId,
                        image: image ?? DEFAULT_REDIS_IMAGE,
                    }),
                ];
            }))));
            return items.flat();
        }));
        return rows.flat();
    }),
    reconcile: Effect.fn(function* ({ id, news, output }) {
        const props = news ?? {};
        const projectId = projectIdOf(props.project) ?? output?.projectId;
        if (projectId === undefined) {
            return yield* new RedisProjectRequired({
                message: "Redis requires a resolved Railway.Project",
            });
        }
        const environmentId = environmentIdOf(props.environment) ??
            environmentIdOf(props.project) ??
            output?.environmentId;
        if (environmentId === undefined) {
            return yield* new RedisProjectRequired({
                message: "Redis requires a Railway environment (pass environment or a Project with environmentId)",
            });
        }
        const name = yield* resolveName(id, props.name, output?.name);
        const image = props.image ?? output?.image ?? DEFAULT_REDIS_IMAGE;
        let current = output?.serviceId !== undefined && output.serviceId.length > 0
            ? yield* getById(output.serviceId)
            : undefined;
        if (current === undefined) {
            current = yield* findByName(projectId, name);
        }
        const existingVars = current !== undefined
            ? yield* listVariableMap(projectId, environmentId, current.id)
            : {};
        const password = props.password !== undefined
            ? unwrapSecret(props.password)
            : existingVars[REDIS_PASSWORD_ENV] !== undefined &&
                existingVars[REDIS_PASSWORD_ENV].length > 0 &&
                !existingVars[REDIS_PASSWORD_ENV].includes("${{")
                ? existingVars[REDIS_PASSWORD_ENV]
                : yield* generatePassword;
        const env = desiredVariables(password);
        const startCommand = startCommandFor(image, password);
        if (current === undefined) {
            const created = yield* railway
                .createService({
                input: {
                    projectId,
                    environmentId,
                    name,
                    source: { image },
                    variables: env,
                },
            }, serviceSelection)
                .pipe(railway.catchTags("RailwayValidationError", () => Effect.succeed(undefined)));
            current = created ?? (yield* findByName(projectId, name));
        }
        if (current === undefined || isGoneService(current)) {
            return yield* new RedisNotCreated({ name, projectId });
        }
        if (current.name !== name) {
            current = yield* railway.updateService({
                id: current.id,
                input: { name },
            }, serviceSelection);
        }
        let instance = yield* waitForInstance(environmentId, current.id);
        let needsDeploy = false;
        const observedImage = instance?.source?.image ?? undefined;
        const imageChanged = !sameImage(observedImage, image);
        const observedRegion = instance?.region ?? undefined;
        const regionChanged = props.region !== undefined && props.region !== observedRegion;
        const observedStart = instance?.startCommand ?? undefined;
        const startChanged = (observedStart ?? undefined) !== startCommand;
        if (imageChanged || regionChanged || startChanged) {
            yield* railway.updateServiceInstance({
                environmentId,
                serviceId: current.id,
                input: {
                    ...(imageChanged ? { source: { image } } : {}),
                    ...(regionChanged ? { region: props.region } : {}),
                    ...(startChanged ? { startCommand: startCommand ?? null } : {}),
                },
            });
            needsDeploy = true;
            instance = (yield* getInstance(environmentId, current.id)) ?? instance;
        }
        const envChanged = yield* syncVariables({
            projectId,
            environmentId,
            serviceId: current.id,
            desired: env,
        });
        if (envChanged)
            needsDeploy = true;
        if (needsDeploy || instance?.latestDeployment == null) {
            yield* railway
                .serviceInstanceDeployV2({
                environmentId,
                serviceId: current.id,
            })
                .pipe(railway.catchTags("RailwayValidationError", () => Effect.void));
        }
        instance =
            (yield* waitForDeployment(environmentId, current.id)) ?? instance;
        return toAttrs({
            service: current,
            instance,
            projectId,
            environmentId,
            image,
        });
    }),
    delete: Effect.fn(function* ({ output }) {
        const serviceId = output.serviceId;
        if (serviceId.length === 0)
            return;
        yield* railway
            .deleteService({ id: serviceId })
            .pipe(railway.catchTags(["RailwayNotFound"], () => Effect.void));
        yield* waitUntilDeleted("Service", serviceId, getById(serviceId).pipe(Effect.map((service) => service === undefined)));
    }),
});
//# sourceMappingURL=Redis.js.map