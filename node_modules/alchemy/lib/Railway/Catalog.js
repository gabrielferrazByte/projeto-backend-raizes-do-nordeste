import * as railway from "@distilled.cloud/railway";
import * as Data from "effect/Data";
import * as Effect from "effect/Effect";
import { resolveWorkspace } from "./Environment.js";
const regionSelection = {
    id: true,
    name: true,
    region: true,
};
export class CatalogNotFound extends Data.TaggedError("Railway.CatalogNotFound") {
}
const notFound = (kind, ref) => new CatalogNotFound({ kind, ref });
/**
 * Current token workspace (`me.workspace ?? me.workspaces[0]`).
 * Feeds `projectCreate({ input: { workspaceId } })`. Not a resource.
 */
export const currentWorkspace = resolveWorkspace;
/**
 * List Railway regions via `regions`. Pass `projectId` to scope the
 * catalog to a project; omit it for the workspace default set.
 */
export const listRegions = Effect.fn(function* (projectId) {
    const regions = yield* railway.regions(projectId === undefined ? {} : { projectId }, regionSelection);
    return regions ?? [];
});
/**
 * Resolve a Railway region by code (`us-west2`, `us-east4`, …), display
 * name, or id.
 */
export const findRegion = (ref) => Effect.gen(function* () {
    const regions = yield* listRegions();
    const found = regions.find((item) => item.region === ref || item.name === ref || item.id === ref);
    if (found === undefined) {
        return yield* notFound("region", ref);
    }
    return found;
});
/**
 * Workspace audit logs. Query-only — Railway has no audit-log
 * create/update/delete, so this is not a resource.
 */
export { AuditLog, getAuditLog, listAuditLogEventTypes, listAuditLogs, } from "./AuditLog.js";
//# sourceMappingURL=Catalog.js.map