import { waitUntilDeleted } from "./GraphQL.js";
import * as railway from "@distilled.cloud/railway";
import * as Data from "effect/Data";
import * as Effect from "effect/Effect";
import * as Stream from "effect/Stream";
import { Unowned } from "../AdoptPolicy.js";
import { isResolved } from "../Diff.js";
import * as Provider from "../Provider.js";
import { Resource } from "../Resource.js";
import { createRailwayName, matchesAlchemyPhysicalName, sanitizeRailwayName, } from "./Metadata.js";
import { ownedProjects, projectEnvironmentIds } from "./Project.js";
const selection = {
    id: true,
    name: true,
    environmentId: true,
    projectId: true,
    status: true,
    domain: true,
    domains: { domain: true, port: true, prefix: true },
    consoleTargetId: true,
    createdAt: true,
};
/**
 * A Railway.CloudAgent is a persistent coding-agent VM in an
 * environment. Sleep keeps the disk; wake re-runs the entrypoint.
 * Cloud agents are Priority Boarding and bill at VM rates while
 * running.
 *
 * @see https://docs.railway.com/cloud-agents
 *
 * ### Create a CloudAgent
 * Pass the Project (or an Environment). Alchemy generates a unique
 * name unless you pass one. `domain` is the public URL for port 8080.
 *
 * **Example:** Next to a Project
 * ```typescript
 * const site = yield* Railway.Project("Site");
 * const agent = yield* Railway.CloudAgent("Coder", {
 *   environment: site,
 * });
 * ```
 *
 * :::caution[Changing `environment` or `name` replaces the agent]
 * Railway has no cloud-agent update API. A new VM is created, then
 * the old one is deleted — the disk is not copied.
 * :::
 *
 * ### A stable name
 * Pass `name` when you need a stable agent name. Changing it later
 * replaces the agent.
 *
 * **Example:** Explicit name
 * ```typescript
 * const agent = yield* Railway.CloudAgent("Coder", {
 *   environment: site,
 *   name: "reviews",
 * });
 * ```
 *
 * ### Variables
 * `variables` are create-only. Pair them with a new agent — Railway
 * does not update variables on an existing VM. Values may reference
 * other services via `Railway.ref`.
 *
 * **Example:** Variables at create
 * ```typescript
 * const agent = yield* Railway.CloudAgent("Coder", {
 *   environment: site,
 *   variables: {
 *     DATABASE_URL: Railway.ref(db, "DATABASE_URL"),
 *   },
 * });
 * ```
 *
 * ### Sleep and wake
 * Sleep stops compute billing and keeps the disk. Wake re-runs the
 * entrypoint with files in place. These are not reconciler steps —
 * call {@link sleepCloudAgent} / {@link wakeCloudAgent}.
 *
 * **Example:** Sleep then wake
 * ```typescript
 * yield* Railway.sleepCloudAgent(agent);
 * yield* Railway.wakeCloudAgent(agent);
 * ```
 *
 * ### Module-scope declarations
 * Resource-valued props accept the resource or an Effect producing it.
 *
 * **Example:** Module-scope CloudAgent
 * ```typescript
 * // src/agent.ts
 * import * as Railway from "alchemy/Railway";
 *
 * export const Site = Railway.Project("Site");
 * export const Coder = Railway.CloudAgent("Coder", {
 *   environment: Site,
 * });
 * ```
 *
 * @resource
 */
export const CloudAgent = Resource("Railway.CloudAgent");
export class CloudAgentNotCreated extends Data.TaggedError("Railway.CloudAgentNotCreated") {
}
export class CloudAgentEnvironmentRequired extends Data.TaggedError("Railway.CloudAgentEnvironmentRequired") {
}
const toAttrs = (agent, fallback) => ({
    cloudAgentId: agent.id,
    name: agent.name || fallback?.name || "",
    environmentId: agent.environmentId || fallback?.environmentId || "",
    projectId: agent.projectId || fallback?.projectId || "",
    status: agent.status,
    domain: agent.domain ?? undefined,
    domains: agent.domains.map((item) => ({
        domain: item.domain,
        port: item.port,
        prefix: item.prefix,
    })),
    consoleTargetId: agent.consoleTargetId ?? undefined,
    createdAt: agent.createdAt,
});
const resolveName = (id, name, existing) => Effect.gen(function* () {
    if (name !== undefined)
        return sanitizeRailwayName(name);
    if (existing !== undefined)
        return existing;
    return yield* createRailwayName(id);
});
const isGone = (agent) => agent === undefined || agent.status === "DELETING";
const environmentIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.environmentId === "string" && rec.environmentId.length > 0
        ? rec.environmentId
        : undefined;
};
const projectIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.projectId === "string" && rec.projectId.length > 0
        ? rec.projectId
        : undefined;
};
const listAgents = (environmentId) => railway
    .cloudAgents({ environmentId, mine: true }, selection)
    .pipe(railway.catchTags(["RailwayNotFound", "RailwayForbidden", "RailwayPlanLimitExceeded"], () => Effect.succeed([])));
const findById = (environmentId, id) => listAgents(environmentId).pipe(Effect.map((items) => items.find((agent) => agent.id === id)));
const findByName = (environmentId, name) => listAgents(environmentId).pipe(Effect.map((items) => items.find((agent) => !isGone(agent) && agent.name === name)));
const listEnvironmentIds = (project) => railway.environments
    .items({ projectId: project.projectId, first: 50 }, { id: true, deletedAt: true })
    .pipe(Stream.filter((env) => env.deletedAt == null), Stream.map((env) => env.id), Stream.runCollect, Effect.map((ids) => {
    const set = new Set(Array.from(ids));
    if (project.environmentId.length > 0) {
        set.add(project.environmentId);
    }
    return Array.from(set);
}), railway.catchTags(["RailwayNotFound"], () => Effect.succeed(project.environmentId.length > 0 ? [project.environmentId] : [])));
const waitUntilGone = (environmentId, cloudAgentId) => waitUntilDeleted("CloudAgent", cloudAgentId, findById(environmentId, cloudAgentId).pipe(Effect.map((agent) => isGone(agent))));
const cloudAgentIdOf = (agent) => (typeof agent === "string" ? agent : agent.cloudAgentId);
/**
 * Sleep a running cloud agent. Compute billing stops; the disk is
 * kept. Waking re-runs the entrypoint.
 */
export const sleepCloudAgent = Effect.fn(function* (agent) {
    return yield* railway.cloudAgentSleep({ id: cloudAgentIdOf(agent) }, selection);
});
/**
 * Wake a sleeping cloud agent. Crashed or failed agents cannot be
 * woken — create a new one instead.
 */
export const wakeCloudAgent = Effect.fn(function* (agent) {
    return yield* railway.cloudAgentWake({ id: cloudAgentIdOf(agent) }, selection);
});
export const CloudAgentProvider = () => Provider.succeed(CloudAgent, {
    stables: ["cloudAgentId", "environmentId", "projectId", "createdAt"],
    nuke: { dependsOn: ["Railway.Project"] },
    diff: Effect.fn(function* ({ news, output }) {
        if (news === undefined || !isResolved(news))
            return undefined;
        if (output === undefined)
            return undefined;
        const nextEnvironmentId = environmentIdOf(news.environment);
        const environmentChanged = nextEnvironmentId !== undefined &&
            nextEnvironmentId !== output.environmentId;
        const nameChanged = news.name !== undefined &&
            sanitizeRailwayName(news.name) !== output.name;
        if (environmentChanged || nameChanged) {
            return { action: "replace" };
        }
        return undefined;
    }),
    read: Effect.fn(function* ({ id, olds, output }) {
        const name = yield* resolveName(id, olds?.name, output?.name);
        const environmentId = output?.environmentId ??
            (olds !== undefined ? environmentIdOf(olds.environment) : undefined);
        if (environmentId === undefined)
            return undefined;
        const found = (output?.cloudAgentId !== undefined
            ? yield* findById(environmentId, output.cloudAgentId)
            : undefined) ?? (yield* findByName(environmentId, name));
        if (found === undefined || isGone(found))
            return undefined;
        const attrs = toAttrs(found, {
            name,
            environmentId,
            projectId: output?.projectId ??
                (olds !== undefined ? projectIdOf(olds.environment) : undefined),
        });
        if (output !== undefined)
            return attrs;
        return matchesAlchemyPhysicalName(found.name) ? attrs : Unowned(attrs);
    }),
    list: Effect.fn(function* () {
        const projects = yield* ownedProjects();
        const rows = yield* Effect.forEach(projects, (project) => Effect.gen(function* () {
            const envIds = yield* projectEnvironmentIds(project);
            const nested = yield* Effect.forEach(envIds, (environmentId) => listAgents(environmentId).pipe(Effect.map((agents) => agents
                .filter((agent) => !isGone(agent) && matchesAlchemyPhysicalName(agent.name))
                .map((agent) => toAttrs(agent, {
                environmentId,
                projectId: project.projectId,
            })))));
            return nested.flat();
        }));
        const seen = new Set();
        const unique = [];
        for (const row of rows.flat()) {
            if (seen.has(row.cloudAgentId))
                continue;
            seen.add(row.cloudAgentId);
            unique.push(row);
        }
        return unique;
    }),
    reconcile: Effect.fn(function* ({ id, news, output }) {
        const props = news ?? {};
        const environmentId = environmentIdOf(props.environment) ?? output?.environmentId;
        if (environmentId === undefined) {
            return yield* new CloudAgentEnvironmentRequired({
                message: "CloudAgent requires a resolved Railway.Project or Railway.Environment",
            });
        }
        const name = yield* resolveName(id, props.name, output?.name);
        const projectId = projectIdOf(props.environment) ?? output?.projectId ?? "";
        let current = output?.cloudAgentId !== undefined
            ? yield* findById(environmentId, output.cloudAgentId)
            : undefined;
        if (current === undefined || isGone(current)) {
            current = yield* findByName(environmentId, name);
        }
        if (current === undefined || isGone(current)) {
            const created = yield* railway
                .createCloudAgent({
                input: {
                    environmentId,
                    name,
                    ...(props.variables !== undefined
                        ? { variables: props.variables }
                        : {}),
                },
            }, selection)
                .pipe(railway.catchTags("RailwayValidationError", () => Effect.succeed(undefined)));
            current =
                created !== undefined && !isGone(created)
                    ? created
                    : yield* findByName(environmentId, name);
        }
        if (current === undefined || isGone(current)) {
            return yield* new CloudAgentNotCreated({ name, environmentId });
        }
        return toAttrs(current, { name, environmentId, projectId });
    }),
    delete: Effect.fn(function* ({ output }) {
        const cloudAgentId = output.cloudAgentId;
        if (cloudAgentId.length === 0)
            return;
        yield* railway
            .deleteCloudAgent({ id: cloudAgentId })
            .pipe(railway.catchTags(["RailwayNotFound"], () => Effect.void));
        if (output.environmentId.length > 0) {
            yield* waitUntilGone(output.environmentId, cloudAgentId);
        }
    }),
});
//# sourceMappingURL=CloudAgent.js.map