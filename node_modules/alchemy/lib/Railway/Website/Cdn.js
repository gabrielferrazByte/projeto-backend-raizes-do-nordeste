import * as railway from "@distilled.cloud/railway";
import * as Data from "effect/Data";
import * as Effect from "effect/Effect";
import * as Schedule from "effect/Schedule";
import { isResolved } from "../../Diff.js";
import * as Provider from "../../Provider.js";
import { Resource } from "../../Resource.js";
/**
 * Enable Railway's built-in CDN on a Website Service. Static assets
 * (by Content-Type) are cached at the edge; HTML follows
 * {@link CdnProps.htmlCaching}.
 *
 * @resource
 */
export const Cdn = Resource("Railway.Website.Cdn");
export class CdnServiceMissing extends Data.TaggedError("Railway.Website.CdnServiceMissing") {
}
const serviceIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.serviceId === "string" && rec.serviceId.length > 0
        ? rec.serviceId
        : undefined;
};
const environmentIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.environmentId === "string" && rec.environmentId.length > 0
        ? rec.environmentId
        : undefined;
};
const cachingInput = (props) => ({
    htmlCaching: (props.htmlCaching ?? "AUTO").toLowerCase(),
    purgeOnDeploy: props.purgeOnDeploy ?? "HTML",
    defaultTtlSeconds: props.defaultTtlSeconds ?? 7200,
});
const edgeConfigSelection = {
    id: true,
    enabled: true,
    caching: {
        mode: true,
        htmlCaching: true,
        purgeOnDeploy: true,
        defaultTtlSeconds: true,
    },
};
export class CdnPublicDomainPending extends Data.TaggedError("Railway.Website.CdnPublicDomainPending") {
}
export class CdnConfigurationPending extends Data.TaggedError("Railway.Website.CdnConfigurationPending") {
}
const getConfig = (serviceId, environmentId) => railway
    .serviceInstance({ serviceId, environmentId }, { edgeConfig: edgeConfigSelection })
    .pipe(Effect.map((instance) => instance.edgeConfig ?? undefined), railway.catchTags("RailwayNotFound", () => Effect.succeed(undefined)));
const getReadyConfig = (serviceId, environmentId) => railway
    .serviceInstance({ serviceId, environmentId }, {
    edgeConfig: edgeConfigSelection,
    domains: {
        serviceDomains: { syncStatus: true },
        customDomains: { syncStatus: true },
    },
})
    .pipe(Effect.flatMap((instance) => [
    ...instance.domains.serviceDomains,
    ...instance.domains.customDomains,
].some((domain) => domain.syncStatus === "ACTIVE" ||
    domain.syncStatus === "UNSPECIFIED")
    ? Effect.succeed(instance.edgeConfig ?? undefined)
    : Effect.fail(new CdnPublicDomainPending({ serviceId, environmentId }))), Effect.retry({
    while: (error) => error._tag === "Railway.Website.CdnPublicDomainPending",
    times: 8,
    schedule: Schedule.spaced("2 seconds"),
}));
export const CdnProvider = () => Provider.succeed(Cdn, {
    stables: ["serviceId", "environmentId", "edgeConfigId"],
    nuke: { dependsOn: ["Railway.Service"] },
    diff: Effect.fn(function* ({ news, output }) {
        if (news === undefined || !isResolved(news) || output === undefined) {
            return undefined;
        }
        const serviceId = serviceIdOf(news.service);
        const environmentId = environmentIdOf(news.environment);
        if ((serviceId !== undefined && serviceId !== output.serviceId) ||
            (environmentId !== undefined && environmentId !== output.environmentId)) {
            return { action: "replace", deleteFirst: true };
        }
        return undefined;
    }),
    list: () => Effect.succeed([]),
    read: Effect.fn(function* ({ olds, output }) {
        const serviceId = output?.serviceId ?? serviceIdOf(olds?.service);
        const environmentId = output?.environmentId ?? environmentIdOf(olds?.environment);
        if (serviceId === undefined || environmentId === undefined)
            return undefined;
        const current = yield* getConfig(serviceId, environmentId);
        return current === undefined
            ? undefined
            : {
                serviceId,
                environmentId,
                edgeConfigId: current.id,
                enabled: current.enabled &&
                    current.caching != null &&
                    current.caching.mode.toLowerCase() !== "off",
            };
    }),
    reconcile: Effect.fn(function* ({ news, output }) {
        const serviceId = serviceIdOf(news.service) ?? output?.serviceId ?? "";
        const environmentId = environmentIdOf(news.environment) ?? output?.environmentId ?? "";
        if (serviceId.length === 0 || environmentId.length === 0) {
            return yield* new CdnServiceMissing({
                message: "Railway.Website.Cdn requires a Service and environment.",
            });
        }
        const config = { caching: cachingInput(news) };
        let current = yield* getReadyConfig(serviceId, environmentId);
        if (current?.enabled !== true ||
            current.caching == null ||
            current.caching.mode.toLowerCase() === "off") {
            current = yield* railway.enableServiceCdn({ input: { environmentId, serviceId } }, edgeConfigSelection);
        }
        const caching = current?.caching;
        const changed = caching?.htmlCaching !== config.caching.htmlCaching ||
            caching?.purgeOnDeploy !== config.caching.purgeOnDeploy ||
            caching?.defaultTtlSeconds !== config.caching.defaultTtlSeconds;
        if (changed) {
            yield* railway.updateServiceEdgeConfig({ input: { serviceId, environmentId, config } }, { id: true });
        }
        const enabled = yield* getConfig(serviceId, environmentId).pipe(Effect.flatMap((observed) => observed?.enabled === true &&
            observed.caching != null &&
            observed.caching.mode.toLowerCase() !== "off" &&
            observed.caching.htmlCaching === config.caching.htmlCaching &&
            observed.caching.purgeOnDeploy === config.caching.purgeOnDeploy &&
            observed.caching.defaultTtlSeconds ===
                config.caching.defaultTtlSeconds
            ? Effect.succeed(observed)
            : Effect.fail(new CdnConfigurationPending({ serviceId, environmentId }))), Effect.retry({
            while: (error) => error._tag === "Railway.Website.CdnConfigurationPending",
            times: 8,
            schedule: Schedule.spaced("1 second"),
        }));
        return {
            serviceId,
            environmentId,
            edgeConfigId: enabled.id,
            enabled: enabled.enabled,
        };
    }),
    delete: Effect.fn(function* ({ output }) {
        if (output === undefined)
            return;
        const current = yield* getConfig(output.serviceId, output.environmentId);
        if (current === undefined || !current.enabled)
            return;
        yield* railway.disableServiceCdn({
            input: {
                environmentId: output.environmentId,
                serviceId: output.serviceId,
            },
        });
    }),
});
//# sourceMappingURL=Cdn.js.map