import * as railway from "@distilled.cloud/railway";
import * as Effect from "effect/Effect";
import * as Provider from "../../Provider.ts";
import { Resource } from "../../Resource.ts";
import type { Providers } from "../Providers.ts";
type PurgeOnDeploy = railway.Scalars["PurgeOnDeploy"];
type Ref<T> = T | Effect.Effect<T, never, Providers>;
export type CdnService = {
    readonly serviceId: string;
};
export type CdnEnvironment = {
    readonly environmentId: string;
};
export interface CdnProps {
    /**
     * Parent Railway Service whose domains receive CDN caching.
     */
    service: Ref<CdnService>;
    /**
     * Environment the Service instance lives in.
     */
    environment: Ref<CdnEnvironment>;
    /**
     * HTML caching mode. `"AUTO"` caches HTML only when the origin sends
     * `max-age` / `s-maxage`. `"FORCE"` uses the default TTL. `"NEVER"`
     * skips HTML.
     * @default "AUTO"
     */
    htmlCaching?: "AUTO" | "FORCE" | "NEVER" | (string & {});
    /**
     * What to purge after a successful deploy.
     * @default "HTML"
     */
    purgeOnDeploy?: PurgeOnDeploy;
    /**
     * Fallback TTL in seconds when the origin sends no freshness.
     * @default 7200
     */
    defaultTtlSeconds?: number;
}
export interface Cdn extends Resource<"Railway.Website.Cdn", CdnProps, {
    serviceId: string;
    environmentId: string;
    edgeConfigId: string;
    enabled: boolean;
}, never, Providers> {
}
/**
 * Enable Railway's built-in CDN on a Website Service. Static assets
 * (by Content-Type) are cached at the edge; HTML follows
 * {@link CdnProps.htmlCaching}.
 *
 * @resource
 */
export declare const Cdn: import("../../Resource.ts").ResourceClass<Cdn>;
declare const CdnServiceMissing_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.Website.CdnServiceMissing";
} & Readonly<A>;
export declare class CdnServiceMissing extends CdnServiceMissing_base<{
    message: string;
}> {
}
declare const CdnPublicDomainPending_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.Website.CdnPublicDomainPending";
} & Readonly<A>;
export declare class CdnPublicDomainPending extends CdnPublicDomainPending_base<{
    serviceId: string;
    environmentId: string;
}> {
}
declare const CdnConfigurationPending_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.Website.CdnConfigurationPending";
} & Readonly<A>;
export declare class CdnConfigurationPending extends CdnConfigurationPending_base<{
    serviceId: string;
    environmentId: string;
}> {
}
export declare const CdnProvider: () => import("effect/Layer").Layer<Provider.Provider<Cdn>, never, railway.GraphQLRequirements>;
export {};
//# sourceMappingURL=Cdn.d.ts.map