import * as railway from "@distilled.cloud/railway";
import * as Effect from "effect/Effect";
import * as Provider from "../Provider.ts";
import { Resource } from "../Resource.ts";
import type { Providers } from "./Providers.ts";
type PrivateNetworkEndpointSyncStatus = railway.Scalars["PrivateNetworkEndpointSyncStatus"];
/**
 * A resource-valued prop: the resource itself, or an Effect that produces
 * it (so `yield* Project(...)` and `Project(...)` both type-check).
 */
type Ref<T> = T | Effect.Effect<T, never, Providers>;
declare const PrivateNetworkConfigPending_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkConfigPending";
} & Readonly<A>;
export declare class PrivateNetworkConfigPending extends PrivateNetworkConfigPending_base<{
    environmentId: string;
    message: string;
}> {
}
declare const PrivateNetworkNameUnsupported_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkNameUnsupported";
} & Readonly<A>;
export declare class PrivateNetworkNameUnsupported extends PrivateNetworkNameUnsupported_base<{
    name: string;
}> {
    get message(): string;
}
/**
 * Environment identity a private network lives in. Accepts a
 * `Railway.Project` (its primary environment), a `Railway.Environment`,
 * or an `{ environmentId, projectId? }` stub.
 */
export type PrivateNetworkEnvironment = {
    readonly environmentId: string;
    readonly projectId?: string;
};
export interface PrivateNetworkProps {
    /**
     * Environment whose platform-managed private network is enabled. Accepts a
     * `Railway.Project`, `Railway.Environment`, or `{ environmentId, projectId }`.
     * Changing it replaces the configuration resource.
     */
    environment: Ref<PrivateNetworkEnvironment>;
    /**
     * Only the platform name `railway` is supported. Custom names are rejected.
     * @deprecated Omit this property; Railway names the environment's network.
     */
    name?: string;
}
export type PrivateNetwork = Resource<"Railway.PrivateNetwork", PrivateNetworkProps, {
    /** Railway public network id (string identity used by endpoint APIs). */
    publicId: string;
    /** Private-networking setting to restore when this resource is removed. */
    previousPrivateNetworkDisabled?: boolean;
    /** Numeric WireGuard network id, as a decimal string. */
    networkId: string;
    /** Physical network name (unique per environment). */
    name: string;
    /** Network DNS label reported by Railway. */
    dnsName: string;
    /** Parent Railway project id. */
    projectId: string;
    /** Environment the network lives in. */
    environmentId: string;
    /** Observed tags. */
    tags: string[];
    /** RFC3339 creation timestamp, if Railway reported one. */
    createdAt: string | undefined;
}, never, Providers>;
declare const PrivateNetworkResource: import("../Resource.ts").ResourceClass<PrivateNetwork>;
/**
 * Enable Railway's platform-managed private network for an environment.
 * This resource manages the environment's networking setting, not a separately
 * created network. Use one PrivateNetwork resource per environment.
 *
 * Destroy restores the setting captured before reconciliation. A network that
 * was already enabled stays enabled; one enabled by this resource is disabled.
 * Platform network objects follow the environment's lifecycle.
 *
 * :::caution[Named networks are no longer supported]
 * Railway removed named-network creation. Remove legacy named-network
 * declarations before adding this environment-managed resource. Custom names
 * are rejected rather than silently mapped to the platform network.
 * :::
 *
 * ### Enable private networking
 * Pass a Project or Environment. `dnsName` is Railway's network DNS label.
 *
 * **Example:** Environment-managed network
 * ```typescript
 * const site = yield* Railway.Project("Site");
 * const net = yield* Railway.PrivateNetwork("Mesh", {
 *   environment: site,
 * });
 * ```
 *
 * ### Endpoints
 * Configure a deployed service's DNS prefix via
 * {@link PrivateNetworkEndpoint}.
 *
 * **Example:** Endpoint on the network
 * ```typescript
 * const endpoint = yield* Railway.PrivateNetworkEndpoint("ApiDns", {
 *   network: net,
 *   service: { serviceId: "deployed-service-id" },
 *   name: "api",
 * });
 * ```
 *
 * ### Module-scope declarations
 * Resource-valued props accept the resource or an Effect producing it.
 *
 * **Example:** Module-scope network
 * ```typescript
 * // src/network.ts
 * import * as Railway from "alchemy/Railway";
 *
 * export const Site = Railway.Project("Site");
 * export const Mesh = Railway.PrivateNetwork("Mesh", {
 *   environment: Site,
 * });
 * ```
 *
 * @resource
 */
export declare const PrivateNetwork: typeof PrivateNetworkResource;
declare const PrivateNetworkNotCreated_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkNotCreated";
} & Readonly<A>;
export declare class PrivateNetworkNotCreated extends PrivateNetworkNotCreated_base<{
    name: string;
    environmentId: string;
}> {
}
declare const PrivateNetworkEnvironmentRequired_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkEnvironmentRequired";
} & Readonly<A>;
export declare class PrivateNetworkEnvironmentRequired extends PrivateNetworkEnvironmentRequired_base<{
    message: string;
}> {
}
export declare const PrivateNetworkProvider: () => import("effect/Layer").Layer<Provider.Provider<PrivateNetwork>, never, import("./Environment.ts").RailwayEnvironment | railway.GraphQLRequirements>;
/**
 * Network identity an endpoint attaches to. Accepts a
 * `Railway.PrivateNetwork` or a `{ publicId, environmentId? }` stub.
 */
export type PrivateNetworkEndpointNetwork = {
    readonly publicId: string;
    readonly environmentId?: string;
    readonly projectId?: string;
    readonly dnsName?: string;
};
/**
 * Service identity an endpoint attaches to. Accepts a `Railway.Service`
 * or a `{ serviceId, name? }` stub.
 */
export type PrivateNetworkEndpointService = {
    readonly serviceId: string;
    readonly name?: string;
};
export interface PrivateNetworkEndpointProps {
    /**
     * Platform-managed network. Accepts a `Railway.PrivateNetwork` or
     * `{ publicId, environmentId }`. Changing it replaces the configuration resource.
     */
    network: Ref<PrivateNetworkEndpointNetwork>;
    /**
     * Service the endpoint advertises. Accepts a `Railway.Service` or
     * `{ serviceId }`. Changing it replaces the endpoint.
     */
    service: Ref<PrivateNetworkEndpointService>;
    /**
     * Service DNS prefix. Defaults to the service name.
     * Updates the service's environment configuration without replacing its endpoint.
     */
    name?: string;
}
export type PrivateNetworkEndpoint = Resource<"Railway.PrivateNetworkEndpoint", PrivateNetworkEndpointProps, {
    /** Railway public endpoint id. */
    publicId: string;
    /** Observed DNS prefix, not a fully qualified hostname. */
    dnsName: string;
    /** Pending DNS name while a rename is in flight, if any. */
    newDnsName: string | undefined;
    /** Internal IPs advertised on the mesh. */
    privateIps: string[];
    /** Service instance the endpoint is bound to. */
    serviceInstanceId: string;
    /** Previous DNS override to restore on removal; null restores the platform default. */
    previousDnsPrefix?: string | null;
    /** Parent service id. */
    serviceId: string;
    /** Parent network public id. */
    privateNetworkId: string;
    /** Environment the endpoint lives in. */
    environmentId: string;
    /** Parent Railway project id, if known. */
    projectId: string | undefined;
    /** Observed Railway sync status (`ACTIVE`, `CREATING`, …). */
    syncStatus: PrivateNetworkEndpointSyncStatus;
    /** Observed tags. */
    tags: string[];
    /** RFC3339 creation timestamp, if Railway reported one. */
    createdAt: string | undefined;
}, never, Providers>;
declare const PrivateNetworkEndpointResource: import("../Resource.ts").ResourceClass<PrivateNetworkEndpoint>;
/**
 * Configure the DNS prefix of a service's platform-managed endpoint on a
 * {@link PrivateNetwork}. The service must already have an instance in the
 * environment. Use one endpoint configuration resource per service/environment.
 *
 * Destroy restores the previous DNS override if the current override still
 * matches this resource's value. Unrelated or externally changed settings are
 * preserved. The platform endpoint itself follows the service's lifecycle.
 * Legacy state without a captured DNS override cannot be restored: deletion
 * fails explicitly while the endpoint exists, rather than silently forgetting it.
 *
 * ### Configure a service
 * Pass the network and service. Omit `name` to use the service name
 * as the DNS prefix.
 *
 * **Example:** Default prefix
 * ```typescript
 * const endpoint = yield* Railway.PrivateNetworkEndpoint("ApiDns", {
 *   network: net,
 *   service: api,
 * });
 * ```
 *
 * ### Custom DNS name
 * `name` sets the service's private DNS prefix.
 * Updating it renames in place.
 *
 * **Example:** Custom prefix
 * ```typescript
 * const endpoint = yield* Railway.PrivateNetworkEndpoint("ApiDns", {
 *   network: net,
 *   service: api,
 *   name: "api",
 * });
 * ```
 *
 * :::caution[Changing the target replaces the configuration resource]
 * Changing `network` or `service` restores the old target's prior DNS override
 * and applies the desired prefix to the new target. It does not delete either
 * platform-managed endpoint.
 * :::
 *
 * @resource
 */
export declare const PrivateNetworkEndpoint: typeof PrivateNetworkEndpointResource;
declare const PrivateNetworkEndpointNotCreated_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkEndpointNotCreated";
} & Readonly<A>;
export declare class PrivateNetworkEndpointNotCreated extends PrivateNetworkEndpointNotCreated_base<{
    privateNetworkId: string;
    serviceId: string;
    expectedPrefix?: string;
    observedPrefix?: string;
    pendingPrefix?: string;
}> {
    get message(): string;
}
declare const PrivateNetworkEndpointRestoreUnavailable_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkEndpointRestoreUnavailable";
} & Readonly<A>;
export declare class PrivateNetworkEndpointRestoreUnavailable extends PrivateNetworkEndpointRestoreUnavailable_base<{
    serviceId: string;
    environmentId: string;
}> {
    get message(): string;
}
declare const PrivateNetworkEndpointTargetMissing_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkEndpointTargetMissing";
} & Readonly<A>;
export declare class PrivateNetworkEndpointTargetMissing extends PrivateNetworkEndpointTargetMissing_base<{
    message: string;
}> {
}
declare const PrivateNetworkEndpointNameUnavailable_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "Railway.PrivateNetworkEndpointNameUnavailable";
} & Readonly<A>;
export declare class PrivateNetworkEndpointNameUnavailable extends PrivateNetworkEndpointNameUnavailable_base<{
    name: string;
    privateNetworkId: string;
    serviceId: string;
}> {
}
export declare const PrivateNetworkEndpointProvider: () => import("effect/Layer").Layer<Provider.Provider<PrivateNetworkEndpoint>, never, import("./Environment.ts").RailwayEnvironment | railway.GraphQLRequirements>;
export {};
//# sourceMappingURL=PrivateNetwork.d.ts.map