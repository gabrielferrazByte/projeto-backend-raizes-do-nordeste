import { projectBuckets } from "./GraphQL.js";
import * as railway from "@distilled.cloud/railway";
import * as Data from "effect/Data";
import * as Effect from "effect/Effect";
import * as Redacted from "effect/Redacted";
import * as Schedule from "effect/Schedule";
import * as Stream from "effect/Stream";
import { Unowned } from "../AdoptPolicy.js";
import { isResolved } from "../Diff.js";
import * as Provider from "../Provider.js";
import { Resource } from "../Resource.js";
import { createRailwayName, matchesAlchemyPhysicalName, sanitizeRailwayName, } from "./Metadata.js";
import { ownedProjects, projectEnvironmentIds, } from "./Project.js";
import { withEnvironmentConfigLock } from "./transient.js";
const selection = {
    id: true,
    name: true,
    projectId: true,
    createdAt: true,
    updatedAt: true,
};
const credentialsSelection = {
    accessKeyId: true,
    bucketName: true,
    endpoint: true,
    region: true,
    secretAccessKey: true,
    urlStyle: true,
};
/** Default Railway bucket region (US West). Changing region replaces. */
export const DEFAULT_BUCKET_REGION = "sjc";
const resolveBucketProps = (props) => Effect.gen(function* () {
    const resolved = Effect.isEffect(props) ? yield* props : props;
    if (globalThis.__ALCHEMY_RUNTIME__)
        return resolved;
    const project = Effect.isEffect(resolved.project)
        ? yield* resolved.project
        : resolved.project;
    const environment = resolved.environment === undefined
        ? undefined
        : Effect.isEffect(resolved.environment)
            ? yield* resolved.environment
            : resolved.environment;
    return { ...resolved, project, environment };
});
const BucketResource = Resource("Railway.Bucket");
/**
 * A Railway.Bucket is S3-compatible object storage in a Project. Each
 * environment gets its own isolated instance and credentials. Bind
 * {@link PutObject} / {@link GetObject} (and the other S3 object ops) in
 * Service init — Alchemy injects the endpoint and credentials.
 *
 * Railway has no labels. Ownership is stamped into the display name via
 * `createPhysicalName`. `name` updates in place. Changing `project`,
 * `environment`, or `region` replaces the Bucket.
 *
 * @see https://docs.railway.com/storage-buckets
 *
 * ### Create a Bucket
 * Pass a Project. Alchemy generates a unique name and deploys to the
 * project's primary environment in `sjc`.
 *
 * **Example:** Generated name
 * ```typescript
 * const site = yield* Railway.Project("Site");
 * const data = yield* Railway.Bucket("Data", { project: site });
 * ```
 *
 * :::caution[Changing `project` replaces the Bucket]
 * The Bucket is created in the new Project. The old Bucket is deleted.
 * :::
 *
 * ### A stable name
 * Pass `name` when you need a stable display name. The S3 API name is
 * this plus a short hash (`s3BucketName`).
 *
 * **Example:** Explicit name
 * ```typescript
 * const data = yield* Railway.Bucket("Data", {
 *   project: site,
 *   name: "uploads",
 * });
 * ```
 *
 * ### Region
 * Omit `region` to use `sjc`. Changing it replaces the Bucket.
 *
 * **Example:** Pin a region
 * ```typescript
 * const data = yield* Railway.Bucket("Data", {
 *   project: site,
 *   region: "iad",
 * });
 * ```
 *
 * :::caution[Changing `region` replaces the Bucket]
 * Railway cannot move a bucket. A new Bucket is created, then the old
 * one is deleted.
 * :::
 *
 * ### Environment
 * Defaults to the Project's primary environment. Pass a
 * `Railway.Environment` (or `{ environmentId }`) to target another one.
 *
 * **Example:** Extra environment
 * ```typescript
 * const staging = yield* Railway.Environment("Staging", { project: site });
 * const data = yield* Railway.Bucket("StagingData", {
 *   project: site,
 *   environment: staging,
 * });
 * ```
 *
 * :::caution[Changing `environment` replaces the Bucket]
 * The Bucket instance is created in the new environment. The old
 * instance is deleted.
 * :::
 *
 * ### Put and get objects
 * Railway buckets speak the S3 API. Bind {@link PutObject} /
 * {@link GetObject} in Service init.
 *
 * **Example:** Put an object from a Service
 * ```typescript
 * import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
 *
 * export default class Api extends Railway.Service<Api>()(
 *   "Api",
 *   { project: Site, main: import.meta.url },
 *   Effect.gen(function* () {
 *     const putObject = yield* Railway.PutObject(Data);
 *     const getObject = yield* Railway.GetObject(Data);
 *
 *     return {
 *       fetch: Effect.gen(function* () {
 *         yield* putObject({
 *           Key: "hello.txt",
 *           Body: "hello",
 *           ContentType: "text/plain",
 *         });
 *         const obj = yield* getObject({ Key: "hello.txt" });
 *         return HttpServerResponse.json({ ok: obj.ETag !== undefined });
 *       }),
 *     };
 *   }).pipe(Effect.provide([Railway.PutObjectHttp, Railway.GetObjectHttp])),
 * ) {}
 * ```
 *
 * ### Module-scope declarations
 * Declare the Project once. Pass it into every child. Resource-valued
 * props accept the resource or an Effect producing it.
 *
 * **Example:** Module-scope Bucket
 * ```typescript
 * // src/data.ts
 * import * as Railway from "alchemy/Railway";
 *
 * export const Site = Railway.Project("Site");
 * export const Data = Railway.Bucket("Data", { project: Site });
 * ```
 *
 * @resource
 */
export const Bucket = Object.assign((id, props) => BucketResource(id, resolveBucketProps(props)), BucketResource);
export class BucketNotCreated extends Data.TaggedError("Railway.BucketNotCreated") {
}
export class BucketProjectRequired extends Data.TaggedError("Railway.BucketProjectRequired") {
}
export class BucketEnvironmentRequired extends Data.TaggedError("Railway.BucketEnvironmentRequired") {
}
class BucketCredentialsPending extends Data.TaggedError("Railway.BucketCredentialsPending") {
}
class BucketDeployPending extends Data.TaggedError("Railway.BucketDeployPending") {
}
const projectIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.projectId === "string" && rec.projectId.length > 0
        ? rec.projectId
        : undefined;
};
const environmentIdOf = (value) => {
    if (value === null || typeof value !== "object")
        return undefined;
    const rec = value;
    return typeof rec.environmentId === "string" && rec.environmentId.length > 0
        ? rec.environmentId
        : undefined;
};
const redact = (value) => value !== undefined && value.length > 0 ? Redacted.make(value) : undefined;
const keepSecret = (next, previous) => next ?? previous;
const parseEnvironmentConfig = (value) => {
    if (value === null || typeof value !== "object" || Array.isArray(value)) {
        return {};
    }
    const buckets = value.buckets;
    if (buckets === null ||
        typeof buckets !== "object" ||
        Array.isArray(buckets)) {
        return {};
    }
    return { buckets: buckets };
};
const instanceOf = (config, bucketId) => {
    const row = config.buckets?.[bucketId];
    if (row === undefined || row === null)
        return undefined;
    return row;
};
const isDeployed = (config, bucketId) => {
    const row = instanceOf(config, bucketId);
    return row !== undefined && row.isDeleted !== true;
};
const toAttrs = (bucket, extra) => ({
    bucketId: bucket.id,
    name: bucket.name,
    projectId: bucket.projectId,
    environmentId: extra.environmentId,
    region: extra.region ?? extra.previous?.region,
    s3BucketName: extra.credentials?.bucketName ?? extra.previous?.s3BucketName,
    endpoint: extra.credentials?.endpoint ?? extra.previous?.endpoint,
    urlStyle: extra.credentials?.urlStyle ?? extra.previous?.urlStyle,
    s3Region: extra.credentials?.region ?? extra.previous?.s3Region,
    accessKeyId: keepSecret(redact(extra.credentials?.accessKeyId), extra.previous?.accessKeyId),
    secretAccessKey: keepSecret(redact(extra.credentials?.secretAccessKey), extra.previous?.secretAccessKey),
    createdAt: bucket.createdAt,
    updatedAt: bucket.updatedAt,
});
const resolveName = (id, name, existing) => Effect.gen(function* () {
    if (name !== undefined)
        return sanitizeRailwayName(name);
    if (existing !== undefined)
        return existing;
    return yield* createRailwayName(id);
});
const listProjectBuckets = (projectId) => projectBuckets(projectId, selection).pipe(railway.catchTags(["RailwayNotFound"], () => Effect.succeed([])));
const findInProject = (projectId, match) => listProjectBuckets(projectId).pipe(Effect.map((buckets) => buckets.find(match)));
const getEnvironmentConfig = (environmentId, projectId) => railway.environment({ id: environmentId, projectId }, { config: true }).pipe(Effect.map((env) => parseEnvironmentConfig(env.config)), Effect.retry({
    while: (e) => railway.isErrorTag(e, "RailwayNotFound"),
    times: 8,
    schedule: Schedule.spaced("1 second"),
}), railway.catchTags(["RailwayNotFound"], () => Effect.succeed({})));
const environmentIdsOf = (project) => railway.environments
    .items({ projectId: project.projectId, first: 50 }, { id: true, deletedAt: true })
    .pipe(Stream.filter((env) => env.deletedAt == null), Stream.map((env) => env.id), Stream.runCollect, Effect.map((ids) => {
    const set = new Set(Array.from(ids));
    if (project.environmentId.length > 0) {
        set.add(project.environmentId);
    }
    return Array.from(set);
}), railway.catchTags(["RailwayNotFound"], () => Effect.succeed(project.environmentId.length > 0 ? [project.environmentId] : [])));
const commitBucketPatch = (input) => withEnvironmentConfigLock(input.environmentId, railway.environmentPatchCommit({
    environmentId: input.environmentId,
    commitMessage: input.commitMessage,
    patch: input.patch,
}));
const ensureDeployed = Effect.fn(function* (input) {
    const config = yield* getEnvironmentConfig(input.environmentId, input.projectId);
    if (isDeployed(config, input.bucketId)) {
        return instanceOf(config, input.bucketId);
    }
    yield* commitBucketPatch({
        environmentId: input.environmentId,
        commitMessage: `Alchemy: create bucket ${input.name}`,
        patch: {
            buckets: {
                [input.bucketId]: {
                    region: input.region,
                    isCreated: true,
                },
            },
        },
    }).pipe(Effect.retry({
        while: (e) => railway.isErrorTag(e, "RailwayNotFound"),
        times: 8,
        schedule: Schedule.spaced("1 second"),
    }));
    const synced = yield* getEnvironmentConfig(input.environmentId, input.projectId).pipe(Effect.flatMap((next) => {
        if (!isDeployed(next, input.bucketId)) {
            return Effect.fail(new BucketDeployPending({ bucketId: input.bucketId }));
        }
        return Effect.succeed(instanceOf(next, input.bucketId));
    }), Effect.retry({
        while: (e) => e._tag === "Railway.BucketDeployPending",
        times: 8,
        schedule: Schedule.spaced("1 second"),
    }));
    return synced;
});
const fetchCredentials = (input) => railway
    .bucketS3Credentials({
    bucketId: input.bucketId,
    environmentId: input.environmentId,
    projectId: input.projectId,
}, credentialsSelection)
    .pipe(Effect.flatMap((items) => {
    const first = items[0];
    if (first === undefined) {
        return Effect.fail(new BucketCredentialsPending({ bucketId: input.bucketId }));
    }
    return Effect.succeed(first);
}), railway.catchTags(["RailwayNotFound", "RailwayBucketCredentialsNotReady"], () => Effect.fail(new BucketCredentialsPending({ bucketId: input.bucketId }))), Effect.retry({
    while: (e) => e._tag === "Railway.BucketCredentialsPending",
    times: 4,
    schedule: Schedule.spaced("2 seconds"),
}));
const waitUntilGone = (input) => getEnvironmentConfig(input.environmentId, input.projectId).pipe(Effect.flatMap((config) => !isDeployed(config, input.bucketId)
    ? Effect.void
    : Effect.fail(new BucketDeployPending({ bucketId: input.bucketId }))), Effect.retry({
    schedule: Schedule.spaced("1 second"),
    while: (error) => error._tag === "Railway.BucketDeployPending",
    times: 8,
}));
export const BucketProvider = () => Provider.succeed(Bucket, {
    stables: ["bucketId", "projectId", "environmentId", "createdAt"],
    nuke: { dependsOn: ["Railway.Project"] },
    diff: Effect.fn(function* ({ news, output }) {
        if (news === undefined || !isResolved(news))
            return undefined;
        if (output === undefined)
            return undefined;
        const nextProject = projectIdOf(news.project);
        const projectChanged = nextProject !== undefined && nextProject !== output.projectId;
        const nextEnv = environmentIdOf(news.environment);
        const environmentChanged = nextEnv !== undefined && nextEnv !== output.environmentId;
        const desiredRegion = news.region ?? DEFAULT_BUCKET_REGION;
        const regionChanged = output.region !== undefined && desiredRegion !== output.region;
        if (projectChanged || environmentChanged || regionChanged) {
            return { action: "replace" };
        }
        return undefined;
    }),
    read: Effect.fn(function* ({ id, olds, output }) {
        const projectId = output?.projectId ??
            (olds !== undefined ? projectIdOf(olds.project) : undefined);
        const environmentId = output?.environmentId ??
            (olds !== undefined
                ? (environmentIdOf(olds.environment) ?? environmentIdOf(olds.project))
                : undefined);
        const name = yield* resolveName(id, olds?.name, output?.name);
        if (projectId === undefined)
            return undefined;
        const found = (output?.bucketId !== undefined && output.bucketId.length > 0
            ? yield* findInProject(projectId, (bucket) => bucket.id === output.bucketId)
            : undefined) ??
            (yield* findInProject(projectId, (bucket) => bucket.name === name));
        if (found === undefined)
            return undefined;
        const envId = environmentId ?? output?.environmentId ?? "";
        const config = envId.length > 0
            ? yield* getEnvironmentConfig(envId, projectId)
            : {};
        if (envId.length > 0 && !isDeployed(config, found.id)) {
            return undefined;
        }
        const attrs = toAttrs(found, {
            environmentId: envId,
            region: instanceOf(config, found.id)?.region ?? output?.region,
            previous: output,
        });
        if (output !== undefined)
            return attrs;
        return matchesAlchemyPhysicalName(found.name) ? attrs : Unowned(attrs);
    }),
    list: Effect.fn(function* () {
        const projects = yield* ownedProjects();
        const rows = yield* Effect.forEach(projects, (project) => Effect.gen(function* () {
            const envIds = yield* projectEnvironmentIds(project);
            const buckets = yield* listProjectBuckets(project.projectId);
            const nested = yield* Effect.forEach(envIds, (environmentId) => getEnvironmentConfig(environmentId, project.projectId).pipe(Effect.map((config) => buckets
                .filter((bucket) => matchesAlchemyPhysicalName(bucket.name) &&
                isDeployed(config, bucket.id))
                .map((bucket) => toAttrs(bucket, {
                environmentId,
                region: instanceOf(config, bucket.id)?.region ?? undefined,
            })))));
            return nested.flat();
        }));
        const seen = new Set();
        const unique = [];
        for (const row of rows.flat()) {
            const key = `${row.bucketId}:${row.environmentId}`;
            if (seen.has(key))
                continue;
            seen.add(key);
            unique.push(row);
        }
        return unique;
    }),
    reconcile: Effect.fn(function* ({ id, news, output }) {
        const props = news ?? {};
        const projectId = projectIdOf(props.project) ?? output?.projectId;
        if (projectId === undefined) {
            return yield* new BucketProjectRequired({
                message: "Bucket requires a resolved Railway.Project",
            });
        }
        const environmentId = environmentIdOf(props.environment) ??
            environmentIdOf(props.project) ??
            output?.environmentId;
        if (environmentId === undefined) {
            return yield* new BucketEnvironmentRequired({
                message: "Bucket requires a Railway environment (pass environment or a Project with environmentId)",
            });
        }
        const name = yield* resolveName(id, props.name, output?.name);
        const region = props.region ?? output?.region ?? DEFAULT_BUCKET_REGION;
        let current = output?.bucketId !== undefined && output.bucketId.length > 0
            ? yield* findInProject(projectId, (bucket) => bucket.id === output.bucketId)
            : undefined;
        if (current === undefined) {
            current = yield* findInProject(projectId, (bucket) => bucket.name === name);
        }
        if (current === undefined) {
            const created = yield* railway
                .createBucket({
                input: {
                    projectId,
                    name,
                },
            }, selection)
                .pipe(Effect.retry({
                while: (e) => railway.isErrorTag(e, "RailwayNotFound"),
                times: 8,
                schedule: Schedule.spaced("1 second"),
            }), railway.catchTags("RailwayValidationError", () => Effect.succeed(undefined)));
            current =
                created ??
                    (yield* findInProject(projectId, (bucket) => bucket.name === name));
        }
        if (current === undefined) {
            return yield* new BucketNotCreated({ name, projectId });
        }
        if (current.name !== name) {
            current = yield* railway.updateBucket({
                id: current.id,
                input: { name },
            }, selection);
        }
        const deployed = yield* ensureDeployed({
            bucketId: current.id,
            environmentId,
            projectId,
            region,
            name,
        });
        const credentials = yield* fetchCredentials({
            bucketId: current.id,
            environmentId,
            projectId,
        });
        return toAttrs(current, {
            environmentId,
            region: deployed?.region ?? region,
            credentials,
            previous: output,
        });
    }),
    delete: Effect.fn(function* ({ output }) {
        const bucketId = output.bucketId;
        const environmentId = output.environmentId;
        const projectId = output.projectId;
        if (bucketId.length === 0 || environmentId.length === 0)
            return;
        yield* commitBucketPatch({
            environmentId,
            commitMessage: `Alchemy: delete bucket ${output.name}`,
            patch: {
                buckets: {
                    [bucketId]: { isDeleted: true },
                },
            },
        }).pipe(railway.catchTags(["RailwayNotFound"], () => Effect.void));
        if (projectId.length > 0) {
            yield* waitUntilGone({ bucketId, projectId, environmentId });
        }
    }),
});
//# sourceMappingURL=Bucket.js.map