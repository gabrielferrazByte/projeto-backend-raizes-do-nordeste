import { Fragment as _Fragment, jsx as _jsx, jsxs as _jsxs } from "@alchemy.run/sigil/jsx-runtime";
/** @jsxImportSource @alchemy.run/sigil */
/**
 * GUI-style dashboard behind bare `alchemy profile`. One Sigil app stays
 * mounted for the whole session and screens replace each other in place:
 *
 *   overview — chip tabs (default profile first), selected profile's
 *              provider details with an up/down focus cursor, keybind bar,
 *              inline rename/new/delete/remove
 *   edit     — replaces the overview: per-provider cycle rows
 *              (keep / reconfigure / remove, add for unconnected)
 *
 * Pure store actions (create/rename/delete) round-trip through
 * the hub via a request bridge WITHOUT unmounting — results come back as
 * in-app notices. Only flows that must prompt in the transcript (provider
 * configuration, credential refresh) resolve the session. The built-in
 * `default` profile is pinned first and can be neither renamed nor deleted.
 */
import * as Effect from "effect/Effect";
import * as Fiber from "effect/Fiber";
import * as Scheduler from "effect/Scheduler";
import { useEffect, useState } from "@alchemy.run/sigil/react";
import { Alert, Box, CycleList, Gutter, InlineConfirm, KeyBar, LiveStore, Pointer, PromptFrame, Spinner, Stack, Status, Tabs, Text, TextField, Toast, useCycleNavigation, useGlyphs, useKeyGlyphs, useLiveStore, useTerminalInput, useTerminalSize, VirtualList, } from "../ui/index.js";
import { CliKit, theme, } from "../../CliKit/index.js";
import { editStateStyle, ProviderBlock, providerBlockHeight, providerColumnWidths, } from "./Profile.js";
/**
 * Immutable-snapshot store on top of `LiveStore`, so every mutation notifies
 * the mounted dashboard. The resolver bridge and the notice auto-dismiss
 * timer live outside the snapshot — they carry no visual state.
 */
export class DashStore extends LiveStore {
    resolver = null;
    constructor(entries) {
        super({
            entries,
            details: new Map(),
            notice: undefined,
            busy: false,
            focus: undefined,
            flow: undefined,
        });
    }
    detailsFor(name) {
        return this.snapshot().details.get(name) ?? { state: "loading" };
    }
    detailNames() {
        return new Set(this.snapshot().details.keys());
    }
    setDetails(name, details) {
        this.update((state) => ({
            ...state,
            details: new Map(state.details).set(name, details),
        }));
    }
    setFlow(flow) {
        this.update((state) => ({ ...state, flow }));
    }
    clearFocus() {
        this.update((state) => ({ ...state, focus: undefined }));
    }
    dispose() {
        this.resolver = null;
    }
    clearNotice() {
        this.update((state) => ({ ...state, notice: undefined }));
    }
    applyResult(result) {
        this.update((state) => ({
            ...state,
            entries: result.entries,
            notice: { ok: result.ok, message: result.message },
            focus: result.ok ? result.selected : undefined,
            busy: false,
        }));
    }
    bindResolver(resolve) {
        this.resolver = resolve;
    }
    dispatch = (action) => {
        if (this.snapshot().busy || this.resolver === null)
            return;
        const resolve = this.resolver;
        this.resolver = null;
        this.update((state) => ({ ...state, busy: true, notice: undefined }));
        resolve(action);
    };
}
function DetailsPane({ details, refreshingProvider, focusedIndex, }) {
    if (details.state === "loading") {
        return _jsx(Spinner, { label: "resolving credentials\u2026" });
    }
    if (details.state === "failed") {
        return _jsx(Status, { variant: "error", children: details.message });
    }
    if (details.providers.length === 0) {
        return (_jsx(Text, { tone: "muted", children: "No accounts connected \u2014 press e to add one." }));
    }
    const { providers } = details;
    const { nameWidth, methodWidth } = providerColumnWidths(providers);
    const focusedProvider = providers[focusedIndex]?.name;
    const reauthHint = "press r to re-login";
    // The same blocks `profile show` prints, windowed to the rows the terminal
    // leaves the pane: the list shrinks to fit (see the root layout in
    // `Dashboard`) and scrolls just enough to keep the focused provider in view.
    // The profile-level slot (no focused provider) shows the list from the top.
    return (_jsx(Box, { flexDirection: "column", minHeight: 0, children: _jsx(VirtualList, { items: providers, getKey: (provider) => provider.name, itemHeight: (provider, index) => providerBlockHeight(provider, index === 0), focusedIndex: Math.max(0, focusedIndex), renderItem: (provider, index) => (_jsx(ProviderBlock, { provider: provider, first: index === 0, nameWidth: nameWidth, methodWidth: methodWidth, reauthHint: reauthHint, refreshingProvider: refreshingProvider, focusedProvider: focusedProvider, focusColumn: true })) }) }));
}
function EditScreen({ profile, rows, onApply, onBack, }) {
    const { cursor, indices, move, cycle } = useCycleNavigation(rows.map((row) => row.states.length));
    const keys = useKeyGlyphs();
    const glyphs = useGlyphs();
    const [unchanged, setUnchanged] = useState(false);
    useTerminalInput((input, key) => {
        const plain = !key.ctrl && !key.meta;
        if (key.escape)
            return onBack();
        if (key.up || (plain && input === "k"))
            move(-1);
        else if (key.down || (plain && input === "j"))
            move(1);
        else if ((plain && input === " ") || key.right) {
            cycle(1);
            setUnchanged(false);
        }
        else if (key.left) {
            cycle(-1);
            setUnchanged(false);
        }
        else if (key.enter) {
            if (indices.every((index) => index === 0))
                setUnchanged(true);
            else
                onApply(rows.map((row, i) => row.states[indices[i]]));
        }
    });
    if (rows.length === 0) {
        return (_jsxs(Stack, { children: [_jsxs(Text, { children: [_jsx(Text, { bold: true, color: theme.color.accent, children: "edit accounts" }), _jsxs(Text, { tone: "muted", children: [" \u00B7 ", profile] })] }), _jsx(Status, { children: "No providers are available for this profile." }), _jsx(KeyBar, { keys: [[keys.escape, "back"]] })] }));
    }
    const choices = rows.map((row) => ({
        label: row.provider,
        description: row.method ?? "not connected",
        states: row.states.map((state) => ({
            value: state,
            label: editStateStyle[state].label,
            icon: glyphs[editStateStyle[state].icon],
            variant: editStateStyle[state].variant,
        })),
    }));
    return (_jsxs(Stack, { gap: 1, children: [_jsxs(Text, { children: [_jsx(Text, { bold: true, color: theme.color.accent, children: "edit accounts" }), _jsxs(Text, { tone: "muted", children: [" \u00B7 ", profile] })] }), _jsx(CycleList, { choices: choices, cursor: cursor, indices: indices }), unchanged ? (_jsx(Alert, { variant: "warning", title: "No changes to apply", children: "Press Space to change an account, or Esc to go back." })) : null, _jsx(KeyBar, { keys: [
                    [keys.upDown, "navigate"],
                    [keys.space, "change"],
                    [keys.enter, "apply"],
                    [keys.escape, "back"],
                ] })] }));
}
function DashboardControls({ mode, busy, flow, entry, provider, keybinds, keyGlyphs, store, setMode, }) {
    if (flow?.inline !== true && (busy || flow !== undefined))
        return null;
    if (mode === "normal")
        return _jsx(KeyBar, { keys: keybinds, marginTop: 0 });
    if (mode === "delete" && entry !== undefined) {
        return (_jsx(InlineConfirm, { message: `Delete '${entry.name}' and all its stored credentials?`, confirmLabel: "Delete", cancelLabel: "Cancel", onSubmit: (confirmed) => {
                setMode("normal");
                if (confirmed)
                    store.dispatch({ kind: "delete", name: entry.name });
            }, onCancel: () => setMode("normal") }));
    }
    if (mode === "remove" && entry !== undefined && provider !== undefined) {
        return (_jsx(InlineConfirm, { message: `Remove '${provider.name}' from profile '${entry.name}'?`, confirmLabel: "Remove", cancelLabel: "Cancel", onSubmit: (confirmed) => {
                setMode("normal");
                if (confirmed) {
                    store.dispatch({
                        kind: "edit-apply",
                        name: entry.name,
                        add: [],
                        reconfigure: [],
                        remove: [provider.name],
                    });
                }
            }, onCancel: () => setMode("normal") }));
    }
    const renaming = mode === "rename" && entry !== undefined;
    return (_jsx(PromptFrame, { message: renaming ? `rename '${entry.name}' to` : "new profile name", layout: "inline", keys: [
            [keyGlyphs.enter, "confirm"],
            [keyGlyphs.escape, "cancel"],
        ], children: _jsx(TextField, { placeholder: renaming ? `${entry.name}-new` : "my-profile", onSubmit: (value) => {
                const name = value.trim();
                if (name.length === 0)
                    return;
                setMode("normal");
                store.dispatch(renaming
                    ? { kind: "rename", name: entry.name, newName: name }
                    : { kind: "create", name });
            }, onCancel: () => setMode("normal") }, `${mode}-${entry?.name ?? ""}`) }));
}
export function Dashboard({ store, initialSelected, }) {
    const state = useLiveStore(store);
    const keyGlyphs = useKeyGlyphs();
    const { rows } = useTerminalSize();
    const [selected, setSelected] = useState(initialSelected);
    // -1 is the profile-level slot: no provider is focused and profile actions
    // are shown. Up/down cycles through this slot and every connected provider.
    const [focusedProvider, setFocusedProvider] = useState(-1);
    const [mode, setMode] = useState("normal");
    const [screen, setScreen] = useState("overview");
    const { entries, focus: requestedFocus, flow, busy, notice } = state;
    useEffect(() => {
        if (requestedFocus === undefined)
            return;
        const focusIndex = entries.findIndex((entry) => entry.name === requestedFocus);
        store.clearFocus();
        if (focusIndex >= 0) {
            setSelected(focusIndex);
        }
    }, [entries, requestedFocus, store]);
    const index = Math.min(Math.max(selected, 0), entries.length - 1);
    const entry = entries[index];
    const details = entry === undefined ? undefined : store.detailsFor(entry.name);
    const providers = details?.state === "ready" ? details.providers : [];
    const provider = providers[focusedProvider];
    const moveProviderFocus = (delta) => setFocusedProvider((current) => {
        const slotCount = providers.length + 1;
        const position = current + 1;
        return ((position + delta + slotCount) % slotCount) - 1;
    });
    useEffect(() => setFocusedProvider(-1), [entry?.name]);
    useTerminalInput((input, key) => {
        // flow prompts, the edit screen, and the inline TextField/InlineConfirm
        // modes own the keyboard
        if (flow !== undefined || screen === "edit" || busy)
            return;
        if (mode !== "normal")
            return;
        if (input === "q" || key.escape || (key.ctrl && input === "c")) {
            store.dispatch({ kind: "exit" });
        }
        else if (key.ctrl || key.meta) {
            return;
        }
        else if (entry === undefined) {
            if (input === "n")
                setMode("create");
        }
        else if (key.left) {
            setSelected((s) => (s + entries.length - 1) % entries.length);
            setFocusedProvider(-1);
        }
        else if (key.right) {
            setSelected((s) => (s + 1) % entries.length);
            setFocusedProvider(-1);
        }
        else if (key.up && providers.length > 0) {
            moveProviderFocus(-1);
        }
        else if (key.down && providers.length > 0) {
            moveProviderFocus(1);
        }
        else if (provider === undefined && input === "R" && !entry.isDefault) {
            setMode("rename");
        }
        else if (provider === undefined && input === "d" && !entry.isDefault) {
            setMode("delete");
        }
        else if (provider === undefined &&
            input === "e" &&
            details?.state === "ready") {
            setScreen("edit");
        }
        else if (provider === undefined && input === "r") {
            store.dispatch({ kind: "refresh", name: entry.name });
        }
        else if (provider === undefined && input === "n") {
            setMode("create");
        }
        else if (input === "e" && provider !== undefined) {
            store.dispatch({
                kind: "edit-apply",
                name: entry.name,
                add: [],
                reconfigure: [provider.name],
                remove: [],
            });
        }
        else if (input === "r" && provider !== undefined) {
            store.dispatch({
                kind: "refresh",
                name: entry.name,
                provider: provider.name,
            });
        }
        else if (input === "d" && provider !== undefined) {
            setMode("remove");
        }
    });
    if (flow !== undefined && !flow.inline) {
        return (_jsxs(Text, { children: [_jsx(Text, { bold: true, color: theme.color.accent, children: flow.kind === "refresh" ? "refresh" : "edit accounts" }), _jsxs(Text, { tone: "muted", children: [" \u00B7 ", flow.name] })] }));
    }
    if (screen === "edit" &&
        entry !== undefined &&
        details !== undefined &&
        details.state === "ready") {
        const rows = [
            ...details.providers.map((provider) => ({
                provider: provider.name,
                method: provider.method,
                states: ["keep", "reconfigure", "remove"],
            })),
            ...details.available.map((name) => ({
                provider: name,
                method: undefined,
                states: ["skip", "add"],
            })),
        ];
        return (_jsx(EditScreen, { profile: entry.name, rows: rows, onBack: () => setScreen("overview"), onApply: (choices) => {
                const pick = (state) => rows.flatMap((row, i) => choices[i] === state ? [row.provider] : []);
                const action = {
                    kind: "edit-apply",
                    name: entry.name,
                    add: pick("add"),
                    reconfigure: pick("reconfigure"),
                    remove: pick("remove"),
                };
                setScreen("overview");
                if (action.add.length +
                    action.reconfigure.length +
                    action.remove.length ===
                    0) {
                    return;
                }
                store.dispatch(action);
            } }));
    }
    const annotation = entry !== undefined && entry.isActive ? "active" : "";
    const keybinds = entry === undefined
        ? [
            ["n", "new"],
            ["q", "quit"],
        ]
        : [
            [keyGlyphs.leftRight, "switch profile"],
            ...(providers.length === 0
                ? []
                : [[keyGlyphs.upDown, "focus provider"]]),
            ...(provider !== undefined
                ? [
                    ["e", "reconfigure"],
                    ["r", "refresh"],
                    ["d", "remove"],
                ]
                : [
                    ...(details?.state === "ready"
                        ? [
                            ["e", "edit"],
                            ["r", "refresh"],
                        ]
                        : []),
                    ["n", "new"],
                    ...(entry.isDefault
                        ? []
                        : [
                            ["R", "rename"],
                            ["d", "delete"],
                        ]),
                ]),
            ["q", "quit"],
        ];
    // The frame never exceeds the terminal: the provider list is the only part
    // allowed to shrink (a `VirtualList` windows it to whatever rows are left),
    // so every other row of chrome opts out of shrinking. Short lists keep the
    // compact layout because the cap is a maximum, not a fixed height.
    return (_jsxs(Stack, { maxHeight: rows, children: [_jsx(Stack, { flexShrink: 0, children: _jsx(Tabs, { tabs: entries.map((e) => ({
                        id: e.name,
                        label: e.name,
                        marked: e.isActive,
                    })), active: entry?.name ?? "" }) }), _jsx(Stack, { gap: 1, minHeight: 0, children: entry === undefined ? (_jsx(Text, { tone: "muted", children: "No profiles yet \u2014 press n to create one." })) : (_jsxs(_Fragment, { children: [_jsx(Box, { flexShrink: 0, children: _jsx(Gutter, { children: _jsxs(Box, { flexDirection: "row", children: [_jsx(Pointer, { focused: provider === undefined }), _jsx(Text, { children: " " }), _jsx(Text, { bold: true, color: provider === undefined ? theme.paint.focus : undefined, children: entry.name }), annotation === "" ? null : (_jsxs(Text, { tone: "muted", children: [" \u00B7 ", annotation] }))] }) }) }), _jsx(Box, { flexDirection: "column", minHeight: 0, children: _jsx(DetailsPane, { details: details ?? { state: "loading" }, focusedIndex: focusedProvider, refreshingProvider: flow?.kind === "refresh" ? flow.provider : undefined }) })] })) }), _jsx(Box, { minHeight: 1, flexShrink: 0, paddingLeft: theme.space.indent, children: busy && flow === undefined ? (_jsx(Spinner, { label: "working\u2026" })) : notice !== undefined ? (_jsx(Toast, { variant: notice.ok ? "info" : "error", children: notice.message })) : null }), _jsx(Stack, { flexShrink: 0, children: _jsx(DashboardControls, { mode: mode, busy: busy, flow: flow, entry: entry, provider: provider, keybinds: keybinds, keyGlyphs: keyGlyphs, store: store, setMode: setMode }) })] }));
}
/**
 * Runs the dashboard inside CliKit's application renderer. Pure actions and
 * edit/refresh flows share the same frame, and the application clears it on
 * exit.
 */
export const runProfileDashboardSession = (options) => Effect.flatMap(CliKit, (cli) => cli.application(
// live.open is Scope-bound; the session scope is its release backstop
// (the ensuring(live.close) below settles it on the normal path).
Effect.scoped(Effect.gen(function* () {
    const store = new DashStore(options.entries);
    let noticeFiber;
    const applyResult = Effect.fn(function* (result) {
        if (noticeFiber !== undefined)
            yield* Fiber.interrupt(noticeFiber);
        store.applyResult(result);
        if (result.ok) {
            noticeFiber = yield* Effect.sleep("4 seconds").pipe(Effect.andThen(Effect.sync(() => store.clearNotice())), Effect.forkChild);
        }
    });
    const loadInto = (name) => options.loadDetails(name).pipe(Effect.flatMap((payload) => Effect.sync(() => store.setDetails(name, { state: "ready", ...payload }))), Effect.catch((error) => Effect.sync(() => store.setDetails(name, {
        state: "failed",
        message: error.message,
    }))), Effect.catchDefect((defect) => Effect.sync(() => store.setDetails(name, {
        state: "failed",
        message: String(defect),
    }))), 
    // Provider discovery can build very large Layers. Yield often
    // enough for Sigil's 80ms animation clock to keep painting while
    // that CPU-heavy Effect graph is evaluated.
    Effect.provideService(Scheduler.MaxOpsBeforeYield, 64));
    const initialSelected = Math.max(0, options.entries.findIndex((entry) => entry.name === options.selected));
    const live = yield* cli.live.open(_jsx(Dashboard, { store: store, initialSelected: initialSelected }), { placement: "beforeTranscript" });
    // Mount the spinner before starting stack import/provider builds.
    // Forking the loader first allowed synchronous module evaluation to
    // delay the dashboard's first frame, making it look fully hung.
    const loader = yield* Effect.forEach(options.entries, (entry) => loadInto(entry.name), { concurrency: 2, discard: true }).pipe(Effect.delay("1 millis"), Effect.forkChild);
    yield* Effect.gen(function* () {
        while (true) {
            const action = yield* Effect.callback((resume) => {
                store.bindResolver((action) => resume(Effect.succeed(action)));
            });
            switch (action.kind) {
                case "exit":
                    return;
                case "refresh":
                case "edit-apply": {
                    store.setFlow({
                        kind: action.kind,
                        name: action.name,
                        provider: action.kind === "refresh" ? action.provider : undefined,
                        // refresh keeps the overview on screen with a spinner; only
                        // account editing takes over the whole view
                        inline: action.kind === "refresh",
                    });
                    const flowEffect = options.runFlow(action, {
                        onProviderStart: (provider) => Effect.sync(() => store.setFlow({
                            kind: "refresh",
                            name: action.name,
                            provider,
                            inline: true,
                        })),
                    });
                    // Provider login implementations emit useful progress in
                    // standalone commands. Inside the dashboard that progress
                    // belongs in the provider pane, not the transcript.
                    const quietRefreshCli = {
                        ...cli,
                        output: {
                            ...cli.output,
                            info: () => Effect.void,
                            success: () => Effect.void,
                        },
                    };
                    const result = yield* cli.wizard(action.kind === "refresh"
                        ? flowEffect.pipe(Effect.provideService(CliKit, quietRefreshCli))
                        : flowEffect);
                    const entries = yield* options.reloadEntries;
                    if (action.kind === "refresh") {
                        // Keep the provider-level spinner visible until the new
                        // expiry/token details have replaced the old ones.
                        yield* loadInto(action.name);
                    }
                    else {
                        store.setDetails(action.name, { state: "loading" });
                        yield* loadInto(action.name).pipe(Effect.forkChild);
                    }
                    store.setFlow(undefined);
                    yield* applyResult({
                        ok: result.ok,
                        message: result.message,
                        entries,
                        selected: action.name,
                    });
                    break;
                }
                default: {
                    const result = yield* options.execute(action);
                    // a renamed/created profile needs its details (re)resolved
                    const names = store.detailNames();
                    yield* applyResult(result);
                    yield* Effect.forEach(result.entries.filter((e) => !names.has(e.name)), (e) => loadInto(e.name), { concurrency: 2, discard: true }).pipe(Effect.forkChild);
                }
            }
        }
    }).pipe(Effect.ensuring(Effect.sync(() => store.dispose())), Effect.ensuring(Effect.suspend(() => noticeFiber === undefined
        ? Effect.void
        : Fiber.interrupt(noticeFiber))), Effect.ensuring(live.close), Effect.ensuring(Fiber.interrupt(loader)));
}))));
//# sourceMappingURL=ProfileDashboard.js.map