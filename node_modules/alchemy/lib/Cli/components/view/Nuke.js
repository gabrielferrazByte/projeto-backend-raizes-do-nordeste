import { jsx as _jsx, jsxs as _jsxs } from "@alchemy.run/sigil/jsx-runtime";
/** @jsxImportSource @alchemy.run/sigil */
import * as Effect from "effect/Effect";
import * as Exit from "effect/Exit";
import { Plan, PlanTree } from "./PlanView.js";
import { planDecisionScreen } from "./PlanDecision.js";
import { Progress } from "../../../Report.js";
import { CliKit } from "../../CliKit/index.js";
import { LiveStore, ProgressBar, Row, SpinnerGlyph, Stack, Text, useLiveStore, } from "../ui/index.js";
export class NukeProgressStore extends LiveStore {
    constructor() {
        super({ total: 0, completed: 0, resources: 0, inFlight: [] });
    }
    emit(event) {
        this.update((state) => {
            switch (event._tag) {
                case "nuke.scan.started":
                    return { ...state, total: event.total };
                case "nuke.scan.provider.started":
                    return { ...state, inFlight: [...state.inFlight, event.provider] };
                case "nuke.scan.provider.completed":
                    return {
                        ...state,
                        completed: state.completed + 1,
                        resources: state.resources + event.resources,
                        inFlight: state.inFlight.filter((id) => id !== event.provider),
                    };
                default:
                    return state;
            }
        });
    }
}
export function NukeProgress({ store }) {
    const state = useLiveStore(store);
    return (_jsxs(Stack, { paddingX: 2, paddingY: 1, gap: 1, children: [_jsxs(Row, { gap: 2, children: [_jsx(ProgressBar, { value: state.total === 0 ? 0 : state.completed / state.total, width: 32, showPercent: false, variant: "info" }), _jsxs(Text, { bold: true, children: ["[", state.completed, "/", state.total, "]"] })] }), _jsxs(Stack, { children: [_jsxs(Text, { tone: "muted", children: ["Scanning providers \u00B7 ", state.resources, " resource", state.resources === 1 ? "" : "s", " found"] }), state.inFlight.slice(0, 10).map((id) => (_jsxs(Row, { gap: 1, children: [_jsx(SpinnerGlyph, {}), _jsxs(Text, { children: ["Scanning ", id] })] }, id))), state.inFlight.length > 10 ? (_jsxs(Text, { tone: "muted", children: ["\u2026and ", state.inFlight.length - 10, " more"] })) : null, state.total === 0 ? (_jsxs(Row, { gap: 1, children: [_jsx(SpinnerGlyph, {}), _jsx(Text, { children: "Discovering providers" })] })) : null] })] }));
}
const logNukeEvent = (event, interactive) => {
    if (event._tag === "nuke.scan.provider.completed") {
        if (event.error !== undefined)
            return Effect.logWarning(`${event.provider}: ${event.error}`);
        if (!interactive)
            return Effect.logInfo(`scanned ${event.provider} (${event.resources})`);
    }
    else if (event._tag === "nuke.resource.failed") {
        return Effect.logWarning(`${event.provider} ${event.resource}: ${event.message}`);
    }
    else if (!interactive && event._tag === "nuke.resource.deleted") {
        return Effect.logInfo(`deleted ${event.provider} ${event.resource}`);
    }
    return Effect.void;
};
/** Scan progress stays separate until the inventory is ready to review. */
export const renderNukeScan = () => (effect) => Effect.gen(function* () {
    const cli = yield* CliKit;
    const store = new NukeProgressStore();
    const live = cli.terminal.input && !process.env.DEBUG
        ? yield* cli.live.open(_jsx(NukeProgress, { store: store }))
        : undefined;
    return yield* effect.pipe(Effect.provideService(Progress, (event) => Effect.gen(function* () {
        if (live)
            store.emit(event);
        yield* logNukeEvent(event, live !== undefined);
    })), Effect.ensuring(live?.close ?? Effect.void));
});
/** Drive the same resource rows used by the preview through their deletion lifecycle. */
export const renderNukeDelete = (targets, mode) => (effect) => Effect.gen(function* () {
    const cli = yield* CliKit;
    const tree = new PlanTree(nukePlan(targets, { mode }), {
        mode: "apply",
        label: "Deleting resources",
        busy: true,
    });
    const live = cli.terminal.input && !process.env.DEBUG
        ? yield* cli.live.open(_jsx(Plan, { tree: tree }), { persistOnClose: true })
        : undefined;
    return yield* effect.pipe(Effect.provideService(Progress, (event) => Effect.gen(function* () {
        if (event._tag === "apply.resource.status" ||
            event._tag === "apply.resource.note")
            tree.emit(event);
        else if (event._tag === "nuke.pass.started")
            tree.setLabel(`Deleting resources · pass ${event.pass}`);
        yield* logNukeEvent(event, live !== undefined);
    })), Effect.onExit((exit) => Effect.sync(() => {
        const success = Exit.isSuccess(exit) &&
            exit.value.failed.length === 0 &&
            exit.value.held.length === 0;
        tree.finish(success ? "success" : "failure", success ? "Nuke complete" : "Nuke incomplete");
        tree.setViewport("full");
    })), Effect.ensuring(live?.close ?? Effect.void));
});
/** Cloud inventory rows carry display identity only, never fabricated stack state. */
export const nukePlan = (targets, options) => {
    return {
        defaultMode: options.mode,
        rows: targets
            .map((target, index) => ({
            key: `nuke/${index}`,
            type: "resource",
            id: target.providerId,
            resourceType: target.providerId,
            detail: target.displayName && target.displayName !== "unknown"
                ? target.displayName
                : undefined,
            depth: 0,
            action: "delete",
            providerMode: options.mode,
        }))
            .sort((a, b) => a.id.localeCompare(b.id) ||
            (a.detail ?? "").localeCompare(b.detail ?? "")),
        summary: {
            counts: {
                create: 0,
                update: 0,
                adopted: 0,
                delete: targets.length,
                orphaned: 0,
                replace: 0,
                noop: 0,
            },
            taskCounts: { run: 0, delete: 0, noop: 0 },
            bindingChanges: 0,
        },
    };
};
export const reviewNuke = Effect.fn(function* (targets, options) {
    const cli = yield* CliKit;
    const plan = nukePlan(targets, options);
    if (options.yes || options.dryRun) {
        yield* cli.output.print(_jsx(Plan, { tree: new PlanTree(plan, {
                mode: "review",
                label: "Nuke",
                viewport: "full",
            }) }));
        return true;
    }
    return yield* cli.prompt.custom(planDecisionScreen({
        plan,
        label: "Nuke",
        message: `Permanently DELETE ${targets.length} ${options.mode === "local" ? "locally emulated " : ""}resource(s)? This cannot be undone.`,
        choices: [
            { value: true, label: "Delete" },
            { value: false, label: "Cancel" },
        ],
        initialValue: false,
    }));
});
//# sourceMappingURL=Nuke.js.map