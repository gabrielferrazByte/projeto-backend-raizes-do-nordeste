import { jsx as _jsx, jsxs as _jsxs } from "@alchemy.run/sigil/jsx-runtime";
import { useSyncExternalStore } from "@alchemy.run/sigil/react";
import { useGlyphs } from "./Environment.js";
import { SpinnerGlyph } from "./Feedback.js";
import { ProgressBar } from "./ProgressBar.js";
import { Box, Row, Stack } from "./Layout.js";
import { Text } from "./Typography.js";
import { theme } from "../../../Util/Theme.js";
/**
 * The one status row shape shared by TaskTree and the plan/apply views:
 * glyph-or-spinner, bold label, muted detail, then any trailing cells.
 */
export function TaskRow({ icon, iconColor, spinning = false, label, detail, depth = 0, children, }) {
    const glyphs = useGlyphs();
    return (_jsxs(Row, { gap: 1, paddingLeft: depth * theme.space.indent, children: [spinning ? (_jsx(SpinnerGlyph, { color: iconColor })) : (_jsx(Text, { color: iconColor, children: icon ?? glyphs.bullet })), _jsx(Text, { bold: depth === 0, children: label }), detail === undefined ? null : _jsxs(Text, { tone: "muted", children: ["\u00B7 ", detail] }), children] }));
}
export function ProgressGroup({ rows, width = 20, labelWidth, }) {
    return (_jsx(Stack, { children: rows.map((row) => {
            const complete = row.total > 0 && row.completed >= row.total;
            const variant = row.failed ? "error" : complete ? "success" : "info";
            return (_jsxs(Row, { children: [_jsx(ProgressBar, { value: row.total <= 0 ? 0 : row.completed / row.total, width: width, showPercent: false, variant: variant }), labelWidth === undefined ? (_jsx(Text, { children: row.label })) : (_jsx(Box, { width: labelWidth, flexShrink: 0, children: _jsx(Text, { wrap: "truncate-end", children: row.label }) })), _jsxs(Text, { bold: true, tone: variant === "error" ? "danger" : variant, children: [row.completed, "/", row.total] }), row.failed ? (_jsxs(Text, { tone: "danger", children: ["(", row.failed, " failed)"] })) : null, row.detail === undefined ? null : (_jsx(Text, { tone: "muted", children: row.detail }))] }, row.id));
        }) }));
}
export class LiveStore {
    value;
    listeners = new Set();
    constructor(initial) {
        this.value = initial;
    }
    subscribe = (listener) => {
        this.listeners.add(listener);
        return () => this.listeners.delete(listener);
    };
    snapshot = () => this.value;
    set = (value) => {
        this.value = value;
        for (const listener of this.listeners)
            listener();
    };
    update = (f) => this.set(f(this.value));
}
export const useLiveStore = (store) => useSyncExternalStore(store.subscribe, store.snapshot);
//# sourceMappingURL=Live.js.map