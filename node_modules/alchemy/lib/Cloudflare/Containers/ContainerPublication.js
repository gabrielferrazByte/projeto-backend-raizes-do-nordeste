import * as Effect from "effect/Effect";
import * as Schedule from "effect/Schedule";
export const retryContainerPublication = (publication) => Effect.retry(publication, {
    while: (error) => error._tag === "DockerRegistryBlobUnknown" ||
        error._tag === "DockerRegistryUnavailable",
    schedule: Schedule.max([Schedule.spaced("3 seconds"), Schedule.recurs(5)]),
});
//# sourceMappingURL=ContainerPublication.js.map