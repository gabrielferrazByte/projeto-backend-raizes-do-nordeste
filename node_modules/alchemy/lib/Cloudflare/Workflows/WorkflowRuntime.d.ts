import * as Context from "effect/Context";
import type * as Effect from "effect/Effect";
import type { WorkflowCronSchedule, WorkflowStepContextData, WorkflowStepEvent, WorkflowTaskOptions, WorkflowWaitForEventOptions } from "./Workflow.ts";
declare const WorkflowEvent_base: Context.ServiceClass<WorkflowEvent, "Cloudflare.Workflows.WorkflowEvent", {
    payload: unknown;
    timestamp: Date;
    instanceId: string;
    workflowName: string;
    /**
     * Present when Cloudflare created this instance from a native
     * {@link WorkflowProps.schedules} cron expression. Absent for
     * instances started with `create` / `createBatch`.
     */
    schedule?: WorkflowCronSchedule;
}>;
/**
 * Service that carries the current workflow event payload.
 * `yield* WorkflowEvent` inside a workflow body to access it.
 */
export declare class WorkflowEvent extends WorkflowEvent_base {
}
declare const WorkflowStepContext_base: Context.ServiceClass<WorkflowStepContext, "Cloudflare.WorkflowStepContext", WorkflowStepContextData>;
/**
 * Runtime information for the current `task` attempt.
 */
export declare class WorkflowStepContext extends WorkflowStepContext_base {
}
declare const WorkflowStep_base: Context.ServiceClass<WorkflowStep, "Cloudflare.Workflows.WorkflowStep", {
    do<T>(options: WorkflowTaskOptions<T, any, any>): Effect.Effect<T>;
    sleep(name: string, duration: string | number): Effect.Effect<void>;
    sleepUntil(name: string, timestamp: Date | number): Effect.Effect<void>;
    waitForEvent<T>(name: string, options: WorkflowWaitForEventOptions): Effect.Effect<WorkflowStepEvent<T>>;
}>;
/**
 * Internal service that wraps the Cloudflare `WorkflowStep` object.
 * Not accessed directly by users -- use `task`, `sleep`, `sleepUntil`, and
 * `waitForEvent` instead.
 */
export declare class WorkflowStep extends WorkflowStep_base {
}
export {};
//# sourceMappingURL=WorkflowRuntime.d.ts.map