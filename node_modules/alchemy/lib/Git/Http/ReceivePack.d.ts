/** Streaming smart-HTTP decoding and report-status encoding. No authorization policy. */
import * as Effect from "effect/Effect";
import * as HttpServerRequest from "effect/unstable/http/HttpServerRequest";
import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
import { StoreError } from "../Protocol/Store.ts";
import { type PushInput } from "../Push.ts";
import { type CommitPushResult } from "../RepoObject.ts";
export { ReceivePack as endpoint } from "../Api/Protocol.ts";
export interface Push {
    readonly _tag: "Push";
    readonly input: PushInput;
    readonly updates: PushInput["updates"];
    readonly capabilities: ReadonlySet<string>;
}
export declare const probeResponse: () => HttpServerResponse.HttpServerResponse;
/** Decode only the bounded command header. The body remains streaming until preparePush. */
export declare const decode: (request: HttpServerRequest.HttpServerRequest) => Effect.Effect<{
    readonly _tag: "Probe";
    input?: undefined;
    updates?: undefined;
    capabilities?: undefined;
} | {
    _tag: "Push";
    input: PushInput;
    updates: readonly Readonly<{
        oldOid: import("../Protocol/ObjectCodec.ts").Oid;
        newOid: import("../Protocol/ObjectCodec.ts").Oid;
        ref: string;
    }>[];
    capabilities: ReadonlySet<string>;
}, StoreError | import("../RepoObject.ts").WireProtocolError, import("effect/Scope").Scope>;
/** Encode the negotiated Git report; policy rejection is a Git result, not JSON. */
export declare const response: (push: Push, result: CommitPushResult) => HttpServerResponse.HttpServerResponse;
/** Refuse the whole push without ingesting objects or moving any refs. */
export declare const reject: (push: Push, reason: string) => HttpServerResponse.HttpServerResponse;
/** Encode malformed protocol input or a failed unpack operation. */
export declare const failure: (reason: string) => HttpServerResponse.HttpServerResponse;
//# sourceMappingURL=ReceivePack.d.ts.map