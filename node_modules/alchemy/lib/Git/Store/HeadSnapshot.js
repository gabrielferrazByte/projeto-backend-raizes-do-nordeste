export const encodeHeadSnapshot = (snapshot) => JSON.stringify(snapshot);
/** `undefined` on malformed/foreign content — callers fall to the DO. */
export const decodeHeadSnapshot = (raw) => {
    try {
        const parsed = JSON.parse(raw);
        return parsed.v === 1 && Array.isArray(parsed.refs) ? parsed : undefined;
    }
    catch {
        return undefined;
    }
};
//# sourceMappingURL=HeadSnapshot.js.map