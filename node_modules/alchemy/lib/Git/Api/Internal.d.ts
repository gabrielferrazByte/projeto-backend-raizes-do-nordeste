/**
 * The engine's internal API: routes the engine calls on itself, never a
 * user. Register `Git.InternalApiLive` beside public routes, outside user middleware.
 */
import * as HttpApiEndpoint from "effect/unstable/httpapi/HttpApiEndpoint";
import * as HttpApi from "effect/unstable/httpapi/HttpApi";
import * as HttpApiGroup from "effect/unstable/httpapi/HttpApiGroup";
/**
 * The push pipeline's hashing endpoint (DESIGN §22.7), reached through the
 * Worker's self service binding by the fan-out {@link Hasher} and
 * authenticated with the deploy-time internal secret, never a user
 * credential.
 */
export declare const HashPart: HttpApiEndpoint.HttpApiEndpoint<"hashPart", "POST", "/_alchemy/git/hash", never, never, never, never, import("effect/Schema").toCodecJson<import("effect/unstable/httpapi/HttpApiSchema").NoContent>, never, never, never>;
declare const Internal_base: HttpApiGroup.HttpApiGroup<"internal", HttpApiEndpoint.HttpApiEndpoint<"hashPart", "POST", "/_alchemy/git/hash", never, never, never, never, import("effect/Schema").toCodecJson<import("effect/unstable/httpapi/HttpApiSchema").NoContent>, never, never, never>, true>;
/** The internal group, mounted at the root. */
export declare class Internal extends Internal_base {
}
declare const InternalApi_base: HttpApi.HttpApi<"git-internal", typeof Internal>;
/**
 * The internal API schema. `Git.InternalApiLive` registers its routes using
 * `HttpApiBuilder.layer(Git.InternalApi)` with `Git.InternalLive`.
 */
export declare class InternalApi extends InternalApi_base {
}
export {};
//# sourceMappingURL=Internal.d.ts.map