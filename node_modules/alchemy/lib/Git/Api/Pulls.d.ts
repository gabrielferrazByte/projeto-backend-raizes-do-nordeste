/**
 * The `pulls` group: pull-request lifecycle + merge (DESIGN.md §5).
 *
 * PRs are same-repo branch PRs (no cross-fork), tracking **live** branches
 * by ref name: diff and mergeability are computed on read from current ref
 * tips. Merging fast-forwards when possible; otherwise it creates a merge
 * commit iff the three-way tree merge is trivial (no path touched on both
 * sides relative to the merge base) — a conflicting PR is a typed 409, the
 * server never writes conflict markers.
 */
import * as HttpApiEndpoint from "effect/unstable/httpapi/HttpApiEndpoint";
import * as Schema from "effect/Schema";
import * as HttpApiGroup from "effect/unstable/httpapi/HttpApiGroup";
import { BranchMissing, MergeConflict, MergeResult, NothingToMerge, Pull, PullDetail, PullExists, PullNotFound, PullStateConflict, ReadOnlyRepo, RefConflict, RepoNotFound, ValidationError, PushDenied } from "./Schema.ts";
/** `(owner, repo, number)` path segments for single-PR endpoints. */
export declare const PullPath: Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>;
/**
 * Opens a PR. `base`/`head` accept short (`main`) or full
 * (`refs/heads/main`) branch names — only branches are legal (no tags).
 */
export declare const CreatePull: HttpApiEndpoint.HttpApiEndpoint<"create", "POST", "/repos/:owner/:repo/pulls", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    readonly title: Schema.String;
    readonly body: Schema.optional<Schema.String>;
    /** Target branch, short or full name. */
    readonly base: Schema.String;
    /** Source branch, short or full name. Must differ from `base`. */
    readonly head: Schema.String;
}>>, never, Schema.toCodecJson<typeof Pull>, Schema.toCodecJson<typeof RepoNotFound | typeof ValidationError | typeof BranchMissing | typeof PullExists>, never, never>;
/** Lists PRs, newest first. `state` defaults to `open`. Anonymous on public repos. */
export declare const ListPulls: HttpApiEndpoint.HttpApiEndpoint<"list", "GET", "/repos/:owner/:repo/pulls", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
}>>, Schema.toCodecStringTree<Schema.Struct<{
    /** Filter by lifecycle state. @default open */
    readonly state: Schema.optional<Schema.Literals<readonly ["open", "closed", "merged", "all"]>>;
    readonly cursor: Schema.optional<Schema.String>;
    readonly limit: Schema.optional<Schema.Int>;
}>>, never, never, Schema.toCodecJson<Schema.Struct<{
    readonly items: Schema.$Array<typeof Pull>;
    readonly nextCursor: Schema.NullOr<Schema.String>;
    readonly hasMore: Schema.Boolean;
}>>, Schema.toCodecJson<typeof RepoNotFound>, never, never>;
/** Reads one PR with live compare fields (aheadBy/behindBy/mergeable). */
export declare const GetPull: HttpApiEndpoint.HttpApiEndpoint<"get", "GET", "/repos/:owner/:repo/pulls/:number", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, never, never, Schema.toCodecJson<typeof PullDetail>, Schema.toCodecJson<typeof RepoNotFound | typeof PullNotFound>, never, never>;
/** Patches title/body, or closes/reopens via `state`. */
export declare const UpdatePull: HttpApiEndpoint.HttpApiEndpoint<"update", "PATCH", "/repos/:owner/:repo/pulls/:number", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    readonly title: Schema.optional<Schema.String>;
    /** `null` clears the body. */
    readonly body: Schema.optional<Schema.NullOr<Schema.String>>;
    /**
     * `closed` closes an open PR; `open` reopens a closed one. Merged
     * PRs reject both (409).
     */
    readonly state: Schema.optional<Schema.Literals<readonly ["open", "closed"]>>;
}>>, never, Schema.toCodecJson<typeof Pull>, Schema.toCodecJson<typeof RepoNotFound | typeof PullNotFound | typeof PullStateConflict>, never, never>;
/**
 * Merges an open PR: fast-forward when possible, else a merge commit iff
 * the three-way tree merge is trivial.
 */
export declare const MergePull: HttpApiEndpoint.HttpApiEndpoint<"merge", "POST", "/repos/:owner/:repo/pulls/:number/merge", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    /**
     * Merge-commit message; defaults to
     * `Merge pull request #N from <head>`. Ignored on fast-forward.
     */
    readonly message: Schema.optional<Schema.String>;
    /**
     * Race guard: fail RefConflict if the head tip moved (force-push)
     * since the caller inspected it.
     */
    readonly expectedHeadOid: Schema.optional<Schema.brand<Schema.String, "Oid">>;
}>>, never, Schema.toCodecJson<typeof MergeResult>, Schema.toCodecJson<typeof ReadOnlyRepo | typeof PushDenied | typeof RepoNotFound | typeof RefConflict | typeof PullNotFound | typeof BranchMissing | typeof PullStateConflict | typeof NothingToMerge | typeof MergeConflict>, never, never>;
declare const Pulls_base: HttpApiGroup.HttpApiGroup<"pulls", HttpApiEndpoint.HttpApiEndpoint<"create", "POST", "/api/v1/repos/:owner/:repo/pulls", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    readonly title: Schema.String;
    readonly body: Schema.optional<Schema.String>;
    /** Target branch, short or full name. */
    readonly base: Schema.String;
    /** Source branch, short or full name. Must differ from `base`. */
    readonly head: Schema.String;
}>>, never, Schema.toCodecJson<typeof Pull>, Schema.toCodecJson<typeof RepoNotFound | typeof ValidationError | typeof BranchMissing | typeof PullExists>, never, never> | HttpApiEndpoint.HttpApiEndpoint<"get", "GET", "/api/v1/repos/:owner/:repo/pulls/:number", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, never, never, Schema.toCodecJson<typeof PullDetail>, Schema.toCodecJson<typeof RepoNotFound | typeof PullNotFound>, never, never> | HttpApiEndpoint.HttpApiEndpoint<"list", "GET", "/api/v1/repos/:owner/:repo/pulls", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
}>>, Schema.toCodecStringTree<Schema.Struct<{
    /** Filter by lifecycle state. @default open */
    readonly state: Schema.optional<Schema.Literals<readonly ["open", "closed", "merged", "all"]>>;
    readonly cursor: Schema.optional<Schema.String>;
    readonly limit: Schema.optional<Schema.Int>;
}>>, never, never, Schema.toCodecJson<Schema.Struct<{
    readonly items: Schema.$Array<typeof Pull>;
    readonly nextCursor: Schema.NullOr<Schema.String>;
    readonly hasMore: Schema.Boolean;
}>>, Schema.toCodecJson<typeof RepoNotFound>, never, never> | HttpApiEndpoint.HttpApiEndpoint<"merge", "POST", "/api/v1/repos/:owner/:repo/pulls/:number/merge", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    /**
     * Merge-commit message; defaults to
     * `Merge pull request #N from <head>`. Ignored on fast-forward.
     */
    readonly message: Schema.optional<Schema.String>;
    /**
     * Race guard: fail RefConflict if the head tip moved (force-push)
     * since the caller inspected it.
     */
    readonly expectedHeadOid: Schema.optional<Schema.brand<Schema.String, "Oid">>;
}>>, never, Schema.toCodecJson<typeof MergeResult>, Schema.toCodecJson<typeof ReadOnlyRepo | typeof PushDenied | typeof RepoNotFound | typeof RefConflict | typeof PullNotFound | typeof BranchMissing | typeof PullStateConflict | typeof NothingToMerge | typeof MergeConflict>, never, never> | HttpApiEndpoint.HttpApiEndpoint<"update", "PATCH", "/api/v1/repos/:owner/:repo/pulls/:number", Schema.toCodecStringTree<Schema.Struct<{
    readonly owner: Schema.String;
    readonly repo: Schema.String;
    /** The PR number (path segment; decoded from its string form). */
    readonly number: Schema.Int;
}>>, never, Schema.toCodecJson<Schema.Struct<{
    readonly title: Schema.optional<Schema.String>;
    /** `null` clears the body. */
    readonly body: Schema.optional<Schema.NullOr<Schema.String>>;
    /**
     * `closed` closes an open PR; `open` reopens a closed one. Merged
     * PRs reject both (409).
     */
    readonly state: Schema.optional<Schema.Literals<readonly ["open", "closed"]>>;
}>>, never, Schema.toCodecJson<typeof Pull>, Schema.toCodecJson<typeof RepoNotFound | typeof PullNotFound | typeof PullStateConflict>, never, never>, false>;
/** The `pulls` group, mounted at `/api/v1`. */
export declare class Pulls extends Pulls_base {
}
export {};
//# sourceMappingURL=Pulls.d.ts.map