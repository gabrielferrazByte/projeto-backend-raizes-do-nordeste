/**
 * The hasher wire protocol (DESIGN §22.7, §22.9) — the part of `Hasher.ts`
 * a hasher isolate needs and nothing more: no Cloudflare provider imports,
 * so a dynamic worker bundling it stays small.
 *
 * Request: the raw part bytes, coordinates in the query string. Response:
 * `u32 len | frame` repeated — the scan first (`encodeScanResult`: `u32
 * jsonLength | json | blob area`, entries referencing content/zdata as
 * `[offset, length]` into the blob area), then, when a spill was
 * requested, the uploaded part as JSON.
 */
import * as Effect from "effect/Effect";
import * as Schema from "effect/Schema";
import type { DeltaBase, DeltaJob, DeltaResolved, EntryBounds, ScanResult } from "../Protocol/PartialScan.ts";
declare const HashError_base: Schema.Class<HashError, Schema.TaggedStruct<"HashError", {
    readonly reason: Schema.String;
}>, import("effect/Cause").YieldableError>;
export declare class HashError extends HashError_base {
}
export declare const encodeBoundsRequest: (payload: Uint8Array, bounds: ReadonlyArray<EntryBounds>) => Uint8Array;
export declare const decodeBoundsRequest: (body: Uint8Array) => {
    readonly bounds: ReadonlyArray<EntryBounds>;
    readonly payload: Uint8Array;
};
/** The internal route the self-binding layer posts to. */
export declare const HASH_ROUTE = "/_alchemy/git/hash";
export declare const encodeScanResult: (result: ScanResult) => Uint8Array;
export declare const decodeScanResult: (bytes: Uint8Array) => ScanResult;
/**
 * Response framing of the hash route: `u32 len | frame` repeated — the
 * scan first, then (spill only) the uploaded part as JSON once its upload
 * has finished. The client acts on the scan without waiting for the part.
 */
export declare const frame: (bytes: Uint8Array) => Uint8Array;
/** Reads length-prefixed frames off a response body, one at a time. */
export declare const makeFrameReader: (body: ReadableStream<Uint8Array>) => () => Effect.Effect<Uint8Array | undefined, HashError>;
/** `u32 jsonLen | json | blobs` with bases and deltas referenced as [offset, length]. */
export declare const encodeDeltaBatch: (bases: ReadonlyArray<DeltaBase>, jobs: ReadonlyArray<DeltaJob>) => Uint8Array;
export declare const decodeDeltaBatch: (bytes: Uint8Array) => {
    readonly bases: Array<DeltaBase>;
    readonly jobs: Array<DeltaJob>;
};
export declare const encodeDeltaResults: (results: ReadonlyArray<DeltaResolved>) => Uint8Array;
export declare const decodeDeltaResults: (bytes: Uint8Array) => Array<DeltaResolved>;
export {};
//# sourceMappingURL=Protocol.d.ts.map