/** Encodes staged rows for one `stagePush` call. */
export const encodeStagedBatch = (objects) => {
    const meta = [];
    let blobBytes = 0;
    for (const object of objects) {
        meta.push({
            o: object.oid,
            t: object.type,
            s: object.size,
            n: object.zdata.byteLength,
            z: object.zsize,
            p: object.pack === undefined
                ? undefined
                : { i: object.pack.packId, f: object.pack.offset },
        });
        blobBytes += object.zdata.byteLength;
    }
    const json = new TextEncoder().encode(JSON.stringify(meta));
    const out = new Uint8Array(4 + json.length + blobBytes);
    new DataView(out.buffer).setUint32(0, json.length);
    out.set(json, 4);
    let at = 4 + json.length;
    for (const object of objects) {
        out.set(object.zdata, at);
        at += object.zdata.byteLength;
    }
    return out;
};
/** Decodes a `stagePush` batch back into staged rows (views into `bytes`). */
export const decodeStagedBatch = (bytes) => {
    const jsonLen = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).getUint32(0);
    const meta = JSON.parse(new TextDecoder().decode(bytes.subarray(4, 4 + jsonLen)));
    const out = [];
    let at = 4 + jsonLen;
    for (const row of meta) {
        const zdata = bytes.subarray(at, at + row.n);
        at += row.n;
        out.push({
            oid: row.o,
            type: row.t,
            size: row.s,
            zdata,
            zsize: row.z,
            pack: row.p === undefined ? undefined : { packId: row.p.i, offset: row.p.f },
        });
    }
    return out;
};
//# sourceMappingURL=PushWire.js.map