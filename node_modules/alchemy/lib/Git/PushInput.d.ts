/** Construct a streaming push from an application's own protocol or background job. */
import * as Effect from "effect/Effect";
import * as Schema from "effect/Schema";
import * as Stream from "effect/Stream";
import { type PushInput, type RefUpdate } from "./Push.ts";
/**
 * Prepare input from decoded ref updates and raw pack bytes, with no HTTP dependency.
 * Use Stream.empty for a ref-only mutation. The input and producer share the caller's scope.
 */
export declare const fromStream: <E, R>(updates: ReadonlyArray<RefUpdate>, pack: Stream.Stream<Uint8Array, E, R>, options?: {
    readonly atomic?: boolean;
    readonly declaredBytes?: number;
}) => Effect.Effect<PushInput, Schema.SchemaError, R | import("effect/Scope").Scope>;
//# sourceMappingURL=PushInput.d.ts.map