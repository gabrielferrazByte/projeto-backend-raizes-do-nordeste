import * as NodeChild from "node:child_process";
import * as NodeFs from "node:fs";
import * as NodeNet from "node:net";
import * as NodeOs from "node:os";
import * as NodePath from "node:path";
//#region src/core/DockerLoopback.ts
/**
* Hostname rewritten into container env so loopback still looks local
* (Prisma's `prisma+postgres://` plain-HTTP gate). Mapped in the sidecar
* ExtraHosts — to `127.0.0.1` on native Linux (unix-socket tunnel) or
* Docker `host-gateway` on Docker Desktop.
*/
const CONTAINER_LOOPBACK_ALIAS = "host.docker.localhost";
/** Bind-mount target inside the workerd `<name>-proxy` sidecar. */
const CONTAINER_LOOPBACK_MOUNT = "/alchemy/host-loopback";
const LOOPBACK_HOST_IN_VALUE = /(?:^|[\s,;=]|\/\/(?:[^/\s@]*@)?)(localhost|127\.0\.0\.1|0\.0\.0\.0|\[::1\]|host\.docker\.localhost)(?=[:/?#]|[\s,;]|$)/i;
const LOOPBACK_HOST_PORT = /(?:localhost|127\.0\.0\.1|0\.0\.0\.0|\[::1\]|host\.docker\.localhost):(\d{1,5})\b/gi;
const DSN_PORT = /(?:^|[\s;])port=(\d{1,5})\b/gi;
const unixServers = /* @__PURE__ */ new Map();
const netnsForwarders = /* @__PURE__ */ new Map();
/**
* Native Linux Docker: a SYN to `host-gateway` (bridge IP) is host INPUT
* and UFW/nftables can drop it. Docker Desktop's gateway already reaches
* host loopback, so the unix-socket path is Linux-only.
*/
const usesUnixSocketLoopback = () => process.platform === "linux";
const nsenterAvailable = () => {
	try {
		return NodeChild.spawnSync("nsenter", ["--version"], { encoding: "utf8" }).error === void 0;
	} catch {
		return false;
	}
};
const loopbackSocketDir = () => NodePath.join(NodeOs.tmpdir(), "alchemy-dev-loopback");
const addPort = (ports, raw) => {
	const port = Number(raw);
	if (Number.isInteger(port) && port > 0 && port <= 65535) ports.add(port);
};
/**
* Ports a container will dial if this env value is a loopback URL / DSN.
* Empty when the value is not loopback (Neon, PlanetScale, …).
*/
const loopbackPortsFromEnvValue = (value) => {
	if (!LOOPBACK_HOST_IN_VALUE.test(value)) return [];
	LOOPBACK_HOST_IN_VALUE.lastIndex = 0;
	const ports = /* @__PURE__ */ new Set();
	for (const match of value.matchAll(LOOPBACK_HOST_PORT)) if (match[1] !== void 0) addPort(ports, match[1]);
	for (const match of value.matchAll(DSN_PORT)) if (match[1] !== void 0) addPort(ports, match[1]);
	return [...ports];
};
const loopbackPortsFromEnv = (env) => {
	const ports = /* @__PURE__ */ new Set();
	if (env === void 0) return [];
	const values = Array.isArray(env) ? env.map((entry) => {
		const eq = entry.indexOf("=");
		return eq === -1 ? "" : entry.slice(eq + 1);
	}) : Object.values(env);
	for (const value of values) for (const port of loopbackPortsFromEnvValue(value)) ports.add(port);
	return [...ports];
};
const sidecarLoopbackExtraHost = () => usesUnixSocketLoopback() ? `${CONTAINER_LOOPBACK_ALIAS}:127.0.0.1` : `${CONTAINER_LOOPBACK_ALIAS}:host-gateway`;
const sidecarLoopbackBinds = (ports) => {
	if (!usesUnixSocketLoopback() || ports.length === 0) return [];
	return [`${loopbackSocketDir()}:${CONTAINER_LOOPBACK_MOUNT}`];
};
const mergeSidecarLoopbackHostConfig = (original, ports) => {
	const extraHost = sidecarLoopbackExtraHost();
	const extraHosts = [...original?.ExtraHosts ?? []];
	if (!extraHosts.includes(extraHost)) extraHosts.push(extraHost);
	const binds = [...original?.Binds ?? []];
	for (const bind of sidecarLoopbackBinds(ports)) if (!binds.includes(bind)) binds.push(bind);
	return {
		...original,
		ExtraHosts: extraHosts,
		...binds.length > 0 ? { Binds: binds } : {}
	};
};
const pipeSockets = (incoming, outgoing) => {
	incoming.pipe(outgoing);
	outgoing.pipe(incoming);
	const fail = () => {
		incoming.destroy();
		outgoing.destroy();
	};
	incoming.on("error", fail);
	outgoing.on("error", fail);
};
const socketPathFor = (port) => NodePath.join(loopbackSocketDir(), `${port}.sock`);
const FORWARDER_SOURCE = `\
import * as net from "node:net";
import * as path from "node:path";

const dir = process.argv[2];
const ports = process.argv.slice(3).map(Number).filter((p) => p > 0 && p <= 65535);
if (dir === undefined || ports.length === 0) process.exit(1);

for (const port of ports) {
  const server = net.createServer((incoming) => {
    const outgoing = net.connect(path.join(dir, \`\${port}.sock\`));
    incoming.pipe(outgoing);
    outgoing.pipe(incoming);
    const fail = () => {
      incoming.destroy();
      outgoing.destroy();
    };
    incoming.on("error", fail);
    outgoing.on("error", fail);
  });
  server.listen({ host: "127.0.0.1", port, exclusive: false });
}
`;
const writeForwarderScript = (dir) => {
	const file = NodePath.join(dir, "forward.mjs");
	NodeFs.writeFileSync(file, FORWARDER_SOURCE);
	return file;
};
/**
* Host unix socket per port → `127.0.0.1:port`. The container never SYNs
* the docker bridge; UFW INPUT is not on this path.
*/
const ensureLoopbackUnixSockets = (ports) => {
	if (!usesUnixSocketLoopback()) return;
	const unique = [...new Set(ports.filter((port) => Number.isInteger(port) && port > 0))];
	if (unique.length === 0) return;
	const dir = loopbackSocketDir();
	NodeFs.mkdirSync(dir, { recursive: true });
	writeForwarderScript(dir);
	for (const port of unique) {
		if (unixServers.has(port)) continue;
		const sock = socketPathFor(port);
		try {
			NodeFs.unlinkSync(sock);
		} catch {}
		const server = NodeNet.createServer((incoming) => {
			const outgoing = NodeNet.connect({
				host: "127.0.0.1",
				port
			});
			pipeSockets(incoming, outgoing);
		});
		server.on("error", () => {
			unixServers.delete(port);
		});
		server.listen(sock);
		unixServers.set(port, server);
	}
};
const closeLoopbackUnixSockets = () => {
	for (const [port, server] of unixServers) {
		unixServers.delete(port);
		server.close();
		try {
			NodeFs.unlinkSync(socketPathFor(port));
		} catch {}
	}
	for (const [id, child] of netnsForwarders) {
		netnsForwarders.delete(id);
		child.kill();
	}
};
const ufwAllowHint = (ports) => {
	return `sudo ufw allow from 172.16.0.0/12 to any port ${ports.length > 0 ? ports.join(",") : "<prisma-or-dev-ports>"} proto tcp`;
};
/**
* Listen on `127.0.0.1:<port>` in the sidecar netns and forward through
* the host unix socket. Only the network namespace is entered so this
* process still uses the host filesystem (and host bun/node).
*/
const attachLoopbackNetnsForwarder = (input) => {
	if (!usesUnixSocketLoopback()) return { ok: true };
	const ports = [...new Set(input.ports.filter((port) => Number.isInteger(port) && port > 0))];
	if (ports.length === 0) return { ok: true };
	if (input.pid <= 0) return {
		ok: false,
		error: "container pid is not available"
	};
	if (!nsenterAvailable()) return {
		ok: false,
		error: "nsenter not found on PATH"
	};
	const keys = [...new Set(input.keys.filter((key) => key.length > 0))];
	for (const key of keys) netnsForwarders.get(key)?.kill();
	const dir = loopbackSocketDir();
	const script = writeForwarderScript(dir);
	try {
		const child = NodeChild.spawn("nsenter", [
			"-t",
			String(input.pid),
			"-n",
			"--",
			process.execPath,
			script,
			dir,
			...ports.map(String)
		], { stdio: "ignore" });
		child.on("error", () => {
			for (const key of keys) if (netnsForwarders.get(key) === child) netnsForwarders.delete(key);
		});
		child.on("exit", () => {
			for (const key of keys) if (netnsForwarders.get(key) === child) netnsForwarders.delete(key);
		});
		for (const key of keys) netnsForwarders.set(key, child);
		return { ok: true };
	} catch (error) {
		return {
			ok: false,
			error: error instanceof Error ? error.message : String(error)
		};
	}
};
const detachLoopbackNetnsForwarder = (containerId) => {
	const child = netnsForwarders.get(containerId);
	if (child === void 0) return;
	netnsForwarders.delete(containerId);
	child.kill();
};
const isContainerStartPath = (url) => url !== void 0 && /\/containers\/[^/]+\/start(?:\?|$)/.test(url);
const containerIdFromPath = (url) => {
	if (url === void 0) return void 0;
	return /\/containers\/([^/?]+)/.exec(url)?.[1];
};
//#endregion
export { CONTAINER_LOOPBACK_ALIAS, CONTAINER_LOOPBACK_MOUNT, attachLoopbackNetnsForwarder, closeLoopbackUnixSockets, containerIdFromPath, detachLoopbackNetnsForwarder, ensureLoopbackUnixSockets, isContainerStartPath, loopbackPortsFromEnv, loopbackPortsFromEnvValue, loopbackSocketDir, mergeSidecarLoopbackHostConfig, sidecarLoopbackBinds, sidecarLoopbackExtraHost, ufwAllowHint, usesUnixSocketLoopback };

//# sourceMappingURL=DockerLoopback.mjs.map