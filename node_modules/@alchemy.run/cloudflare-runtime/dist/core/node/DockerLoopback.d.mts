//#region src/core/DockerLoopback.d.ts
/**
 * Hostname rewritten into container env so loopback still looks local
 * (Prisma's `prisma+postgres://` plain-HTTP gate). Mapped in the sidecar
 * ExtraHosts — to `127.0.0.1` on native Linux (unix-socket tunnel) or
 * Docker `host-gateway` on Docker Desktop.
 */
declare const CONTAINER_LOOPBACK_ALIAS = "host.docker.localhost";
/** Bind-mount target inside the workerd `<name>-proxy` sidecar. */
declare const CONTAINER_LOOPBACK_MOUNT = "/alchemy/host-loopback";
/**
 * Native Linux Docker: a SYN to `host-gateway` (bridge IP) is host INPUT
 * and UFW/nftables can drop it. Docker Desktop's gateway already reaches
 * host loopback, so the unix-socket path is Linux-only.
 */
declare const usesUnixSocketLoopback: () => boolean;
declare const loopbackSocketDir: () => string;
/**
 * Ports a container will dial if this env value is a loopback URL / DSN.
 * Empty when the value is not loopback (Neon, PlanetScale, …).
 */
declare const loopbackPortsFromEnvValue: (value: string) => number[];
declare const loopbackPortsFromEnv: (env: Record<string, string> | ReadonlyArray<string> | undefined) => number[];
declare const sidecarLoopbackExtraHost: () => "host.docker.localhost:127.0.0.1" | "host.docker.localhost:host-gateway";
declare const sidecarLoopbackBinds: (ports: readonly number[]) => readonly string[];
type HostConfig = {
  ExtraHosts?: Array<string>;
  Binds?: Array<string>;
};
declare const mergeSidecarLoopbackHostConfig: <T extends HostConfig>(original: T | undefined, ports: readonly number[]) => T & {
  ExtraHosts: string[];
  Binds?: string[];
};
/**
 * Host unix socket per port → `127.0.0.1:port`. The container never SYNs
 * the docker bridge; UFW INPUT is not on this path.
 */
declare const ensureLoopbackUnixSockets: (ports: readonly number[]) => void;
declare const closeLoopbackUnixSockets: () => void;
declare const ufwAllowHint: (ports: readonly number[]) => string;
/**
 * Listen on `127.0.0.1:<port>` in the sidecar netns and forward through
 * the host unix socket. Only the network namespace is entered so this
 * process still uses the host filesystem (and host bun/node).
 */
declare const attachLoopbackNetnsForwarder: (input: {
  keys: readonly string[];
  pid: number;
  ports: readonly number[];
}) => {
  ok: true;
} | {
  ok: false;
  error: string;
};
declare const detachLoopbackNetnsForwarder: (containerId: string) => void;
declare const isContainerStartPath: (url: string | undefined) => boolean;
declare const containerIdFromPath: (url: string | undefined) => string | undefined;
//#endregion
export { CONTAINER_LOOPBACK_ALIAS, CONTAINER_LOOPBACK_MOUNT, attachLoopbackNetnsForwarder, closeLoopbackUnixSockets, containerIdFromPath, detachLoopbackNetnsForwarder, ensureLoopbackUnixSockets, isContainerStartPath, loopbackPortsFromEnv, loopbackPortsFromEnvValue, loopbackSocketDir, mergeSidecarLoopbackHostConfig, sidecarLoopbackBinds, sidecarLoopbackExtraHost, ufwAllowHint, usesUnixSocketLoopback };
//# sourceMappingURL=DockerLoopback.d.mts.map