import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Redacted from "effect/Redacted";
import { ConfigError } from "@distilled.cloud/core/errors";
export declare const DEFAULT_API_BASE_URL = "https://api.prisma.io";
export interface Config {
    readonly apiToken: Redacted.Redacted<string>;
    readonly apiBaseUrl: string;
}
declare const Credentials_base: Context.ServiceClass<Credentials, "PrismaCredentials", Effect.Effect<Config, ConfigError, never>>;
export declare class Credentials extends Credentials_base {
}
/** Layer from a plain token + optional base URL. */
export declare const fromApiToken: (config: {
    readonly apiToken: string;
    readonly apiBaseUrl?: string;
}) => Layer.Layer<Credentials>;
/**
 * Layer resolving credentials from `PRISMA_API_TOKEN`, with
 * `PRISMA_POSTGRES_API_TOKEN` as a backwards-compatible fallback.
 * The base URL is fixed to {@link DEFAULT_API_BASE_URL}.
 */
export declare const CredentialsFromEnv: Layer.Layer<Credentials>;
export {};
//# sourceMappingURL=credentials.d.ts.map