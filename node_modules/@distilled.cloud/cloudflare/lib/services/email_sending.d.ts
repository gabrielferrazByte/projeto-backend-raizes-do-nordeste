import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { type CloudflareOpError, type CloudflareOpContext } from "../protocol.ts";
import { ResultInfo } from "../pagination.ts";
export type { CloudflareOpError, CloudflareOpContext };
declare const Forbidden_base: S.Class<Forbidden, S.TaggedStruct<"Forbidden", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class Forbidden extends /*@__PURE__*/ Forbidden_base {
}
declare const SendingSubdomainAlreadyExists_base: S.Class<SendingSubdomainAlreadyExists, S.TaggedStruct<"SendingSubdomainAlreadyExists", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class SendingSubdomainAlreadyExists extends /*@__PURE__*/ SendingSubdomainAlreadyExists_base {
}
declare const SendingSubdomainNotFound_base: S.Class<SendingSubdomainNotFound, S.TaggedStruct<"SendingSubdomainNotFound", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class SendingSubdomainNotFound extends /*@__PURE__*/ SendingSubdomainNotFound_base {
}
export interface CreateSubdomainRequest {
    /** Identifier. */
    zoneId: string;
    /** The domain name within the zone. A wildcard is allowed only as the complete leftmost label (`*.example.com`) and requires the account wildcard Email Sending entitlement. */
    name: string;
}
export declare const CreateSubdomainRequest: S.Schema<CreateSubdomainRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface CreateSubdomainResponse {
    /** Whether Email Sending is enabled on this subdomain. */
    enabled: boolean;
    /** The exact domain name or a leftmost wildcard such as `*.example.com`. */
    name: string;
    /** Sending subdomain identifier. */
    tag: string;
    /** The date and time the destination address has been created. */
    created?: string | null;
    /** The DKIM selector used for email signing. Wildcard rows publish the selector and sign with `d=&lt;base&gt;`. */
    dkimSelector?: string | null;
    /** Whether a send request that includes a recipient suppressed on this subdomain drops that recipient and still delivers to the rest, instead of failing the entire request. */
    dropSuppressedRecipients?: boolean | null;
    /** The date and time the destination address was last modified. */
    modified?: string | null;
    /** Whether sent messages from this subdomain can be previewed in the activity log. */
    previewEnabled?: boolean | null;
    /** The return-path domain used for bounce handling. Wildcard rows use `cf-bounce.&lt;base&gt;`. */
    returnPathDomain?: string | null;
}
export declare const CreateSubdomainResponse: S.Schema<CreateSubdomainResponse>;
export interface CreateSuppressionRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** The email address to suppress. */
    email: string;
    /** Expiration timestamp for the suppression. Omit or set to null for a permanent suppression that never expires. */
    expiresAt?: string;
    /** Advisory note for this suppression. Not enforced or validated beyond length. */
    note?: string;
}
export declare const CreateSuppressionRequest: S.Schema<CreateSuppressionRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface CreateSuppressionResponse {
    /** The suppression's identifier. */
    id: string;
}
export declare const CreateSuppressionResponse: S.Schema<CreateSuppressionResponse>;
export interface DeleteSubdomainRequest {
    /** Identifier. */
    zoneId: string;
    /** Sending subdomain identifier. */
    subdomainId: string;
}
export declare const DeleteSubdomainRequest: S.Schema<DeleteSubdomainRequest>;
export interface DeleteSubdomainResponse {
}
export declare const DeleteSubdomainResponse: S.Schema<DeleteSubdomainResponse>;
export interface DeleteSuppressionRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** The suppression's identifier. */
    suppressionId: string;
}
export declare const DeleteSuppressionRequest: S.Schema<DeleteSuppressionRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface DeleteSuppressionResponse {
    /** The suppression's identifier. */
    id: string;
}
export declare const DeleteSuppressionResponse: S.Schema<DeleteSuppressionResponse>;
export interface EditSubdomainRequest {
    /** Identifier. */
    zoneId: string;
    /** Sending subdomain identifier. */
    subdomainId: string;
    /** Whether a send request that includes a recipient suppressed on this subdomain drops that recipient and still delivers to the rest, instead of failing the entire request. */
    dropSuppressedRecipients?: boolean;
    /** Whether sent messages from this subdomain can be previewed in the activity log. */
    previewEnabled?: boolean;
}
export declare const EditSubdomainRequest: S.Schema<EditSubdomainRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface EditSubdomainResponse {
    /** Whether Email Sending is enabled on this subdomain. */
    enabled: boolean;
    /** The exact domain name or a leftmost wildcard such as `*.example.com`. */
    name: string;
    /** Sending subdomain identifier. */
    tag: string;
    /** The date and time the destination address has been created. */
    created?: string | null;
    /** The DKIM selector used for email signing. Wildcard rows publish the selector and sign with `d=&lt;base&gt;`. */
    dkimSelector?: string | null;
    /** Whether a send request that includes a recipient suppressed on this subdomain drops that recipient and still delivers to the rest, instead of failing the entire request. */
    dropSuppressedRecipients?: boolean | null;
    /** The date and time the destination address was last modified. */
    modified?: string | null;
    /** Whether sent messages from this subdomain can be previewed in the activity log. */
    previewEnabled?: boolean | null;
    /** The return-path domain used for bounce handling. Wildcard rows use `cf-bounce.&lt;base&gt;`. */
    returnPathDomain?: string | null;
}
export declare const EditSubdomainResponse: S.Schema<EditSubdomainResponse>;
export interface EditSuppressionRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** The suppression's identifier. */
    suppressionId: string;
    /** New expiry. Send `null` to make the suppression permanent; omit to leave it unchanged. */
    expiresAt?: string;
    /** Replacement advisory note. Send an empty string to clear it; omit to leave it unchanged. */
    note?: string;
}
export declare const EditSuppressionRequest: S.Schema<EditSuppressionRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface EditSuppressionResponse {
    /** Unique identifier for this suppression. */
    id: string;
    /** When the suppression was created. */
    createdAt: string;
    /** The suppressed email address. */
    email: string;
    /** When the suppression expires. Null for a permanent suppression. */
    expiresAt: string;
    /** Whether clients may mutate this suppression. This is determined by the server and must not be inferred from `reason`. */
    readOnly: boolean;
    /** Why the address is suppressed: `manual`, `complaint`, `hard_bounce`, `soft_bounce`, or `policy`. */
    reason: string;
    /** Advisory note for this suppression, if any. */
    note?: string | null;
}
export declare const EditSuppressionResponse: S.Schema<EditSuppressionResponse>;
export interface GetSubdomainRequest {
    /** Identifier. */
    zoneId: string;
    /** Sending subdomain identifier. */
    subdomainId: string;
}
export declare const GetSubdomainRequest: S.Schema<GetSubdomainRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface GetSubdomainResponse {
    /** Whether Email Sending is enabled on this subdomain. */
    enabled: boolean;
    /** The exact domain name or a leftmost wildcard such as `*.example.com`. */
    name: string;
    /** Sending subdomain identifier. */
    tag: string;
    /** The date and time the destination address has been created. */
    created?: string | null;
    /** The DKIM selector used for email signing. Wildcard rows publish the selector and sign with `d=&lt;base&gt;`. */
    dkimSelector?: string | null;
    /** Whether a send request that includes a recipient suppressed on this subdomain drops that recipient and still delivers to the rest, instead of failing the entire request. */
    dropSuppressedRecipients?: boolean | null;
    /** The date and time the destination address was last modified. */
    modified?: string | null;
    /** Whether sent messages from this subdomain can be previewed in the activity log. */
    previewEnabled?: boolean | null;
    /** The return-path domain used for bounce handling. Wildcard rows use `cf-bounce.&lt;base&gt;`. */
    returnPathDomain?: string | null;
}
export declare const GetSubdomainResponse: S.Schema<GetSubdomainResponse>;
export interface GetSubdomainDnsRequest {
    /** Identifier. */
    zoneId: string;
    /** Sending subdomain identifier. */
    subdomainId: string;
}
export declare const GetSubdomainDnsRequest: S.Schema<GetSubdomainDnsRequest>;
export type SubdomainsDnsGetResultItemType = "A" | "AAAA" | "CNAME" | "HTTPS" | "TXT" | "SRV" | "LOC" | "MX" | "NS" | "CERT" | "DNSKEY" | "DS" | "NAPTR" | "SMIMEA" | "SSHFP" | "SVCB" | "TLSA" | "URI";
export declare const SubdomainsDnsGetResultItemType: any;
export interface SubdomainsDnsGetResultItem {
    /** DNS record content. */
    content?: string | null;
    /** DNS record name (or @ for the zone apex). */
    name?: string | null;
    /** Required for MX, SRV and URI records. Unused by other record types. Records with lower priorities are preferred. */
    priority?: number | null;
    /** Time to live, in seconds, of the DNS record. Must be between 60 and 86400, or 1 for 'automatic'. */
    ttl?: number | null;
    /** DNS record type. */
    type?: SubdomainsDnsGetResultItemType | null;
}
export declare const SubdomainsDnsGetResultItem: S.Schema<SubdomainsDnsGetResultItem>;
export type SubdomainsDnsGetResultList = Array<SubdomainsDnsGetResultItem>;
export declare const SubdomainsDnsGetResultList: S.Schema<SubdomainsDnsGetResultList>;
export interface GetSubdomainDnsResponse {
    /** The unwrapped `result` payload of the v4 response envelope. */
    result: SubdomainsDnsGetResultList;
    /** Pagination info from the envelope's `result_info`. */
    resultInfo?: ResultInfo | null;
}
export declare const GetSubdomainDnsResponse: S.Schema<GetSubdomainDnsResponse>;
export interface GetSuppressionRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** The suppression's identifier. */
    suppressionId: string;
}
export declare const GetSuppressionRequest: S.Schema<GetSuppressionRequest>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface GetSuppressionResponse {
    /** Unique identifier for this suppression. */
    id: string;
    /** When the suppression was created. */
    createdAt: string;
    /** The suppressed email address. */
    email: string;
    /** When the suppression expires. Null for a permanent suppression. */
    expiresAt: string;
    /** Whether clients may mutate this suppression. This is determined by the server and must not be inferred from `reason`. */
    readOnly: boolean;
    /** Why the address is suppressed: `manual`, `complaint`, `hard_bounce`, `soft_bounce`, or `policy`. */
    reason: string;
    /** Advisory note for this suppression, if any. */
    note?: string | null;
}
export declare const GetSuppressionResponse: S.Schema<GetSuppressionResponse>;
export interface ImportSuppressionRequestItemsItem {
    /** The email address to suppress. */
    email: string;
    /** Expiration timestamp for the suppression. Omit or set to null for a permanent suppression that never expires. */
    expiresAt?: string;
    /** Advisory note for this suppression. Not enforced or validated beyond length. */
    note?: string;
}
export declare const ImportSuppressionRequestItemsItem: S.Schema<ImportSuppressionRequestItemsItem>;
export type ImportSuppressionRequestItemsList = Array<ImportSuppressionRequestItemsItem>;
export declare const ImportSuppressionRequestItemsList: S.Schema<ImportSuppressionRequestItemsList>;
export interface ImportSuppressionRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** Suppressions to import. Items with a duplicate email address are deduplicated before processing. */
    items: ImportSuppressionRequestItemsList;
}
export declare const ImportSuppressionRequest: S.Schema<ImportSuppressionRequest>;
export type ImportSuppressionResponseItemsItemStatus = "processed" | "invalid" | "error" | "skipped";
export declare const ImportSuppressionResponseItemsItemStatus: any;
export interface ImportSuppressionResponseItemsItem {
    /** Zero-based index of this item in the request body. */
    index: number;
    /** Outcome for this item. */
    status: ImportSuppressionResponseItemsItemStatus;
    /** The created or promoted suppression's identifier. Present when `status` is `processed`. */
    id?: string | null;
    /** The submitted email address for this item. */
    email?: string | null;
    /** Human-readable error message. Present when `status` is `invalid`, `error`, or `skipped`. */
    error?: string | null;
}
export declare const ImportSuppressionResponseItemsItem: S.Schema<ImportSuppressionResponseItemsItem>;
export type ImportSuppressionResponseItemsList = Array<ImportSuppressionResponseItemsItem>;
export declare const ImportSuppressionResponseItemsList: S.Schema<ImportSuppressionResponseItemsList>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface ImportSuppressionResponse {
    /** Number of items dropped because their email address repeated an earlier item in this request. Counted once and excluded from `items`. */
    deduplicated: number;
    /** Number of items that failed to import due to an unexpected error. */
    errors: number;
    /** Number of items with an invalid email address. */
    invalid: number;
    /** Per-item results, in the same order as the request body. */
    items: ImportSuppressionResponseItemsList;
    /** Number of items successfully created or promoted. */
    processed: number;
    /** Number of items skipped because the existing suppression is not customer-managed (for example, a read-only policy suppression). */
    skipped: number;
    /** Total number of items in the request body, including duplicates. */
    total: number;
}
export declare const ImportSuppressionResponse: S.Schema<ImportSuppressionResponse>;
export interface ListSubdomainsRequest {
    /** Identifier. */
    zoneId: string;
}
export declare const ListSubdomainsRequest: S.Schema<ListSubdomainsRequest>;
export interface SubdomainsListResultItem {
    /** Whether Email Sending is enabled on this subdomain. */
    enabled: boolean;
    /** The exact domain name or a leftmost wildcard such as `*.example.com`. */
    name: string;
    /** Sending subdomain identifier. */
    tag: string;
    /** The date and time the destination address has been created. */
    created?: string | null;
    /** The DKIM selector used for email signing. Wildcard rows publish the selector and sign with `d=&lt;base&gt;`. */
    dkimSelector?: string | null;
    /** Whether a send request that includes a recipient suppressed on this subdomain drops that recipient and still delivers to the rest, instead of failing the entire request. */
    dropSuppressedRecipients?: boolean | null;
    /** The date and time the destination address was last modified. */
    modified?: string | null;
    /** Whether sent messages from this subdomain can be previewed in the activity log. */
    previewEnabled?: boolean | null;
    /** The return-path domain used for bounce handling. Wildcard rows use `cf-bounce.&lt;base&gt;`. */
    returnPathDomain?: string | null;
}
export declare const SubdomainsListResultItem: S.Schema<SubdomainsListResultItem>;
export type SubdomainsListResultList = Array<SubdomainsListResultItem>;
export declare const SubdomainsListResultList: S.Schema<SubdomainsListResultList>;
export interface ListSubdomainsResponse {
    /** The unwrapped `result` payload of the v4 response envelope. */
    result: SubdomainsListResultList;
    /** Pagination info from the envelope's `result_info`. */
    resultInfo?: ResultInfo | null;
}
export declare const ListSubdomainsResponse: S.Schema<ListSubdomainsResponse>;
export type ListSuppressionsRequestReason = "manual" | "complaint" | "hard_bounce" | "soft_bounce" | "policy";
export declare const ListSuppressionsRequestReason: any;
export interface ListSuppressionsRequest {
    /** Cloudflare account ID. */
    accountId: string;
    /** Opaque pagination cursor returned as `result_info.next_cursor`. It carries the filters that produced it. */
    cursor?: string;
    /** Exact email-address filter. */
    email?: string;
    /** Maximum number of suppressions to return per page. */
    perPage?: number;
    /** Filter to suppressions with this reason. */
    reason?: ListSuppressionsRequestReason | (string & {});
    /** A complete address is an exact match; a value ending in `@` matches that username across every domain. Prefix searches may return short intermediate pages while the bounded account scan advances. */
    search?: string;
}
export declare const ListSuppressionsRequest: S.Schema<ListSuppressionsRequest>;
export interface ListSuppressionsResultItem {
    /** Unique identifier for this suppression. */
    id: string;
    /** When the suppression was created. */
    createdAt: string;
    /** The suppressed email address. */
    email: string;
    /** When the suppression expires. Null for a permanent suppression. */
    expiresAt: string;
    /** Whether clients may mutate this suppression. This is determined by the server and must not be inferred from `reason`. */
    readOnly: boolean;
    /** Why the address is suppressed: `manual`, `complaint`, `hard_bounce`, `soft_bounce`, or `policy`. */
    reason: string;
    /** Advisory note for this suppression, if any. */
    note?: string | null;
}
export declare const ListSuppressionsResultItem: S.Schema<ListSuppressionsResultItem>;
export type ListSuppressionsResultList = Array<ListSuppressionsResultItem>;
export declare const ListSuppressionsResultList: S.Schema<ListSuppressionsResultList>;
export type ListSuppressionsResponse = ListSuppressionsResultList;
export declare const ListSuppressionsResponse: S.Schema<ListSuppressionsResponse>;
export interface SendRequestFromEmailSendingEmailAddressObject {
    /** Email address (e.g., '[user@example.com](mailto:user@example.com)'). */
    address: string;
    /** Display name for the email address (e.g., 'John Doe'). Optional; set to null or leave it unset to send the address on its own. */
    name?: string;
}
export declare const SendRequestFromEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestFrom = string | SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestFrom: any;
export type SendRequestAttachmentsItemInlineDisposition = "inline";
export declare const SendRequestAttachmentsItemInlineDisposition: any;
export interface SendRequestAttachmentsItemInline {
    /** Base64-encoded content of the attachment. */
    content: string;
    /** Content ID used to reference this attachment in HTML via cid: URI (e.g., ![](cid:logo)). */
    contentId: string;
    /** Use 'inline' to embed the attachment in the email body. */
    disposition: SendRequestAttachmentsItemInlineDisposition;
    /** Filename for the attachment. */
    filename: string;
    /** MIME type of the attachment (e.g., 'image/png', 'text/plain'). */
    type: string;
}
export declare const SendRequestAttachmentsItemInline: S.Schema<SendRequestAttachmentsItemInline>;
export type SendRequestAttachmentsItemAttachmentDisposition = "attachment";
export declare const SendRequestAttachmentsItemAttachmentDisposition: any;
export interface SendRequestAttachmentsItemAttachment {
    /** Base64-encoded content of the attachment. */
    content: string;
    /** Use 'attachment' for a standard file attachment. */
    disposition: SendRequestAttachmentsItemAttachmentDisposition;
    /** Filename for the attachment. */
    filename: string;
    /** MIME type of the attachment (e.g., 'application/pdf', 'text/plain'). */
    type: string;
}
export declare const SendRequestAttachmentsItemAttachment: S.Schema<SendRequestAttachmentsItemAttachment>;
export type SendRequestAttachmentsItem = SendRequestAttachmentsItemInline | SendRequestAttachmentsItemAttachment;
export declare const SendRequestAttachmentsItem: any;
export type SendRequestAttachmentsList = Array<SendRequestAttachmentsItem>;
export declare const SendRequestAttachmentsList: S.Schema<SendRequestAttachmentsList>;
export type SendRequestBccEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestBccEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestBccCase2ItemEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestBccCase2ItemEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestBccCase2Item = string | SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestBccCase2Item: any;
export type SendRequestBccCase2List = Array<SendRequestBccCase2Item>;
export declare const SendRequestBccCase2List: S.Schema<SendRequestBccCase2List>;
export type SendRequestBcc = string | SendRequestFromEmailSendingEmailAddressObject | SendRequestBccCase2List;
export declare const SendRequestBcc: any;
export type SendRequestCcEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestCcEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestCcCase2ItemEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestCcCase2ItemEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestCcCase2Item = string | SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestCcCase2Item: any;
export type SendRequestCcCase2List = Array<SendRequestCcCase2Item>;
export declare const SendRequestCcCase2List: S.Schema<SendRequestCcCase2List>;
export type SendRequestCc = string | SendRequestFromEmailSendingEmailAddressObject | SendRequestCcCase2List;
export declare const SendRequestCc: any;
export type SendRequestHeadersMap = {
    [key: string]: string | undefined;
};
export declare const SendRequestHeadersMap: S.Schema<SendRequestHeadersMap>;
export type SendRequestReplyToEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestReplyToEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestReplyTo = string | SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestReplyTo: any;
export type SendRequestToEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestToEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestToCase2ItemEmailSendingEmailAddressObject = SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestToCase2ItemEmailSendingEmailAddressObject: S.Schema<SendRequestFromEmailSendingEmailAddressObject>;
export type SendRequestToCase2Item = string | SendRequestFromEmailSendingEmailAddressObject;
export declare const SendRequestToCase2Item: any;
export type SendRequestToCase2List = Array<SendRequestToCase2Item>;
export declare const SendRequestToCase2List: S.Schema<SendRequestToCase2List>;
export type SendRequestTo = string | SendRequestFromEmailSendingEmailAddressObject | SendRequestToCase2List;
export declare const SendRequestTo: any;
export interface SendEmailSendingRequest {
    /** Identifier of the account. */
    accountId: string;
    /** Sender email address. Either a plain string or an object with address and name. */
    from: SendRequestFrom;
    /** Email subject line. */
    subject: string;
    /** File attachments and inline images. */
    attachments?: SendRequestAttachmentsList;
    /** Blind carbon copy recipient(s). Optional. A single email string, a named address object, or an array of either. */
    bcc?: SendRequestBcc;
    /** Carbon copy recipient(s). Optional. A single email string, a named address object, or an array of either. */
    cc?: SendRequestCc;
    /** Custom email headers as key-value pairs. */
    headers?: SendRequestHeadersMap;
    /** HTML body of the email. Provide at least one of text or html (non-empty). */
    html?: string;
    /** Reply-to address. Either a plain string or an object with address and name. */
    replyTo?: SendRequestReplyTo;
    /** Plain text body of the email. Provide at least one of text or html (non-empty). */
    text?: string;
    /** Recipient(s). Optional if cc or bcc is provided. A single email string, a named address object, or an array of either. */
    to?: SendRequestTo;
}
export declare const SendEmailSendingRequest: S.Schema<SendEmailSendingRequest>;
export type SendResponseDeliveredList = Array<string>;
export declare const SendResponseDeliveredList: S.Schema<SendResponseDeliveredList>;
export type SendResponsePermanentBouncesList = Array<string>;
export declare const SendResponsePermanentBouncesList: S.Schema<SendResponsePermanentBouncesList>;
export type SendResponseQueuedList = Array<string>;
export declare const SendResponseQueuedList: S.Schema<SendResponseQueuedList>;
export type SendResponseSuppressedRecipientsList = Array<string>;
export declare const SendResponseSuppressedRecipientsList: S.Schema<SendResponseSuppressedRecipientsList>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface SendEmailSendingResponse {
    /** Email addresses to which the message was delivered immediately. */
    delivered: SendResponseDeliveredList;
    /** Message ID of the sent email. */
    messageId: string;
    /** Email addresses that permanently bounced. */
    permanentBounces: SendResponsePermanentBouncesList;
    /** Email addresses for which delivery was queued for later. */
    queued: SendResponseQueuedList;
    /** Email addresses dropped because they are on the suppression list. Returned when suppressed-recipient dropping is enabled for the sending subdomain; otherwise the request fails instead. */
    suppressedRecipients: SendResponseSuppressedRecipientsList;
}
export declare const SendEmailSendingResponse: S.Schema<SendEmailSendingResponse>;
export type SendRawRequestRecipientsList = Array<string>;
export declare const SendRawRequestRecipientsList: S.Schema<SendRawRequestRecipientsList>;
export interface SendRawEmailSendingRequest {
    /** Identifier of the account. */
    accountId: string;
    /** Sender email address. */
    from: string;
    /** The full MIME-encoded email message. Should include standard RFC 5322 headers such as From, To, Subject, and Content-Type. The from and recipients fields in the request body control SMTP envelope routing; the From and To headers in the MIME message control what the recipient's email client displays. */
    mimeMessage: string;
    /** List of recipient email addresses. */
    recipients: SendRawRequestRecipientsList;
}
export declare const SendRawEmailSendingRequest: S.Schema<SendRawEmailSendingRequest>;
export type SendRawResponseDeliveredList = Array<string>;
export declare const SendRawResponseDeliveredList: S.Schema<SendRawResponseDeliveredList>;
export type SendRawResponsePermanentBouncesList = Array<string>;
export declare const SendRawResponsePermanentBouncesList: S.Schema<SendRawResponsePermanentBouncesList>;
export type SendRawResponseQueuedList = Array<string>;
export declare const SendRawResponseQueuedList: S.Schema<SendRawResponseQueuedList>;
export type SendRawResponseSuppressedRecipientsList = Array<string>;
export declare const SendRawResponseSuppressedRecipientsList: S.Schema<SendRawResponseSuppressedRecipientsList>;
/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface SendRawEmailSendingResponse {
    /** Email addresses to which the message was delivered immediately. */
    delivered: SendRawResponseDeliveredList;
    /** Message ID of the sent email. */
    messageId: string;
    /** Email addresses that permanently bounced. */
    permanentBounces: SendRawResponsePermanentBouncesList;
    /** Email addresses for which delivery was queued for later. */
    queued: SendRawResponseQueuedList;
    /** Email addresses dropped because they are on the suppression list. Returned when suppressed-recipient dropping is enabled for the sending subdomain; otherwise the request fails instead. */
    suppressedRecipients: SendRawResponseSuppressedRecipientsList;
}
export declare const SendRawEmailSendingResponse: S.Schema<SendRawEmailSendingResponse>;
export type CreateSubdomainError = Forbidden | SendingSubdomainAlreadyExists | CloudflareOpError;
/** Creates a new sending subdomain or re-enables sending on an existing subdomain that had it disabled. If zone-level Email Sending has not been enabled yet, the zone flag is automatically set when the entitlement is present. A leftmost wildcard such as `*.example.com` is accepted only for accounts with wildcard Email Sending enabled. Wildcard senders share the base domain's DKIM signing identity and `cf-bounce.<base>` return path. */
export declare const createSubdomain: API.OperationMethod<CreateSubdomainRequest, CreateSubdomainResponse, CreateSubdomainError, CloudflareOpContext>;
export type CreateSuppressionError = CloudflareOpError;
/** Creates an account-wide suppression. If a mutable legacy zone-linked row already exists, it is promoted without changing its identifier. */
export declare const createSuppression: API.OperationMethod<CreateSuppressionRequest, CreateSuppressionResponse, CreateSuppressionError, CloudflareOpContext>;
export type DeleteSubdomainError = Forbidden | SendingSubdomainNotFound | CloudflareOpError;
/** Disables sending on a subdomain and removes its DNS records. If routing is still active on the subdomain, only sending is disabled. */
export declare const deleteSubdomain: API.OperationMethod<DeleteSubdomainRequest, DeleteSubdomainResponse, DeleteSubdomainError, CloudflareOpContext>;
export type DeleteSuppressionError = CloudflareOpError;
/** Deletes the suppression, its note, and every legacy internal zone membership, allowing future delivery attempts to the address. */
export declare const deleteSuppression: API.OperationMethod<DeleteSuppressionRequest, DeleteSuppressionResponse, DeleteSuppressionError, CloudflareOpContext>;
export type EditSubdomainError = CloudflareOpError;
/** Updates the activity-log preview preference for a sending subdomain. */
export declare const editSubdomain: API.OperationMethod<EditSubdomainRequest, EditSubdomainResponse, EditSubdomainError, CloudflareOpContext>;
export type EditSuppressionError = CloudflareOpError;
/** Updates expiry or advisory note fields without changing legacy internal zone memberships. */
export declare const editSuppression: API.OperationMethod<EditSuppressionRequest, EditSuppressionResponse, EditSuppressionError, CloudflareOpContext>;
export type GetSubdomainError = Forbidden | SendingSubdomainNotFound | CloudflareOpError;
/** Gets information for a specific sending subdomain. */
export declare const getSubdomain: API.OperationMethod<GetSubdomainRequest, GetSubdomainResponse, GetSubdomainError, CloudflareOpContext>;
export type GetSubdomainDnsError = CloudflareOpError;
/** Returns the expected DNS records for a sending subdomain. */
export declare const getSubdomainDns: API.PaginatedOperationMethod<GetSubdomainDnsRequest, GetSubdomainDnsResponse, GetSubdomainDnsError, CloudflareOpContext, SubdomainsDnsGetResultItem>;
export type GetSuppressionError = CloudflareOpError;
/** Gets an Email Sending suppression owned by the account. */
export declare const getSuppression: API.OperationMethod<GetSuppressionRequest, GetSuppressionResponse, GetSuppressionError, CloudflareOpContext>;
export type ImportSuppressionError = CloudflareOpError;
/** Imports up to 1,000 account-level Email Sending suppressions in one request. */
export declare const importSuppression: API.OperationMethod<ImportSuppressionRequest, ImportSuppressionResponse, ImportSuppressionError, CloudflareOpContext>;
export type ListSubdomainsError = Forbidden | CloudflareOpError;
/** Lists all sending-enabled subdomains for the zone. */
export declare const listSubdomains: API.PaginatedOperationMethod<ListSubdomainsRequest, ListSubdomainsResponse, ListSubdomainsError, CloudflareOpContext, SubdomainsListResultItem>;
export type ListSuppressionsError = CloudflareOpError;
/** Lists every active Email Sending suppression owned by the account, including legacy rows with internal zone memberships. */
export declare const listSuppressions: API.OperationMethod<ListSuppressionsRequest, ListSuppressionsResponse, ListSuppressionsError, CloudflareOpContext>;
export type SendEmailSendingError = CloudflareOpError;
/** Send an email for the specified account using the structured builder. Provide the sender, recipients, subject, and at least one of text or html; attachments are optional. */
export declare const sendEmailSending: API.OperationMethod<SendEmailSendingRequest, SendEmailSendingResponse, SendEmailSendingError, CloudflareOpContext>;
export type SendRawEmailSendingError = CloudflareOpError;
/** Send a raw RFC 5322 (MIME) email for the specified account. Provide the full MIME message plus the SMTP envelope (from and recipients). */
export declare const sendRawEmailSending: API.OperationMethod<SendRawEmailSendingRequest, SendRawEmailSendingResponse, SendRawEmailSendingError, CloudflareOpContext>;
//# sourceMappingURL=email_sending.d.ts.map