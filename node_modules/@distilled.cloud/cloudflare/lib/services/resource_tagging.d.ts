import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { type CloudflareOpError, type CloudflareOpContext } from "../protocol.ts";
import { ResultInfo } from "../pagination.ts";
export type { CloudflareOpError, CloudflareOpContext };
declare const Forbidden_base: S.Class<Forbidden, S.TaggedStruct<"Forbidden", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class Forbidden extends /*@__PURE__*/ Forbidden_base {
}
declare const TagPreconditionFailed_base: S.Class<TagPreconditionFailed, S.TaggedStruct<"TagPreconditionFailed", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class TagPreconditionFailed extends /*@__PURE__*/ TagPreconditionFailed_base {
}
declare const ZoneTagResourceNotFound_base: S.Class<ZoneTagResourceNotFound, S.TaggedStruct<"ZoneTagResourceNotFound", {
    readonly code: any;
    readonly message: any;
}>, import("effect/Cause").YieldableError>;
export declare class ZoneTagResourceNotFound extends /*@__PURE__*/ ZoneTagResourceNotFound_base {
}
export interface DeleteAccountTagRequest {
    /** Identifier. */
    accountId: string;
    ifMatch?: string;
    /** Identifier of the resource to remove tags from. */
    resourceId?: string;
    /** Type of the resource to remove tags from. */
    resourceType?: string;
    /** Identifier of the Worker to remove tags from. */
    workerId?: string;
}
export declare const DeleteAccountTagRequest: S.Schema<DeleteAccountTagRequest>;
export interface DeleteAccountTagResponse {
}
export declare const DeleteAccountTagResponse: S.Schema<DeleteAccountTagResponse>;
export interface DeleteZoneTagRequest {
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    ifMatch?: string;
    /** Identifier of the resource to remove tags from. */
    resourceId?: string;
    /** Type of the resource to remove tags from. */
    resourceType?: string;
    /** Identifier of the Access application to remove tags from. */
    accessApplicationId?: string;
}
export declare const DeleteZoneTagRequest: S.Schema<DeleteZoneTagRequest>;
export interface DeleteZoneTagResponse {
}
export declare const DeleteZoneTagResponse: S.Schema<DeleteZoneTagResponse>;
export type AccountTagsGetRequestResourceType = "access_application" | "access_group" | "account" | "account_ruleset" | "ai_gateway" | "alerting_policy" | "alerting_webhook" | "cloudflared_tunnel" | "cws_deployment" | "cws_policy" | "cws_policy_set" | "cws_workload" | "d1_database" | "durable_object_namespace" | "gateway_list" | "gateway_rule" | "image" | "infrastructure_target" | "kv_namespace" | "load_balancer_monitor" | "load_balancer_pool" | "pages_project" | "queue" | "r2_bucket" | "resource_share" | "stream_live_input" | "stream_video" | "vectorize_index" | "worker" | "worker_version";
export declare const AccountTagsGetRequestResourceType: any;
export interface GetAccountTagRequest {
    /** Identifier. */
    accountId: string;
    /** The ID of the resource to retrieve tags for. */
    resourceId: string;
    /** The type of the resource. */
    resourceType: AccountTagsGetRequestResourceType | (string & {});
    /** Worker identifier. Required for worker_version resources. */
    workerId?: string;
}
export declare const GetAccountTagRequest: S.Schema<GetAccountTagRequest>;
export type AccountTagsGetResultAccessApplicationTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAccessApplicationTagsMap: S.Schema<AccountTagsGetResultAccessApplicationTagsMap>;
export type AccountTagsGetResultAccessApplicationType = "access_application";
export declare const AccountTagsGetResultAccessApplicationType: any;
export interface AccountTagsGetResultAccessApplication {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAccessApplicationTagsMap;
    type: AccountTagsGetResultAccessApplicationType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAccessApplication: S.Schema<AccountTagsGetResultAccessApplication>;
export type AccountTagsGetResultAccessApplicationPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAccessApplicationPolicyTagsMap: S.Schema<AccountTagsGetResultAccessApplicationPolicyTagsMap>;
export type AccountTagsGetResultAccessApplicationPolicyType = "access_application_policy";
export declare const AccountTagsGetResultAccessApplicationPolicyType: any;
export interface AccountTagsGetResultAccessApplicationPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAccessApplicationPolicyTagsMap;
    type: AccountTagsGetResultAccessApplicationPolicyType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAccessApplicationPolicy: S.Schema<AccountTagsGetResultAccessApplicationPolicy>;
export type AccountTagsGetResultAccessGroupTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAccessGroupTagsMap: S.Schema<AccountTagsGetResultAccessGroupTagsMap>;
export type AccountTagsGetResultAccessGroupType = "access_group";
export declare const AccountTagsGetResultAccessGroupType: any;
export interface AccountTagsGetResultAccessGroup {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAccessGroupTagsMap;
    type: AccountTagsGetResultAccessGroupType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAccessGroup: S.Schema<AccountTagsGetResultAccessGroup>;
export type AccountTagsGetResultAccountTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAccountTagsMap: S.Schema<AccountTagsGetResultAccountTagsMap>;
export type AccountTagsGetResultAccountType = "account";
export declare const AccountTagsGetResultAccountType: any;
export interface AccountTagsGetResultAccount {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAccountTagsMap;
    type: AccountTagsGetResultAccountType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAccount: S.Schema<AccountTagsGetResultAccount>;
export type AccountTagsGetResultAccountRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAccountRulesetTagsMap: S.Schema<AccountTagsGetResultAccountRulesetTagsMap>;
export type AccountTagsGetResultAccountRulesetType = "account_ruleset";
export declare const AccountTagsGetResultAccountRulesetType: any;
export interface AccountTagsGetResultAccountRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAccountRulesetTagsMap;
    type: AccountTagsGetResultAccountRulesetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAccountRuleset: S.Schema<AccountTagsGetResultAccountRuleset>;
export type AccountTagsGetResultAIGatewayTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAIGatewayTagsMap: S.Schema<AccountTagsGetResultAIGatewayTagsMap>;
export type AccountTagsGetResultAIGatewayType = "ai_gateway";
export declare const AccountTagsGetResultAIGatewayType: any;
export interface AccountTagsGetResultAIGateway {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAIGatewayTagsMap;
    type: AccountTagsGetResultAIGatewayType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAIGateway: S.Schema<AccountTagsGetResultAIGateway>;
export type AccountTagsGetResultAlertingPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAlertingPolicyTagsMap: S.Schema<AccountTagsGetResultAlertingPolicyTagsMap>;
export type AccountTagsGetResultAlertingPolicyType = "alerting_policy";
export declare const AccountTagsGetResultAlertingPolicyType: any;
export interface AccountTagsGetResultAlertingPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAlertingPolicyTagsMap;
    type: AccountTagsGetResultAlertingPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAlertingPolicy: S.Schema<AccountTagsGetResultAlertingPolicy>;
export type AccountTagsGetResultAlertingWebhookTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAlertingWebhookTagsMap: S.Schema<AccountTagsGetResultAlertingWebhookTagsMap>;
export type AccountTagsGetResultAlertingWebhookType = "alerting_webhook";
export declare const AccountTagsGetResultAlertingWebhookType: any;
export interface AccountTagsGetResultAlertingWebhook {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAlertingWebhookTagsMap;
    type: AccountTagsGetResultAlertingWebhookType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAlertingWebhook: S.Schema<AccountTagsGetResultAlertingWebhook>;
export type AccountTagsGetResultAPIGatewayOperationTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultAPIGatewayOperationTagsMap: S.Schema<AccountTagsGetResultAPIGatewayOperationTagsMap>;
export type AccountTagsGetResultAPIGatewayOperationType = "api_gateway_operation";
export declare const AccountTagsGetResultAPIGatewayOperationType: any;
export interface AccountTagsGetResultAPIGatewayOperation {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultAPIGatewayOperationTagsMap;
    type: AccountTagsGetResultAPIGatewayOperationType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultAPIGatewayOperation: S.Schema<AccountTagsGetResultAPIGatewayOperation>;
export type AccountTagsGetResultCloudflaredTunnelTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCloudflaredTunnelTagsMap: S.Schema<AccountTagsGetResultCloudflaredTunnelTagsMap>;
export type AccountTagsGetResultCloudflaredTunnelType = "cloudflared_tunnel";
export declare const AccountTagsGetResultCloudflaredTunnelType: any;
export interface AccountTagsGetResultCloudflaredTunnel {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCloudflaredTunnelTagsMap;
    type: AccountTagsGetResultCloudflaredTunnelType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCloudflaredTunnel: S.Schema<AccountTagsGetResultCloudflaredTunnel>;
export type AccountTagsGetResultCustomCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCustomCertificateTagsMap: S.Schema<AccountTagsGetResultCustomCertificateTagsMap>;
export type AccountTagsGetResultCustomCertificateType = "custom_certificate";
export declare const AccountTagsGetResultCustomCertificateType: any;
export interface AccountTagsGetResultCustomCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCustomCertificateTagsMap;
    type: AccountTagsGetResultCustomCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCustomCertificate: S.Schema<AccountTagsGetResultCustomCertificate>;
export type AccountTagsGetResultCustomHostnameTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCustomHostnameTagsMap: S.Schema<AccountTagsGetResultCustomHostnameTagsMap>;
export type AccountTagsGetResultCustomHostnameType = "custom_hostname";
export declare const AccountTagsGetResultCustomHostnameType: any;
export interface AccountTagsGetResultCustomHostname {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCustomHostnameTagsMap;
    type: AccountTagsGetResultCustomHostnameType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCustomHostname: S.Schema<AccountTagsGetResultCustomHostname>;
export type AccountTagsGetResultCwsDeploymentTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCwsDeploymentTagsMap: S.Schema<AccountTagsGetResultCwsDeploymentTagsMap>;
export type AccountTagsGetResultCwsDeploymentType = "cws_deployment";
export declare const AccountTagsGetResultCwsDeploymentType: any;
export interface AccountTagsGetResultCwsDeployment {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCwsDeploymentTagsMap;
    type: AccountTagsGetResultCwsDeploymentType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCwsDeployment: S.Schema<AccountTagsGetResultCwsDeployment>;
export type AccountTagsGetResultCwsPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCwsPolicyTagsMap: S.Schema<AccountTagsGetResultCwsPolicyTagsMap>;
export type AccountTagsGetResultCwsPolicyType = "cws_policy";
export declare const AccountTagsGetResultCwsPolicyType: any;
export interface AccountTagsGetResultCwsPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCwsPolicyTagsMap;
    type: AccountTagsGetResultCwsPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCwsPolicy: S.Schema<AccountTagsGetResultCwsPolicy>;
export type AccountTagsGetResultCwsPolicySetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCwsPolicySetTagsMap: S.Schema<AccountTagsGetResultCwsPolicySetTagsMap>;
export type AccountTagsGetResultCwsPolicySetType = "cws_policy_set";
export declare const AccountTagsGetResultCwsPolicySetType: any;
export interface AccountTagsGetResultCwsPolicySet {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCwsPolicySetTagsMap;
    type: AccountTagsGetResultCwsPolicySetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCwsPolicySet: S.Schema<AccountTagsGetResultCwsPolicySet>;
export type AccountTagsGetResultCwsWorkloadTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultCwsWorkloadTagsMap: S.Schema<AccountTagsGetResultCwsWorkloadTagsMap>;
export type AccountTagsGetResultCwsWorkloadType = "cws_workload";
export declare const AccountTagsGetResultCwsWorkloadType: any;
export interface AccountTagsGetResultCwsWorkload {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultCwsWorkloadTagsMap;
    type: AccountTagsGetResultCwsWorkloadType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultCwsWorkload: S.Schema<AccountTagsGetResultCwsWorkload>;
export type AccountTagsGetResultD1DatabaseTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultD1DatabaseTagsMap: S.Schema<AccountTagsGetResultD1DatabaseTagsMap>;
export type AccountTagsGetResultD1DatabaseType = "d1_database";
export declare const AccountTagsGetResultD1DatabaseType: any;
export interface AccountTagsGetResultD1Database {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultD1DatabaseTagsMap;
    type: AccountTagsGetResultD1DatabaseType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultD1Database: S.Schema<AccountTagsGetResultD1Database>;
export type AccountTagsGetResultDNSRecordTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultDNSRecordTagsMap: S.Schema<AccountTagsGetResultDNSRecordTagsMap>;
export type AccountTagsGetResultDNSRecordType = "dns_record";
export declare const AccountTagsGetResultDNSRecordType: any;
export interface AccountTagsGetResultDNSRecord {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultDNSRecordTagsMap;
    type: AccountTagsGetResultDNSRecordType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultDNSRecord: S.Schema<AccountTagsGetResultDNSRecord>;
export type AccountTagsGetResultDurableObjectNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultDurableObjectNamespaceTagsMap: S.Schema<AccountTagsGetResultDurableObjectNamespaceTagsMap>;
export type AccountTagsGetResultDurableObjectNamespaceType = "durable_object_namespace";
export declare const AccountTagsGetResultDurableObjectNamespaceType: any;
export interface AccountTagsGetResultDurableObjectNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultDurableObjectNamespaceTagsMap;
    type: AccountTagsGetResultDurableObjectNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultDurableObjectNamespace: S.Schema<AccountTagsGetResultDurableObjectNamespace>;
export type AccountTagsGetResultGatewayListTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultGatewayListTagsMap: S.Schema<AccountTagsGetResultGatewayListTagsMap>;
export type AccountTagsGetResultGatewayListType = "gateway_list";
export declare const AccountTagsGetResultGatewayListType: any;
export interface AccountTagsGetResultGatewayList {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultGatewayListTagsMap;
    type: AccountTagsGetResultGatewayListType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultGatewayList: S.Schema<AccountTagsGetResultGatewayList>;
export type AccountTagsGetResultGatewayRuleTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultGatewayRuleTagsMap: S.Schema<AccountTagsGetResultGatewayRuleTagsMap>;
export type AccountTagsGetResultGatewayRuleType = "gateway_rule";
export declare const AccountTagsGetResultGatewayRuleType: any;
export interface AccountTagsGetResultGatewayRule {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultGatewayRuleTagsMap;
    type: AccountTagsGetResultGatewayRuleType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultGatewayRule: S.Schema<AccountTagsGetResultGatewayRule>;
export type AccountTagsGetResultHealthcheckTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultHealthcheckTagsMap: S.Schema<AccountTagsGetResultHealthcheckTagsMap>;
export type AccountTagsGetResultHealthcheckType = "healthcheck";
export declare const AccountTagsGetResultHealthcheckType: any;
export interface AccountTagsGetResultHealthcheck {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultHealthcheckTagsMap;
    type: AccountTagsGetResultHealthcheckType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultHealthcheck: S.Schema<AccountTagsGetResultHealthcheck>;
export type AccountTagsGetResultImageTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultImageTagsMap: S.Schema<AccountTagsGetResultImageTagsMap>;
export type AccountTagsGetResultImageType = "image";
export declare const AccountTagsGetResultImageType: any;
export interface AccountTagsGetResultImage {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultImageTagsMap;
    type: AccountTagsGetResultImageType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultImage: S.Schema<AccountTagsGetResultImage>;
export type AccountTagsGetResultInfrastructureTargetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultInfrastructureTargetTagsMap: S.Schema<AccountTagsGetResultInfrastructureTargetTagsMap>;
export type AccountTagsGetResultInfrastructureTargetType = "infrastructure_target";
export declare const AccountTagsGetResultInfrastructureTargetType: any;
export interface AccountTagsGetResultInfrastructureTarget {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultInfrastructureTargetTagsMap;
    type: AccountTagsGetResultInfrastructureTargetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultInfrastructureTarget: S.Schema<AccountTagsGetResultInfrastructureTarget>;
export type AccountTagsGetResultKVNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultKVNamespaceTagsMap: S.Schema<AccountTagsGetResultKVNamespaceTagsMap>;
export type AccountTagsGetResultKVNamespaceType = "kv_namespace";
export declare const AccountTagsGetResultKVNamespaceType: any;
export interface AccountTagsGetResultKVNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultKVNamespaceTagsMap;
    type: AccountTagsGetResultKVNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultKVNamespace: S.Schema<AccountTagsGetResultKVNamespace>;
export type AccountTagsGetResultLoadBalancerTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultLoadBalancerTagsMap: S.Schema<AccountTagsGetResultLoadBalancerTagsMap>;
export type AccountTagsGetResultLoadBalancerType = "load_balancer";
export declare const AccountTagsGetResultLoadBalancerType: any;
export interface AccountTagsGetResultLoadBalancer {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultLoadBalancerTagsMap;
    type: AccountTagsGetResultLoadBalancerType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultLoadBalancer: S.Schema<AccountTagsGetResultLoadBalancer>;
export type AccountTagsGetResultLoadBalancerMonitorTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultLoadBalancerMonitorTagsMap: S.Schema<AccountTagsGetResultLoadBalancerMonitorTagsMap>;
export type AccountTagsGetResultLoadBalancerMonitorType = "load_balancer_monitor";
export declare const AccountTagsGetResultLoadBalancerMonitorType: any;
export interface AccountTagsGetResultLoadBalancerMonitor {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultLoadBalancerMonitorTagsMap;
    type: AccountTagsGetResultLoadBalancerMonitorType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultLoadBalancerMonitor: S.Schema<AccountTagsGetResultLoadBalancerMonitor>;
export type AccountTagsGetResultLoadBalancerPoolTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultLoadBalancerPoolTagsMap: S.Schema<AccountTagsGetResultLoadBalancerPoolTagsMap>;
export type AccountTagsGetResultLoadBalancerPoolType = "load_balancer_pool";
export declare const AccountTagsGetResultLoadBalancerPoolType: any;
export interface AccountTagsGetResultLoadBalancerPool {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultLoadBalancerPoolTagsMap;
    type: AccountTagsGetResultLoadBalancerPoolType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultLoadBalancerPool: S.Schema<AccountTagsGetResultLoadBalancerPool>;
export type AccountTagsGetResultManagedClientCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultManagedClientCertificateTagsMap: S.Schema<AccountTagsGetResultManagedClientCertificateTagsMap>;
export type AccountTagsGetResultManagedClientCertificateType = "managed_client_certificate";
export declare const AccountTagsGetResultManagedClientCertificateType: any;
export interface AccountTagsGetResultManagedClientCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultManagedClientCertificateTagsMap;
    type: AccountTagsGetResultManagedClientCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultManagedClientCertificate: S.Schema<AccountTagsGetResultManagedClientCertificate>;
export type AccountTagsGetResultPagesProjectTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultPagesProjectTagsMap: S.Schema<AccountTagsGetResultPagesProjectTagsMap>;
export type AccountTagsGetResultPagesProjectType = "pages_project";
export declare const AccountTagsGetResultPagesProjectType: any;
export interface AccountTagsGetResultPagesProject {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultPagesProjectTagsMap;
    type: AccountTagsGetResultPagesProjectType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultPagesProject: S.Schema<AccountTagsGetResultPagesProject>;
export type AccountTagsGetResultQueueTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultQueueTagsMap: S.Schema<AccountTagsGetResultQueueTagsMap>;
export type AccountTagsGetResultQueueType = "queue";
export declare const AccountTagsGetResultQueueType: any;
export interface AccountTagsGetResultQueue {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultQueueTagsMap;
    type: AccountTagsGetResultQueueType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultQueue: S.Schema<AccountTagsGetResultQueue>;
export type AccountTagsGetResultR2BucketTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultR2BucketTagsMap: S.Schema<AccountTagsGetResultR2BucketTagsMap>;
export type AccountTagsGetResultR2BucketType = "r2_bucket";
export declare const AccountTagsGetResultR2BucketType: any;
export interface AccountTagsGetResultR2Bucket {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultR2BucketTagsMap;
    type: AccountTagsGetResultR2BucketType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultR2Bucket: S.Schema<AccountTagsGetResultR2Bucket>;
export type AccountTagsGetResultResourceShareTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultResourceShareTagsMap: S.Schema<AccountTagsGetResultResourceShareTagsMap>;
export type AccountTagsGetResultResourceShareType = "resource_share";
export declare const AccountTagsGetResultResourceShareType: any;
export interface AccountTagsGetResultResourceShare {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultResourceShareTagsMap;
    type: AccountTagsGetResultResourceShareType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultResourceShare: S.Schema<AccountTagsGetResultResourceShare>;
export type AccountTagsGetResultStreamLiveInputTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultStreamLiveInputTagsMap: S.Schema<AccountTagsGetResultStreamLiveInputTagsMap>;
export type AccountTagsGetResultStreamLiveInputType = "stream_live_input";
export declare const AccountTagsGetResultStreamLiveInputType: any;
export interface AccountTagsGetResultStreamLiveInput {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultStreamLiveInputTagsMap;
    type: AccountTagsGetResultStreamLiveInputType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultStreamLiveInput: S.Schema<AccountTagsGetResultStreamLiveInput>;
export type AccountTagsGetResultStreamVideoTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultStreamVideoTagsMap: S.Schema<AccountTagsGetResultStreamVideoTagsMap>;
export type AccountTagsGetResultStreamVideoType = "stream_video";
export declare const AccountTagsGetResultStreamVideoType: any;
export interface AccountTagsGetResultStreamVideo {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultStreamVideoTagsMap;
    type: AccountTagsGetResultStreamVideoType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultStreamVideo: S.Schema<AccountTagsGetResultStreamVideo>;
export type AccountTagsGetResultVectorizeIndexTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultVectorizeIndexTagsMap: S.Schema<AccountTagsGetResultVectorizeIndexTagsMap>;
export type AccountTagsGetResultVectorizeIndexType = "vectorize_index";
export declare const AccountTagsGetResultVectorizeIndexType: any;
export interface AccountTagsGetResultVectorizeIndex {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultVectorizeIndexTagsMap;
    type: AccountTagsGetResultVectorizeIndexType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultVectorizeIndex: S.Schema<AccountTagsGetResultVectorizeIndex>;
export type AccountTagsGetResultWorkerTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultWorkerTagsMap: S.Schema<AccountTagsGetResultWorkerTagsMap>;
export type AccountTagsGetResultWorkerType = "worker";
export declare const AccountTagsGetResultWorkerType: any;
export interface AccountTagsGetResultWorker {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultWorkerTagsMap;
    type: AccountTagsGetResultWorkerType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultWorker: S.Schema<AccountTagsGetResultWorker>;
export type AccountTagsGetResultWorkerRouteTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultWorkerRouteTagsMap: S.Schema<AccountTagsGetResultWorkerRouteTagsMap>;
export type AccountTagsGetResultWorkerRouteType = "worker_route";
export declare const AccountTagsGetResultWorkerRouteType: any;
export interface AccountTagsGetResultWorkerRoute {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultWorkerRouteTagsMap;
    type: AccountTagsGetResultWorkerRouteType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultWorkerRoute: S.Schema<AccountTagsGetResultWorkerRoute>;
export type AccountTagsGetResultWorkerVersionTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultWorkerVersionTagsMap: S.Schema<AccountTagsGetResultWorkerVersionTagsMap>;
export type AccountTagsGetResultWorkerVersionType = "worker_version";
export declare const AccountTagsGetResultWorkerVersionType: any;
export interface AccountTagsGetResultWorkerVersion {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultWorkerVersionTagsMap;
    type: AccountTagsGetResultWorkerVersionType;
    /** Worker ID is required only for worker_version resources */
    workerId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultWorkerVersion: S.Schema<AccountTagsGetResultWorkerVersion>;
export type AccountTagsGetResultZoneTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultZoneTagsMap: S.Schema<AccountTagsGetResultZoneTagsMap>;
export type AccountTagsGetResultZoneType = "zone";
export declare const AccountTagsGetResultZoneType: any;
export interface AccountTagsGetResultZone {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultZoneTagsMap;
    type: AccountTagsGetResultZoneType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultZone: S.Schema<AccountTagsGetResultZone>;
export type AccountTagsGetResultZoneRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsGetResultZoneRulesetTagsMap: S.Schema<AccountTagsGetResultZoneRulesetTagsMap>;
export type AccountTagsGetResultZoneRulesetType = "zone_ruleset";
export declare const AccountTagsGetResultZoneRulesetType: any;
export interface AccountTagsGetResultZoneRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsGetResultZoneRulesetTagsMap;
    type: AccountTagsGetResultZoneRulesetType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsGetResultZoneRuleset: S.Schema<AccountTagsGetResultZoneRuleset>;
export type AccountTagsGetResult = AccountTagsGetResultAccessApplication | AccountTagsGetResultAccessApplicationPolicy | AccountTagsGetResultAccessGroup | AccountTagsGetResultAccount | AccountTagsGetResultAccountRuleset | AccountTagsGetResultAIGateway | AccountTagsGetResultAlertingPolicy | AccountTagsGetResultAlertingWebhook | AccountTagsGetResultAPIGatewayOperation | AccountTagsGetResultCloudflaredTunnel | AccountTagsGetResultCustomCertificate | AccountTagsGetResultCustomHostname | AccountTagsGetResultCwsDeployment | AccountTagsGetResultCwsPolicy | AccountTagsGetResultCwsPolicySet | AccountTagsGetResultCwsWorkload | AccountTagsGetResultD1Database | AccountTagsGetResultDNSRecord | AccountTagsGetResultDurableObjectNamespace | AccountTagsGetResultGatewayList | AccountTagsGetResultGatewayRule | AccountTagsGetResultHealthcheck | AccountTagsGetResultImage | AccountTagsGetResultInfrastructureTarget | AccountTagsGetResultKVNamespace | AccountTagsGetResultLoadBalancer | AccountTagsGetResultLoadBalancerMonitor | AccountTagsGetResultLoadBalancerPool | AccountTagsGetResultManagedClientCertificate | AccountTagsGetResultPagesProject | AccountTagsGetResultQueue | AccountTagsGetResultR2Bucket | AccountTagsGetResultResourceShare | AccountTagsGetResultStreamLiveInput | AccountTagsGetResultStreamVideo | AccountTagsGetResultVectorizeIndex | AccountTagsGetResultWorker | AccountTagsGetResultWorkerRoute | AccountTagsGetResultWorkerVersion | AccountTagsGetResultZone | AccountTagsGetResultZoneRuleset;
export declare const AccountTagsGetResult: any;
export type GetAccountTagResponse = AccountTagsGetResult;
export declare const GetAccountTagResponse: S.Schema<GetAccountTagResponse>;
export interface GetSummaryRequest {
    /** Identifier. */
    accountId: string;
    /** Cursor for pagination. */
    cursor?: string;
}
export declare const GetSummaryRequest: S.Schema<GetSummaryRequest>;
export type GetSummaryResultItemValuesList = Array<string>;
export declare const GetSummaryResultItemValuesList: S.Schema<GetSummaryResultItemValuesList>;
export interface GetSummaryResultItem {
    /** A tag key. */
    key: string;
    /** All distinct values for this tag key. */
    values: GetSummaryResultItemValuesList;
}
export declare const GetSummaryResultItem: S.Schema<GetSummaryResultItem>;
export type GetSummaryResultList = Array<GetSummaryResultItem>;
export declare const GetSummaryResultList: S.Schema<GetSummaryResultList>;
export type GetSummaryResponse = GetSummaryResultList;
export declare const GetSummaryResponse: S.Schema<GetSummaryResponse>;
export type ZoneTagsGetRequestResourceType = "access_application_policy" | "api_gateway_operation" | "custom_certificate" | "custom_hostname" | "dns_record" | "healthcheck" | "load_balancer" | "managed_client_certificate" | "worker_route" | "zone" | "zone_ruleset";
export declare const ZoneTagsGetRequestResourceType: any;
export interface GetZoneTagRequest {
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** The ID of the resource to retrieve tags for. */
    resourceId: string;
    /** The type of the resource. */
    resourceType: ZoneTagsGetRequestResourceType | (string & {});
    /** Access application ID identifier. Required for access_application_policy resources. */
    accessApplicationId?: string;
}
export declare const GetZoneTagRequest: S.Schema<GetZoneTagRequest>;
export type ZoneTagsGetResultAccessApplicationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAccessApplicationTagsMap: S.Schema<ZoneTagsGetResultAccessApplicationTagsMap>;
export type ZoneTagsGetResultAccessApplicationType = "access_application";
export declare const ZoneTagsGetResultAccessApplicationType: any;
export interface ZoneTagsGetResultAccessApplication {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAccessApplicationTagsMap;
    type: ZoneTagsGetResultAccessApplicationType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAccessApplication: S.Schema<ZoneTagsGetResultAccessApplication>;
export type ZoneTagsGetResultAccessApplicationPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAccessApplicationPolicyTagsMap: S.Schema<ZoneTagsGetResultAccessApplicationPolicyTagsMap>;
export type ZoneTagsGetResultAccessApplicationPolicyType = "access_application_policy";
export declare const ZoneTagsGetResultAccessApplicationPolicyType: any;
export interface ZoneTagsGetResultAccessApplicationPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAccessApplicationPolicyTagsMap;
    type: ZoneTagsGetResultAccessApplicationPolicyType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAccessApplicationPolicy: S.Schema<ZoneTagsGetResultAccessApplicationPolicy>;
export type ZoneTagsGetResultAccessGroupTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAccessGroupTagsMap: S.Schema<ZoneTagsGetResultAccessGroupTagsMap>;
export type ZoneTagsGetResultAccessGroupType = "access_group";
export declare const ZoneTagsGetResultAccessGroupType: any;
export interface ZoneTagsGetResultAccessGroup {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAccessGroupTagsMap;
    type: ZoneTagsGetResultAccessGroupType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAccessGroup: S.Schema<ZoneTagsGetResultAccessGroup>;
export type ZoneTagsGetResultAccountTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAccountTagsMap: S.Schema<ZoneTagsGetResultAccountTagsMap>;
export type ZoneTagsGetResultAccountType = "account";
export declare const ZoneTagsGetResultAccountType: any;
export interface ZoneTagsGetResultAccount {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAccountTagsMap;
    type: ZoneTagsGetResultAccountType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAccount: S.Schema<ZoneTagsGetResultAccount>;
export type ZoneTagsGetResultAccountRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAccountRulesetTagsMap: S.Schema<ZoneTagsGetResultAccountRulesetTagsMap>;
export type ZoneTagsGetResultAccountRulesetType = "account_ruleset";
export declare const ZoneTagsGetResultAccountRulesetType: any;
export interface ZoneTagsGetResultAccountRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAccountRulesetTagsMap;
    type: ZoneTagsGetResultAccountRulesetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAccountRuleset: S.Schema<ZoneTagsGetResultAccountRuleset>;
export type ZoneTagsGetResultAIGatewayTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAIGatewayTagsMap: S.Schema<ZoneTagsGetResultAIGatewayTagsMap>;
export type ZoneTagsGetResultAIGatewayType = "ai_gateway";
export declare const ZoneTagsGetResultAIGatewayType: any;
export interface ZoneTagsGetResultAIGateway {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAIGatewayTagsMap;
    type: ZoneTagsGetResultAIGatewayType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAIGateway: S.Schema<ZoneTagsGetResultAIGateway>;
export type ZoneTagsGetResultAlertingPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAlertingPolicyTagsMap: S.Schema<ZoneTagsGetResultAlertingPolicyTagsMap>;
export type ZoneTagsGetResultAlertingPolicyType = "alerting_policy";
export declare const ZoneTagsGetResultAlertingPolicyType: any;
export interface ZoneTagsGetResultAlertingPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAlertingPolicyTagsMap;
    type: ZoneTagsGetResultAlertingPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAlertingPolicy: S.Schema<ZoneTagsGetResultAlertingPolicy>;
export type ZoneTagsGetResultAlertingWebhookTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAlertingWebhookTagsMap: S.Schema<ZoneTagsGetResultAlertingWebhookTagsMap>;
export type ZoneTagsGetResultAlertingWebhookType = "alerting_webhook";
export declare const ZoneTagsGetResultAlertingWebhookType: any;
export interface ZoneTagsGetResultAlertingWebhook {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAlertingWebhookTagsMap;
    type: ZoneTagsGetResultAlertingWebhookType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAlertingWebhook: S.Schema<ZoneTagsGetResultAlertingWebhook>;
export type ZoneTagsGetResultAPIGatewayOperationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultAPIGatewayOperationTagsMap: S.Schema<ZoneTagsGetResultAPIGatewayOperationTagsMap>;
export type ZoneTagsGetResultAPIGatewayOperationType = "api_gateway_operation";
export declare const ZoneTagsGetResultAPIGatewayOperationType: any;
export interface ZoneTagsGetResultAPIGatewayOperation {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultAPIGatewayOperationTagsMap;
    type: ZoneTagsGetResultAPIGatewayOperationType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultAPIGatewayOperation: S.Schema<ZoneTagsGetResultAPIGatewayOperation>;
export type ZoneTagsGetResultCloudflaredTunnelTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCloudflaredTunnelTagsMap: S.Schema<ZoneTagsGetResultCloudflaredTunnelTagsMap>;
export type ZoneTagsGetResultCloudflaredTunnelType = "cloudflared_tunnel";
export declare const ZoneTagsGetResultCloudflaredTunnelType: any;
export interface ZoneTagsGetResultCloudflaredTunnel {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCloudflaredTunnelTagsMap;
    type: ZoneTagsGetResultCloudflaredTunnelType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCloudflaredTunnel: S.Schema<ZoneTagsGetResultCloudflaredTunnel>;
export type ZoneTagsGetResultCustomCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCustomCertificateTagsMap: S.Schema<ZoneTagsGetResultCustomCertificateTagsMap>;
export type ZoneTagsGetResultCustomCertificateType = "custom_certificate";
export declare const ZoneTagsGetResultCustomCertificateType: any;
export interface ZoneTagsGetResultCustomCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCustomCertificateTagsMap;
    type: ZoneTagsGetResultCustomCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCustomCertificate: S.Schema<ZoneTagsGetResultCustomCertificate>;
export type ZoneTagsGetResultCustomHostnameTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCustomHostnameTagsMap: S.Schema<ZoneTagsGetResultCustomHostnameTagsMap>;
export type ZoneTagsGetResultCustomHostnameType = "custom_hostname";
export declare const ZoneTagsGetResultCustomHostnameType: any;
export interface ZoneTagsGetResultCustomHostname {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCustomHostnameTagsMap;
    type: ZoneTagsGetResultCustomHostnameType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCustomHostname: S.Schema<ZoneTagsGetResultCustomHostname>;
export type ZoneTagsGetResultCwsDeploymentTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCwsDeploymentTagsMap: S.Schema<ZoneTagsGetResultCwsDeploymentTagsMap>;
export type ZoneTagsGetResultCwsDeploymentType = "cws_deployment";
export declare const ZoneTagsGetResultCwsDeploymentType: any;
export interface ZoneTagsGetResultCwsDeployment {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCwsDeploymentTagsMap;
    type: ZoneTagsGetResultCwsDeploymentType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCwsDeployment: S.Schema<ZoneTagsGetResultCwsDeployment>;
export type ZoneTagsGetResultCwsPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCwsPolicyTagsMap: S.Schema<ZoneTagsGetResultCwsPolicyTagsMap>;
export type ZoneTagsGetResultCwsPolicyType = "cws_policy";
export declare const ZoneTagsGetResultCwsPolicyType: any;
export interface ZoneTagsGetResultCwsPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCwsPolicyTagsMap;
    type: ZoneTagsGetResultCwsPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCwsPolicy: S.Schema<ZoneTagsGetResultCwsPolicy>;
export type ZoneTagsGetResultCwsPolicySetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCwsPolicySetTagsMap: S.Schema<ZoneTagsGetResultCwsPolicySetTagsMap>;
export type ZoneTagsGetResultCwsPolicySetType = "cws_policy_set";
export declare const ZoneTagsGetResultCwsPolicySetType: any;
export interface ZoneTagsGetResultCwsPolicySet {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCwsPolicySetTagsMap;
    type: ZoneTagsGetResultCwsPolicySetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCwsPolicySet: S.Schema<ZoneTagsGetResultCwsPolicySet>;
export type ZoneTagsGetResultCwsWorkloadTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultCwsWorkloadTagsMap: S.Schema<ZoneTagsGetResultCwsWorkloadTagsMap>;
export type ZoneTagsGetResultCwsWorkloadType = "cws_workload";
export declare const ZoneTagsGetResultCwsWorkloadType: any;
export interface ZoneTagsGetResultCwsWorkload {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultCwsWorkloadTagsMap;
    type: ZoneTagsGetResultCwsWorkloadType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultCwsWorkload: S.Schema<ZoneTagsGetResultCwsWorkload>;
export type ZoneTagsGetResultD1DatabaseTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultD1DatabaseTagsMap: S.Schema<ZoneTagsGetResultD1DatabaseTagsMap>;
export type ZoneTagsGetResultD1DatabaseType = "d1_database";
export declare const ZoneTagsGetResultD1DatabaseType: any;
export interface ZoneTagsGetResultD1Database {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultD1DatabaseTagsMap;
    type: ZoneTagsGetResultD1DatabaseType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultD1Database: S.Schema<ZoneTagsGetResultD1Database>;
export type ZoneTagsGetResultDNSRecordTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultDNSRecordTagsMap: S.Schema<ZoneTagsGetResultDNSRecordTagsMap>;
export type ZoneTagsGetResultDNSRecordType = "dns_record";
export declare const ZoneTagsGetResultDNSRecordType: any;
export interface ZoneTagsGetResultDNSRecord {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultDNSRecordTagsMap;
    type: ZoneTagsGetResultDNSRecordType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultDNSRecord: S.Schema<ZoneTagsGetResultDNSRecord>;
export type ZoneTagsGetResultDurableObjectNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultDurableObjectNamespaceTagsMap: S.Schema<ZoneTagsGetResultDurableObjectNamespaceTagsMap>;
export type ZoneTagsGetResultDurableObjectNamespaceType = "durable_object_namespace";
export declare const ZoneTagsGetResultDurableObjectNamespaceType: any;
export interface ZoneTagsGetResultDurableObjectNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultDurableObjectNamespaceTagsMap;
    type: ZoneTagsGetResultDurableObjectNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultDurableObjectNamespace: S.Schema<ZoneTagsGetResultDurableObjectNamespace>;
export type ZoneTagsGetResultGatewayListTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultGatewayListTagsMap: S.Schema<ZoneTagsGetResultGatewayListTagsMap>;
export type ZoneTagsGetResultGatewayListType = "gateway_list";
export declare const ZoneTagsGetResultGatewayListType: any;
export interface ZoneTagsGetResultGatewayList {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultGatewayListTagsMap;
    type: ZoneTagsGetResultGatewayListType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultGatewayList: S.Schema<ZoneTagsGetResultGatewayList>;
export type ZoneTagsGetResultGatewayRuleTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultGatewayRuleTagsMap: S.Schema<ZoneTagsGetResultGatewayRuleTagsMap>;
export type ZoneTagsGetResultGatewayRuleType = "gateway_rule";
export declare const ZoneTagsGetResultGatewayRuleType: any;
export interface ZoneTagsGetResultGatewayRule {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultGatewayRuleTagsMap;
    type: ZoneTagsGetResultGatewayRuleType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultGatewayRule: S.Schema<ZoneTagsGetResultGatewayRule>;
export type ZoneTagsGetResultHealthcheckTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultHealthcheckTagsMap: S.Schema<ZoneTagsGetResultHealthcheckTagsMap>;
export type ZoneTagsGetResultHealthcheckType = "healthcheck";
export declare const ZoneTagsGetResultHealthcheckType: any;
export interface ZoneTagsGetResultHealthcheck {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultHealthcheckTagsMap;
    type: ZoneTagsGetResultHealthcheckType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultHealthcheck: S.Schema<ZoneTagsGetResultHealthcheck>;
export type ZoneTagsGetResultImageTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultImageTagsMap: S.Schema<ZoneTagsGetResultImageTagsMap>;
export type ZoneTagsGetResultImageType = "image";
export declare const ZoneTagsGetResultImageType: any;
export interface ZoneTagsGetResultImage {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultImageTagsMap;
    type: ZoneTagsGetResultImageType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultImage: S.Schema<ZoneTagsGetResultImage>;
export type ZoneTagsGetResultInfrastructureTargetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultInfrastructureTargetTagsMap: S.Schema<ZoneTagsGetResultInfrastructureTargetTagsMap>;
export type ZoneTagsGetResultInfrastructureTargetType = "infrastructure_target";
export declare const ZoneTagsGetResultInfrastructureTargetType: any;
export interface ZoneTagsGetResultInfrastructureTarget {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultInfrastructureTargetTagsMap;
    type: ZoneTagsGetResultInfrastructureTargetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultInfrastructureTarget: S.Schema<ZoneTagsGetResultInfrastructureTarget>;
export type ZoneTagsGetResultKVNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultKVNamespaceTagsMap: S.Schema<ZoneTagsGetResultKVNamespaceTagsMap>;
export type ZoneTagsGetResultKVNamespaceType = "kv_namespace";
export declare const ZoneTagsGetResultKVNamespaceType: any;
export interface ZoneTagsGetResultKVNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultKVNamespaceTagsMap;
    type: ZoneTagsGetResultKVNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultKVNamespace: S.Schema<ZoneTagsGetResultKVNamespace>;
export type ZoneTagsGetResultLoadBalancerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultLoadBalancerTagsMap: S.Schema<ZoneTagsGetResultLoadBalancerTagsMap>;
export type ZoneTagsGetResultLoadBalancerType = "load_balancer";
export declare const ZoneTagsGetResultLoadBalancerType: any;
export interface ZoneTagsGetResultLoadBalancer {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultLoadBalancerTagsMap;
    type: ZoneTagsGetResultLoadBalancerType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultLoadBalancer: S.Schema<ZoneTagsGetResultLoadBalancer>;
export type ZoneTagsGetResultLoadBalancerMonitorTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultLoadBalancerMonitorTagsMap: S.Schema<ZoneTagsGetResultLoadBalancerMonitorTagsMap>;
export type ZoneTagsGetResultLoadBalancerMonitorType = "load_balancer_monitor";
export declare const ZoneTagsGetResultLoadBalancerMonitorType: any;
export interface ZoneTagsGetResultLoadBalancerMonitor {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultLoadBalancerMonitorTagsMap;
    type: ZoneTagsGetResultLoadBalancerMonitorType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultLoadBalancerMonitor: S.Schema<ZoneTagsGetResultLoadBalancerMonitor>;
export type ZoneTagsGetResultLoadBalancerPoolTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultLoadBalancerPoolTagsMap: S.Schema<ZoneTagsGetResultLoadBalancerPoolTagsMap>;
export type ZoneTagsGetResultLoadBalancerPoolType = "load_balancer_pool";
export declare const ZoneTagsGetResultLoadBalancerPoolType: any;
export interface ZoneTagsGetResultLoadBalancerPool {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultLoadBalancerPoolTagsMap;
    type: ZoneTagsGetResultLoadBalancerPoolType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultLoadBalancerPool: S.Schema<ZoneTagsGetResultLoadBalancerPool>;
export type ZoneTagsGetResultManagedClientCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultManagedClientCertificateTagsMap: S.Schema<ZoneTagsGetResultManagedClientCertificateTagsMap>;
export type ZoneTagsGetResultManagedClientCertificateType = "managed_client_certificate";
export declare const ZoneTagsGetResultManagedClientCertificateType: any;
export interface ZoneTagsGetResultManagedClientCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultManagedClientCertificateTagsMap;
    type: ZoneTagsGetResultManagedClientCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultManagedClientCertificate: S.Schema<ZoneTagsGetResultManagedClientCertificate>;
export type ZoneTagsGetResultPagesProjectTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultPagesProjectTagsMap: S.Schema<ZoneTagsGetResultPagesProjectTagsMap>;
export type ZoneTagsGetResultPagesProjectType = "pages_project";
export declare const ZoneTagsGetResultPagesProjectType: any;
export interface ZoneTagsGetResultPagesProject {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultPagesProjectTagsMap;
    type: ZoneTagsGetResultPagesProjectType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultPagesProject: S.Schema<ZoneTagsGetResultPagesProject>;
export type ZoneTagsGetResultQueueTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultQueueTagsMap: S.Schema<ZoneTagsGetResultQueueTagsMap>;
export type ZoneTagsGetResultQueueType = "queue";
export declare const ZoneTagsGetResultQueueType: any;
export interface ZoneTagsGetResultQueue {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultQueueTagsMap;
    type: ZoneTagsGetResultQueueType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultQueue: S.Schema<ZoneTagsGetResultQueue>;
export type ZoneTagsGetResultR2BucketTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultR2BucketTagsMap: S.Schema<ZoneTagsGetResultR2BucketTagsMap>;
export type ZoneTagsGetResultR2BucketType = "r2_bucket";
export declare const ZoneTagsGetResultR2BucketType: any;
export interface ZoneTagsGetResultR2Bucket {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultR2BucketTagsMap;
    type: ZoneTagsGetResultR2BucketType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultR2Bucket: S.Schema<ZoneTagsGetResultR2Bucket>;
export type ZoneTagsGetResultResourceShareTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultResourceShareTagsMap: S.Schema<ZoneTagsGetResultResourceShareTagsMap>;
export type ZoneTagsGetResultResourceShareType = "resource_share";
export declare const ZoneTagsGetResultResourceShareType: any;
export interface ZoneTagsGetResultResourceShare {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultResourceShareTagsMap;
    type: ZoneTagsGetResultResourceShareType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultResourceShare: S.Schema<ZoneTagsGetResultResourceShare>;
export type ZoneTagsGetResultStreamLiveInputTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultStreamLiveInputTagsMap: S.Schema<ZoneTagsGetResultStreamLiveInputTagsMap>;
export type ZoneTagsGetResultStreamLiveInputType = "stream_live_input";
export declare const ZoneTagsGetResultStreamLiveInputType: any;
export interface ZoneTagsGetResultStreamLiveInput {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultStreamLiveInputTagsMap;
    type: ZoneTagsGetResultStreamLiveInputType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultStreamLiveInput: S.Schema<ZoneTagsGetResultStreamLiveInput>;
export type ZoneTagsGetResultStreamVideoTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultStreamVideoTagsMap: S.Schema<ZoneTagsGetResultStreamVideoTagsMap>;
export type ZoneTagsGetResultStreamVideoType = "stream_video";
export declare const ZoneTagsGetResultStreamVideoType: any;
export interface ZoneTagsGetResultStreamVideo {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultStreamVideoTagsMap;
    type: ZoneTagsGetResultStreamVideoType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultStreamVideo: S.Schema<ZoneTagsGetResultStreamVideo>;
export type ZoneTagsGetResultVectorizeIndexTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultVectorizeIndexTagsMap: S.Schema<ZoneTagsGetResultVectorizeIndexTagsMap>;
export type ZoneTagsGetResultVectorizeIndexType = "vectorize_index";
export declare const ZoneTagsGetResultVectorizeIndexType: any;
export interface ZoneTagsGetResultVectorizeIndex {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultVectorizeIndexTagsMap;
    type: ZoneTagsGetResultVectorizeIndexType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultVectorizeIndex: S.Schema<ZoneTagsGetResultVectorizeIndex>;
export type ZoneTagsGetResultWorkerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultWorkerTagsMap: S.Schema<ZoneTagsGetResultWorkerTagsMap>;
export type ZoneTagsGetResultWorkerType = "worker";
export declare const ZoneTagsGetResultWorkerType: any;
export interface ZoneTagsGetResultWorker {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultWorkerTagsMap;
    type: ZoneTagsGetResultWorkerType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultWorker: S.Schema<ZoneTagsGetResultWorker>;
export type ZoneTagsGetResultWorkerRouteTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultWorkerRouteTagsMap: S.Schema<ZoneTagsGetResultWorkerRouteTagsMap>;
export type ZoneTagsGetResultWorkerRouteType = "worker_route";
export declare const ZoneTagsGetResultWorkerRouteType: any;
export interface ZoneTagsGetResultWorkerRoute {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultWorkerRouteTagsMap;
    type: ZoneTagsGetResultWorkerRouteType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultWorkerRoute: S.Schema<ZoneTagsGetResultWorkerRoute>;
export type ZoneTagsGetResultWorkerVersionTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultWorkerVersionTagsMap: S.Schema<ZoneTagsGetResultWorkerVersionTagsMap>;
export type ZoneTagsGetResultWorkerVersionType = "worker_version";
export declare const ZoneTagsGetResultWorkerVersionType: any;
export interface ZoneTagsGetResultWorkerVersion {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultWorkerVersionTagsMap;
    type: ZoneTagsGetResultWorkerVersionType;
    /** Worker ID is required only for worker_version resources */
    workerId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultWorkerVersion: S.Schema<ZoneTagsGetResultWorkerVersion>;
export type ZoneTagsGetResultZoneTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultZoneTagsMap: S.Schema<ZoneTagsGetResultZoneTagsMap>;
export type ZoneTagsGetResultZoneType = "zone";
export declare const ZoneTagsGetResultZoneType: any;
export interface ZoneTagsGetResultZone {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultZoneTagsMap;
    type: ZoneTagsGetResultZoneType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultZone: S.Schema<ZoneTagsGetResultZone>;
export type ZoneTagsGetResultZoneRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsGetResultZoneRulesetTagsMap: S.Schema<ZoneTagsGetResultZoneRulesetTagsMap>;
export type ZoneTagsGetResultZoneRulesetType = "zone_ruleset";
export declare const ZoneTagsGetResultZoneRulesetType: any;
export interface ZoneTagsGetResultZoneRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsGetResultZoneRulesetTagsMap;
    type: ZoneTagsGetResultZoneRulesetType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsGetResultZoneRuleset: S.Schema<ZoneTagsGetResultZoneRuleset>;
export type ZoneTagsGetResult = ZoneTagsGetResultAccessApplication | ZoneTagsGetResultAccessApplicationPolicy | ZoneTagsGetResultAccessGroup | ZoneTagsGetResultAccount | ZoneTagsGetResultAccountRuleset | ZoneTagsGetResultAIGateway | ZoneTagsGetResultAlertingPolicy | ZoneTagsGetResultAlertingWebhook | ZoneTagsGetResultAPIGatewayOperation | ZoneTagsGetResultCloudflaredTunnel | ZoneTagsGetResultCustomCertificate | ZoneTagsGetResultCustomHostname | ZoneTagsGetResultCwsDeployment | ZoneTagsGetResultCwsPolicy | ZoneTagsGetResultCwsPolicySet | ZoneTagsGetResultCwsWorkload | ZoneTagsGetResultD1Database | ZoneTagsGetResultDNSRecord | ZoneTagsGetResultDurableObjectNamespace | ZoneTagsGetResultGatewayList | ZoneTagsGetResultGatewayRule | ZoneTagsGetResultHealthcheck | ZoneTagsGetResultImage | ZoneTagsGetResultInfrastructureTarget | ZoneTagsGetResultKVNamespace | ZoneTagsGetResultLoadBalancer | ZoneTagsGetResultLoadBalancerMonitor | ZoneTagsGetResultLoadBalancerPool | ZoneTagsGetResultManagedClientCertificate | ZoneTagsGetResultPagesProject | ZoneTagsGetResultQueue | ZoneTagsGetResultR2Bucket | ZoneTagsGetResultResourceShare | ZoneTagsGetResultStreamLiveInput | ZoneTagsGetResultStreamVideo | ZoneTagsGetResultVectorizeIndex | ZoneTagsGetResultWorker | ZoneTagsGetResultWorkerRoute | ZoneTagsGetResultWorkerVersion | ZoneTagsGetResultZone | ZoneTagsGetResultZoneRuleset;
export declare const ZoneTagsGetResult: any;
export type GetZoneTagResponse = ZoneTagsGetResult;
export declare const GetZoneTagResponse: S.Schema<GetZoneTagResponse>;
export interface ListKeysRequest {
    /** Identifier. */
    accountId: string;
    /** Cursor for pagination. */
    cursor?: string;
}
export declare const ListKeysRequest: S.Schema<ListKeysRequest>;
export type KeysListResultList = Array<string>;
export declare const KeysListResultList: S.Schema<KeysListResultList>;
export interface ListKeysResponse {
    /** The unwrapped `result` payload of the v4 response envelope. */
    result: KeysListResultList;
    /** Pagination info from the envelope's `result_info`. */
    resultInfo?: ResultInfo | null;
}
export declare const ListKeysResponse: S.Schema<ListKeysResponse>;
export type ListRequestIdList = Array<string>;
export declare const ListRequestIdList: S.Schema<ListRequestIdList>;
export type ListRequestTagList = Array<string>;
export declare const ListRequestTagList: S.Schema<ListRequestTagList>;
export type ListRequestType = "access_application" | "access_application_policy" | "access_group" | "account" | "account_ruleset" | "ai_gateway" | "alerting_policy" | "alerting_webhook" | "api_gateway_operation" | "cloudflared_tunnel" | "custom_certificate" | "custom_hostname" | "cws_deployment" | "cws_policy" | "cws_policy_set" | "cws_workload" | "d1_database" | "dns_record" | "durable_object_namespace" | "gateway_list" | "gateway_rule" | "healthcheck" | "image" | "infrastructure_target" | "kv_namespace" | "load_balancer" | "load_balancer_monitor" | "load_balancer_pool" | "managed_client_certificate" | "pages_project" | "queue" | "r2_bucket" | "resource_share" | "stream_live_input" | "stream_video" | "vectorize_index" | "worker" | "worker_route" | "worker_version" | "zone" | "zone_ruleset";
export declare const ListRequestType: any;
export type ListRequestTypeList = Array<ListRequestType | (string & {})>;
export declare const ListRequestTypeList: S.Schema<ListRequestTypeList>;
export interface ListResourceTaggingsRequest {
    /** Identifier. */
    accountId: string;
    /** Filter by resource ID. Can be repeated up to 50 times to filter by multiple IDs. Example: ?id=abc&id=def */
    id?: ListRequestIdList;
    /** Match `tag` keys and values case-insensitively. Stored casing is unchanged. Example: ?tag=environment=production&case_insensitive=true */
    caseInsensitive?: boolean;
    /** Cursor for pagination. */
    cursor?: string;
    /** Filter by resource name. Performs a case-insensitive substring match. Example: ?name=my-zone */
    name?: string;
    /** Filter resources by tag criteria. This parameter can be repeated multiple times, with AND logic between parameters. */
    tag?: ListRequestTagList;
    /** Filter by resource type. Can be repeated to filter by multiple types (OR logic). Example: ?type=zone&amp;type=worker */
    type?: ListRequestTypeList;
}
export declare const ListResourceTaggingsRequest: S.Schema<ListResourceTaggingsRequest>;
export type ListResultItemAccessApplicationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAccessApplicationTagsMap: S.Schema<ListResultItemAccessApplicationTagsMap>;
export type ListResultItemAccessApplicationType = "access_application";
export declare const ListResultItemAccessApplicationType: any;
export interface ListResultItemAccessApplication {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAccessApplicationTagsMap;
    type: ListResultItemAccessApplicationType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAccessApplication: S.Schema<ListResultItemAccessApplication>;
export type ListResultItemAccessApplicationPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAccessApplicationPolicyTagsMap: S.Schema<ListResultItemAccessApplicationPolicyTagsMap>;
export type ListResultItemAccessApplicationPolicyType = "access_application_policy";
export declare const ListResultItemAccessApplicationPolicyType: any;
export interface ListResultItemAccessApplicationPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAccessApplicationPolicyTagsMap;
    type: ListResultItemAccessApplicationPolicyType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAccessApplicationPolicy: S.Schema<ListResultItemAccessApplicationPolicy>;
export type ListResultItemAccessGroupTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAccessGroupTagsMap: S.Schema<ListResultItemAccessGroupTagsMap>;
export type ListResultItemAccessGroupType = "access_group";
export declare const ListResultItemAccessGroupType: any;
export interface ListResultItemAccessGroup {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAccessGroupTagsMap;
    type: ListResultItemAccessGroupType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAccessGroup: S.Schema<ListResultItemAccessGroup>;
export type ListResultItemAccountTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAccountTagsMap: S.Schema<ListResultItemAccountTagsMap>;
export type ListResultItemAccountType = "account";
export declare const ListResultItemAccountType: any;
export interface ListResultItemAccount {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAccountTagsMap;
    type: ListResultItemAccountType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAccount: S.Schema<ListResultItemAccount>;
export type ListResultItemAccountRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAccountRulesetTagsMap: S.Schema<ListResultItemAccountRulesetTagsMap>;
export type ListResultItemAccountRulesetType = "account_ruleset";
export declare const ListResultItemAccountRulesetType: any;
export interface ListResultItemAccountRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAccountRulesetTagsMap;
    type: ListResultItemAccountRulesetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAccountRuleset: S.Schema<ListResultItemAccountRuleset>;
export type ListResultItemAIGatewayTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAIGatewayTagsMap: S.Schema<ListResultItemAIGatewayTagsMap>;
export type ListResultItemAIGatewayType = "ai_gateway";
export declare const ListResultItemAIGatewayType: any;
export interface ListResultItemAIGateway {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAIGatewayTagsMap;
    type: ListResultItemAIGatewayType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAIGateway: S.Schema<ListResultItemAIGateway>;
export type ListResultItemAlertingPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAlertingPolicyTagsMap: S.Schema<ListResultItemAlertingPolicyTagsMap>;
export type ListResultItemAlertingPolicyType = "alerting_policy";
export declare const ListResultItemAlertingPolicyType: any;
export interface ListResultItemAlertingPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAlertingPolicyTagsMap;
    type: ListResultItemAlertingPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAlertingPolicy: S.Schema<ListResultItemAlertingPolicy>;
export type ListResultItemAlertingWebhookTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAlertingWebhookTagsMap: S.Schema<ListResultItemAlertingWebhookTagsMap>;
export type ListResultItemAlertingWebhookType = "alerting_webhook";
export declare const ListResultItemAlertingWebhookType: any;
export interface ListResultItemAlertingWebhook {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAlertingWebhookTagsMap;
    type: ListResultItemAlertingWebhookType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAlertingWebhook: S.Schema<ListResultItemAlertingWebhook>;
export type ListResultItemAPIGatewayOperationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemAPIGatewayOperationTagsMap: S.Schema<ListResultItemAPIGatewayOperationTagsMap>;
export type ListResultItemAPIGatewayOperationType = "api_gateway_operation";
export declare const ListResultItemAPIGatewayOperationType: any;
export interface ListResultItemAPIGatewayOperation {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemAPIGatewayOperationTagsMap;
    type: ListResultItemAPIGatewayOperationType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemAPIGatewayOperation: S.Schema<ListResultItemAPIGatewayOperation>;
export type ListResultItemCloudflaredTunnelTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCloudflaredTunnelTagsMap: S.Schema<ListResultItemCloudflaredTunnelTagsMap>;
export type ListResultItemCloudflaredTunnelType = "cloudflared_tunnel";
export declare const ListResultItemCloudflaredTunnelType: any;
export interface ListResultItemCloudflaredTunnel {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCloudflaredTunnelTagsMap;
    type: ListResultItemCloudflaredTunnelType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCloudflaredTunnel: S.Schema<ListResultItemCloudflaredTunnel>;
export type ListResultItemCustomCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCustomCertificateTagsMap: S.Schema<ListResultItemCustomCertificateTagsMap>;
export type ListResultItemCustomCertificateType = "custom_certificate";
export declare const ListResultItemCustomCertificateType: any;
export interface ListResultItemCustomCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCustomCertificateTagsMap;
    type: ListResultItemCustomCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCustomCertificate: S.Schema<ListResultItemCustomCertificate>;
export type ListResultItemCustomHostnameTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCustomHostnameTagsMap: S.Schema<ListResultItemCustomHostnameTagsMap>;
export type ListResultItemCustomHostnameType = "custom_hostname";
export declare const ListResultItemCustomHostnameType: any;
export interface ListResultItemCustomHostname {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCustomHostnameTagsMap;
    type: ListResultItemCustomHostnameType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCustomHostname: S.Schema<ListResultItemCustomHostname>;
export type ListResultItemCwsDeploymentTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCwsDeploymentTagsMap: S.Schema<ListResultItemCwsDeploymentTagsMap>;
export type ListResultItemCwsDeploymentType = "cws_deployment";
export declare const ListResultItemCwsDeploymentType: any;
export interface ListResultItemCwsDeployment {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCwsDeploymentTagsMap;
    type: ListResultItemCwsDeploymentType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCwsDeployment: S.Schema<ListResultItemCwsDeployment>;
export type ListResultItemCwsPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCwsPolicyTagsMap: S.Schema<ListResultItemCwsPolicyTagsMap>;
export type ListResultItemCwsPolicyType = "cws_policy";
export declare const ListResultItemCwsPolicyType: any;
export interface ListResultItemCwsPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCwsPolicyTagsMap;
    type: ListResultItemCwsPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCwsPolicy: S.Schema<ListResultItemCwsPolicy>;
export type ListResultItemCwsPolicySetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCwsPolicySetTagsMap: S.Schema<ListResultItemCwsPolicySetTagsMap>;
export type ListResultItemCwsPolicySetType = "cws_policy_set";
export declare const ListResultItemCwsPolicySetType: any;
export interface ListResultItemCwsPolicySet {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCwsPolicySetTagsMap;
    type: ListResultItemCwsPolicySetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCwsPolicySet: S.Schema<ListResultItemCwsPolicySet>;
export type ListResultItemCwsWorkloadTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemCwsWorkloadTagsMap: S.Schema<ListResultItemCwsWorkloadTagsMap>;
export type ListResultItemCwsWorkloadType = "cws_workload";
export declare const ListResultItemCwsWorkloadType: any;
export interface ListResultItemCwsWorkload {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemCwsWorkloadTagsMap;
    type: ListResultItemCwsWorkloadType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemCwsWorkload: S.Schema<ListResultItemCwsWorkload>;
export type ListResultItemD1DatabaseTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemD1DatabaseTagsMap: S.Schema<ListResultItemD1DatabaseTagsMap>;
export type ListResultItemD1DatabaseType = "d1_database";
export declare const ListResultItemD1DatabaseType: any;
export interface ListResultItemD1Database {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemD1DatabaseTagsMap;
    type: ListResultItemD1DatabaseType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemD1Database: S.Schema<ListResultItemD1Database>;
export type ListResultItemDNSRecordTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemDNSRecordTagsMap: S.Schema<ListResultItemDNSRecordTagsMap>;
export type ListResultItemDNSRecordType = "dns_record";
export declare const ListResultItemDNSRecordType: any;
export interface ListResultItemDNSRecord {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemDNSRecordTagsMap;
    type: ListResultItemDNSRecordType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemDNSRecord: S.Schema<ListResultItemDNSRecord>;
export type ListResultItemDurableObjectNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemDurableObjectNamespaceTagsMap: S.Schema<ListResultItemDurableObjectNamespaceTagsMap>;
export type ListResultItemDurableObjectNamespaceType = "durable_object_namespace";
export declare const ListResultItemDurableObjectNamespaceType: any;
export interface ListResultItemDurableObjectNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemDurableObjectNamespaceTagsMap;
    type: ListResultItemDurableObjectNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemDurableObjectNamespace: S.Schema<ListResultItemDurableObjectNamespace>;
export type ListResultItemGatewayListTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemGatewayListTagsMap: S.Schema<ListResultItemGatewayListTagsMap>;
export type ListResultItemGatewayListType = "gateway_list";
export declare const ListResultItemGatewayListType: any;
export interface ListResultItemGatewayList {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemGatewayListTagsMap;
    type: ListResultItemGatewayListType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemGatewayList: S.Schema<ListResultItemGatewayList>;
export type ListResultItemGatewayRuleTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemGatewayRuleTagsMap: S.Schema<ListResultItemGatewayRuleTagsMap>;
export type ListResultItemGatewayRuleType = "gateway_rule";
export declare const ListResultItemGatewayRuleType: any;
export interface ListResultItemGatewayRule {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemGatewayRuleTagsMap;
    type: ListResultItemGatewayRuleType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemGatewayRule: S.Schema<ListResultItemGatewayRule>;
export type ListResultItemHealthcheckTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemHealthcheckTagsMap: S.Schema<ListResultItemHealthcheckTagsMap>;
export type ListResultItemHealthcheckType = "healthcheck";
export declare const ListResultItemHealthcheckType: any;
export interface ListResultItemHealthcheck {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemHealthcheckTagsMap;
    type: ListResultItemHealthcheckType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemHealthcheck: S.Schema<ListResultItemHealthcheck>;
export type ListResultItemImageTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemImageTagsMap: S.Schema<ListResultItemImageTagsMap>;
export type ListResultItemImageType = "image";
export declare const ListResultItemImageType: any;
export interface ListResultItemImage {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemImageTagsMap;
    type: ListResultItemImageType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemImage: S.Schema<ListResultItemImage>;
export type ListResultItemInfrastructureTargetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemInfrastructureTargetTagsMap: S.Schema<ListResultItemInfrastructureTargetTagsMap>;
export type ListResultItemInfrastructureTargetType = "infrastructure_target";
export declare const ListResultItemInfrastructureTargetType: any;
export interface ListResultItemInfrastructureTarget {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemInfrastructureTargetTagsMap;
    type: ListResultItemInfrastructureTargetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemInfrastructureTarget: S.Schema<ListResultItemInfrastructureTarget>;
export type ListResultItemKVNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemKVNamespaceTagsMap: S.Schema<ListResultItemKVNamespaceTagsMap>;
export type ListResultItemKVNamespaceType = "kv_namespace";
export declare const ListResultItemKVNamespaceType: any;
export interface ListResultItemKVNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemKVNamespaceTagsMap;
    type: ListResultItemKVNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemKVNamespace: S.Schema<ListResultItemKVNamespace>;
export type ListResultItemLoadBalancerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemLoadBalancerTagsMap: S.Schema<ListResultItemLoadBalancerTagsMap>;
export type ListResultItemLoadBalancerType = "load_balancer";
export declare const ListResultItemLoadBalancerType: any;
export interface ListResultItemLoadBalancer {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemLoadBalancerTagsMap;
    type: ListResultItemLoadBalancerType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemLoadBalancer: S.Schema<ListResultItemLoadBalancer>;
export type ListResultItemLoadBalancerMonitorTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemLoadBalancerMonitorTagsMap: S.Schema<ListResultItemLoadBalancerMonitorTagsMap>;
export type ListResultItemLoadBalancerMonitorType = "load_balancer_monitor";
export declare const ListResultItemLoadBalancerMonitorType: any;
export interface ListResultItemLoadBalancerMonitor {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemLoadBalancerMonitorTagsMap;
    type: ListResultItemLoadBalancerMonitorType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemLoadBalancerMonitor: S.Schema<ListResultItemLoadBalancerMonitor>;
export type ListResultItemLoadBalancerPoolTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemLoadBalancerPoolTagsMap: S.Schema<ListResultItemLoadBalancerPoolTagsMap>;
export type ListResultItemLoadBalancerPoolType = "load_balancer_pool";
export declare const ListResultItemLoadBalancerPoolType: any;
export interface ListResultItemLoadBalancerPool {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemLoadBalancerPoolTagsMap;
    type: ListResultItemLoadBalancerPoolType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemLoadBalancerPool: S.Schema<ListResultItemLoadBalancerPool>;
export type ListResultItemManagedClientCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemManagedClientCertificateTagsMap: S.Schema<ListResultItemManagedClientCertificateTagsMap>;
export type ListResultItemManagedClientCertificateType = "managed_client_certificate";
export declare const ListResultItemManagedClientCertificateType: any;
export interface ListResultItemManagedClientCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemManagedClientCertificateTagsMap;
    type: ListResultItemManagedClientCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemManagedClientCertificate: S.Schema<ListResultItemManagedClientCertificate>;
export type ListResultItemPagesProjectTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemPagesProjectTagsMap: S.Schema<ListResultItemPagesProjectTagsMap>;
export type ListResultItemPagesProjectType = "pages_project";
export declare const ListResultItemPagesProjectType: any;
export interface ListResultItemPagesProject {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemPagesProjectTagsMap;
    type: ListResultItemPagesProjectType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemPagesProject: S.Schema<ListResultItemPagesProject>;
export type ListResultItemQueueTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemQueueTagsMap: S.Schema<ListResultItemQueueTagsMap>;
export type ListResultItemQueueType = "queue";
export declare const ListResultItemQueueType: any;
export interface ListResultItemQueue {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemQueueTagsMap;
    type: ListResultItemQueueType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemQueue: S.Schema<ListResultItemQueue>;
export type ListResultItemR2BucketTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemR2BucketTagsMap: S.Schema<ListResultItemR2BucketTagsMap>;
export type ListResultItemR2BucketType = "r2_bucket";
export declare const ListResultItemR2BucketType: any;
export interface ListResultItemR2Bucket {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemR2BucketTagsMap;
    type: ListResultItemR2BucketType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemR2Bucket: S.Schema<ListResultItemR2Bucket>;
export type ListResultItemResourceShareTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemResourceShareTagsMap: S.Schema<ListResultItemResourceShareTagsMap>;
export type ListResultItemResourceShareType = "resource_share";
export declare const ListResultItemResourceShareType: any;
export interface ListResultItemResourceShare {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemResourceShareTagsMap;
    type: ListResultItemResourceShareType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemResourceShare: S.Schema<ListResultItemResourceShare>;
export type ListResultItemStreamLiveInputTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemStreamLiveInputTagsMap: S.Schema<ListResultItemStreamLiveInputTagsMap>;
export type ListResultItemStreamLiveInputType = "stream_live_input";
export declare const ListResultItemStreamLiveInputType: any;
export interface ListResultItemStreamLiveInput {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemStreamLiveInputTagsMap;
    type: ListResultItemStreamLiveInputType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemStreamLiveInput: S.Schema<ListResultItemStreamLiveInput>;
export type ListResultItemStreamVideoTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemStreamVideoTagsMap: S.Schema<ListResultItemStreamVideoTagsMap>;
export type ListResultItemStreamVideoType = "stream_video";
export declare const ListResultItemStreamVideoType: any;
export interface ListResultItemStreamVideo {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemStreamVideoTagsMap;
    type: ListResultItemStreamVideoType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemStreamVideo: S.Schema<ListResultItemStreamVideo>;
export type ListResultItemVectorizeIndexTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemVectorizeIndexTagsMap: S.Schema<ListResultItemVectorizeIndexTagsMap>;
export type ListResultItemVectorizeIndexType = "vectorize_index";
export declare const ListResultItemVectorizeIndexType: any;
export interface ListResultItemVectorizeIndex {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemVectorizeIndexTagsMap;
    type: ListResultItemVectorizeIndexType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemVectorizeIndex: S.Schema<ListResultItemVectorizeIndex>;
export type ListResultItemWorkerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemWorkerTagsMap: S.Schema<ListResultItemWorkerTagsMap>;
export type ListResultItemWorkerType = "worker";
export declare const ListResultItemWorkerType: any;
export interface ListResultItemWorker {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemWorkerTagsMap;
    type: ListResultItemWorkerType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemWorker: S.Schema<ListResultItemWorker>;
export type ListResultItemWorkerRouteTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemWorkerRouteTagsMap: S.Schema<ListResultItemWorkerRouteTagsMap>;
export type ListResultItemWorkerRouteType = "worker_route";
export declare const ListResultItemWorkerRouteType: any;
export interface ListResultItemWorkerRoute {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemWorkerRouteTagsMap;
    type: ListResultItemWorkerRouteType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemWorkerRoute: S.Schema<ListResultItemWorkerRoute>;
export type ListResultItemWorkerVersionTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemWorkerVersionTagsMap: S.Schema<ListResultItemWorkerVersionTagsMap>;
export type ListResultItemWorkerVersionType = "worker_version";
export declare const ListResultItemWorkerVersionType: any;
export interface ListResultItemWorkerVersion {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemWorkerVersionTagsMap;
    type: ListResultItemWorkerVersionType;
    /** Worker ID is required only for worker_version resources */
    workerId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemWorkerVersion: S.Schema<ListResultItemWorkerVersion>;
export type ListResultItemZoneTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemZoneTagsMap: S.Schema<ListResultItemZoneTagsMap>;
export type ListResultItemZoneType = "zone";
export declare const ListResultItemZoneType: any;
export interface ListResultItemZone {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemZoneTagsMap;
    type: ListResultItemZoneType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemZone: S.Schema<ListResultItemZone>;
export type ListResultItemZoneRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ListResultItemZoneRulesetTagsMap: S.Schema<ListResultItemZoneRulesetTagsMap>;
export type ListResultItemZoneRulesetType = "zone_ruleset";
export declare const ListResultItemZoneRulesetType: any;
export interface ListResultItemZoneRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ListResultItemZoneRulesetTagsMap;
    type: ListResultItemZoneRulesetType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ListResultItemZoneRuleset: S.Schema<ListResultItemZoneRuleset>;
export type ListResultItem = ListResultItemAccessApplication | ListResultItemAccessApplicationPolicy | ListResultItemAccessGroup | ListResultItemAccount | ListResultItemAccountRuleset | ListResultItemAIGateway | ListResultItemAlertingPolicy | ListResultItemAlertingWebhook | ListResultItemAPIGatewayOperation | ListResultItemCloudflaredTunnel | ListResultItemCustomCertificate | ListResultItemCustomHostname | ListResultItemCwsDeployment | ListResultItemCwsPolicy | ListResultItemCwsPolicySet | ListResultItemCwsWorkload | ListResultItemD1Database | ListResultItemDNSRecord | ListResultItemDurableObjectNamespace | ListResultItemGatewayList | ListResultItemGatewayRule | ListResultItemHealthcheck | ListResultItemImage | ListResultItemInfrastructureTarget | ListResultItemKVNamespace | ListResultItemLoadBalancer | ListResultItemLoadBalancerMonitor | ListResultItemLoadBalancerPool | ListResultItemManagedClientCertificate | ListResultItemPagesProject | ListResultItemQueue | ListResultItemR2Bucket | ListResultItemResourceShare | ListResultItemStreamLiveInput | ListResultItemStreamVideo | ListResultItemVectorizeIndex | ListResultItemWorker | ListResultItemWorkerRoute | ListResultItemWorkerVersion | ListResultItemZone | ListResultItemZoneRuleset;
export declare const ListResultItem: any;
export type ListResultList = Array<ListResultItem>;
export declare const ListResultList: S.Schema<ListResultList>;
export interface ListResourceTaggingsResponse {
    /** The unwrapped `result` payload of the v4 response envelope. */
    result: ListResultList;
    /** Pagination info from the envelope's `result_info`. */
    resultInfo?: ResultInfo | null;
}
export declare const ListResourceTaggingsResponse: S.Schema<ListResourceTaggingsResponse>;
export type ValuesListRequestType = "access_application" | "access_application_policy" | "access_group" | "account" | "account_ruleset" | "ai_gateway" | "alerting_policy" | "alerting_webhook" | "api_gateway_operation" | "cloudflared_tunnel" | "custom_certificate" | "custom_hostname" | "cws_deployment" | "cws_policy" | "cws_policy_set" | "cws_workload" | "d1_database" | "dns_record" | "durable_object_namespace" | "gateway_list" | "gateway_rule" | "healthcheck" | "image" | "infrastructure_target" | "kv_namespace" | "load_balancer" | "load_balancer_monitor" | "load_balancer_pool" | "managed_client_certificate" | "pages_project" | "queue" | "r2_bucket" | "resource_share" | "stream_live_input" | "stream_video" | "vectorize_index" | "worker" | "worker_route" | "worker_version" | "zone" | "zone_ruleset";
export declare const ValuesListRequestType: any;
export interface ListValuesRequest {
    /** Identifier. */
    accountId: string;
    tagKey: string;
    /** Cursor for pagination. */
    cursor?: string;
    /** Filter by resource type. */
    type?: ValuesListRequestType | (string & {});
}
export declare const ListValuesRequest: S.Schema<ListValuesRequest>;
export type ValuesListResultList = Array<string>;
export declare const ValuesListResultList: S.Schema<ValuesListResultList>;
export interface ListValuesResponse {
    /** The unwrapped `result` payload of the v4 response envelope. */
    result: ValuesListResultList;
    /** Pagination info from the envelope's `result_info`. */
    resultInfo?: ResultInfo | null;
}
export declare const ListValuesResponse: S.Schema<ListValuesResponse>;
export type AccountTagsUpdateRequestResourceType = "access_application" | "access_group" | "account" | "account_ruleset" | "ai_gateway" | "alerting_policy" | "alerting_webhook" | "cloudflared_tunnel" | "cws_deployment" | "cws_policy" | "cws_policy_set" | "cws_workload" | "d1_database" | "durable_object_namespace" | "gateway_list" | "gateway_rule" | "image" | "infrastructure_target" | "kv_namespace" | "load_balancer_monitor" | "load_balancer_pool" | "pages_project" | "queue" | "r2_bucket" | "resource_share" | "stream_live_input" | "stream_video" | "vectorize_index" | "worker" | "worker_version";
export declare const AccountTagsUpdateRequestResourceType: any;
export type AccountTagsUpdateRequestTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateRequestTagsMap: S.Schema<AccountTagsUpdateRequestTagsMap>;
export interface PutAccountTagRequest {
    /** Identifier. */
    accountId: string;
    /** Identifies the unique resource. */
    resourceId: string;
    /** Enum for base account-level resource types (those with no extra required fields). */
    resourceType: AccountTagsUpdateRequestResourceType | (string & {});
    /** Worker ID is required only for worker_version resources */
    workerId?: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags?: AccountTagsUpdateRequestTagsMap;
    ifMatch?: string;
}
export declare const PutAccountTagRequest: S.Schema<PutAccountTagRequest>;
export type AccountTagsUpdateResultAccessApplicationTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAccessApplicationTagsMap: S.Schema<AccountTagsUpdateResultAccessApplicationTagsMap>;
export type AccountTagsUpdateResultAccessApplicationType = "access_application";
export declare const AccountTagsUpdateResultAccessApplicationType: any;
export interface AccountTagsUpdateResultAccessApplication {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAccessApplicationTagsMap;
    type: AccountTagsUpdateResultAccessApplicationType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAccessApplication: S.Schema<AccountTagsUpdateResultAccessApplication>;
export type AccountTagsUpdateResultAccessApplicationPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAccessApplicationPolicyTagsMap: S.Schema<AccountTagsUpdateResultAccessApplicationPolicyTagsMap>;
export type AccountTagsUpdateResultAccessApplicationPolicyType = "access_application_policy";
export declare const AccountTagsUpdateResultAccessApplicationPolicyType: any;
export interface AccountTagsUpdateResultAccessApplicationPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAccessApplicationPolicyTagsMap;
    type: AccountTagsUpdateResultAccessApplicationPolicyType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAccessApplicationPolicy: S.Schema<AccountTagsUpdateResultAccessApplicationPolicy>;
export type AccountTagsUpdateResultAccessGroupTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAccessGroupTagsMap: S.Schema<AccountTagsUpdateResultAccessGroupTagsMap>;
export type AccountTagsUpdateResultAccessGroupType = "access_group";
export declare const AccountTagsUpdateResultAccessGroupType: any;
export interface AccountTagsUpdateResultAccessGroup {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAccessGroupTagsMap;
    type: AccountTagsUpdateResultAccessGroupType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAccessGroup: S.Schema<AccountTagsUpdateResultAccessGroup>;
export type AccountTagsUpdateResultAccountTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAccountTagsMap: S.Schema<AccountTagsUpdateResultAccountTagsMap>;
export type AccountTagsUpdateResultAccountType = "account";
export declare const AccountTagsUpdateResultAccountType: any;
export interface AccountTagsUpdateResultAccount {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAccountTagsMap;
    type: AccountTagsUpdateResultAccountType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAccount: S.Schema<AccountTagsUpdateResultAccount>;
export type AccountTagsUpdateResultAccountRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAccountRulesetTagsMap: S.Schema<AccountTagsUpdateResultAccountRulesetTagsMap>;
export type AccountTagsUpdateResultAccountRulesetType = "account_ruleset";
export declare const AccountTagsUpdateResultAccountRulesetType: any;
export interface AccountTagsUpdateResultAccountRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAccountRulesetTagsMap;
    type: AccountTagsUpdateResultAccountRulesetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAccountRuleset: S.Schema<AccountTagsUpdateResultAccountRuleset>;
export type AccountTagsUpdateResultAIGatewayTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAIGatewayTagsMap: S.Schema<AccountTagsUpdateResultAIGatewayTagsMap>;
export type AccountTagsUpdateResultAIGatewayType = "ai_gateway";
export declare const AccountTagsUpdateResultAIGatewayType: any;
export interface AccountTagsUpdateResultAIGateway {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAIGatewayTagsMap;
    type: AccountTagsUpdateResultAIGatewayType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAIGateway: S.Schema<AccountTagsUpdateResultAIGateway>;
export type AccountTagsUpdateResultAlertingPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAlertingPolicyTagsMap: S.Schema<AccountTagsUpdateResultAlertingPolicyTagsMap>;
export type AccountTagsUpdateResultAlertingPolicyType = "alerting_policy";
export declare const AccountTagsUpdateResultAlertingPolicyType: any;
export interface AccountTagsUpdateResultAlertingPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAlertingPolicyTagsMap;
    type: AccountTagsUpdateResultAlertingPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAlertingPolicy: S.Schema<AccountTagsUpdateResultAlertingPolicy>;
export type AccountTagsUpdateResultAlertingWebhookTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAlertingWebhookTagsMap: S.Schema<AccountTagsUpdateResultAlertingWebhookTagsMap>;
export type AccountTagsUpdateResultAlertingWebhookType = "alerting_webhook";
export declare const AccountTagsUpdateResultAlertingWebhookType: any;
export interface AccountTagsUpdateResultAlertingWebhook {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAlertingWebhookTagsMap;
    type: AccountTagsUpdateResultAlertingWebhookType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAlertingWebhook: S.Schema<AccountTagsUpdateResultAlertingWebhook>;
export type AccountTagsUpdateResultAPIGatewayOperationTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultAPIGatewayOperationTagsMap: S.Schema<AccountTagsUpdateResultAPIGatewayOperationTagsMap>;
export type AccountTagsUpdateResultAPIGatewayOperationType = "api_gateway_operation";
export declare const AccountTagsUpdateResultAPIGatewayOperationType: any;
export interface AccountTagsUpdateResultAPIGatewayOperation {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultAPIGatewayOperationTagsMap;
    type: AccountTagsUpdateResultAPIGatewayOperationType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultAPIGatewayOperation: S.Schema<AccountTagsUpdateResultAPIGatewayOperation>;
export type AccountTagsUpdateResultCloudflaredTunnelTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCloudflaredTunnelTagsMap: S.Schema<AccountTagsUpdateResultCloudflaredTunnelTagsMap>;
export type AccountTagsUpdateResultCloudflaredTunnelType = "cloudflared_tunnel";
export declare const AccountTagsUpdateResultCloudflaredTunnelType: any;
export interface AccountTagsUpdateResultCloudflaredTunnel {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCloudflaredTunnelTagsMap;
    type: AccountTagsUpdateResultCloudflaredTunnelType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCloudflaredTunnel: S.Schema<AccountTagsUpdateResultCloudflaredTunnel>;
export type AccountTagsUpdateResultCustomCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCustomCertificateTagsMap: S.Schema<AccountTagsUpdateResultCustomCertificateTagsMap>;
export type AccountTagsUpdateResultCustomCertificateType = "custom_certificate";
export declare const AccountTagsUpdateResultCustomCertificateType: any;
export interface AccountTagsUpdateResultCustomCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCustomCertificateTagsMap;
    type: AccountTagsUpdateResultCustomCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCustomCertificate: S.Schema<AccountTagsUpdateResultCustomCertificate>;
export type AccountTagsUpdateResultCustomHostnameTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCustomHostnameTagsMap: S.Schema<AccountTagsUpdateResultCustomHostnameTagsMap>;
export type AccountTagsUpdateResultCustomHostnameType = "custom_hostname";
export declare const AccountTagsUpdateResultCustomHostnameType: any;
export interface AccountTagsUpdateResultCustomHostname {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCustomHostnameTagsMap;
    type: AccountTagsUpdateResultCustomHostnameType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCustomHostname: S.Schema<AccountTagsUpdateResultCustomHostname>;
export type AccountTagsUpdateResultCwsDeploymentTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCwsDeploymentTagsMap: S.Schema<AccountTagsUpdateResultCwsDeploymentTagsMap>;
export type AccountTagsUpdateResultCwsDeploymentType = "cws_deployment";
export declare const AccountTagsUpdateResultCwsDeploymentType: any;
export interface AccountTagsUpdateResultCwsDeployment {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCwsDeploymentTagsMap;
    type: AccountTagsUpdateResultCwsDeploymentType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCwsDeployment: S.Schema<AccountTagsUpdateResultCwsDeployment>;
export type AccountTagsUpdateResultCwsPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCwsPolicyTagsMap: S.Schema<AccountTagsUpdateResultCwsPolicyTagsMap>;
export type AccountTagsUpdateResultCwsPolicyType = "cws_policy";
export declare const AccountTagsUpdateResultCwsPolicyType: any;
export interface AccountTagsUpdateResultCwsPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCwsPolicyTagsMap;
    type: AccountTagsUpdateResultCwsPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCwsPolicy: S.Schema<AccountTagsUpdateResultCwsPolicy>;
export type AccountTagsUpdateResultCwsPolicySetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCwsPolicySetTagsMap: S.Schema<AccountTagsUpdateResultCwsPolicySetTagsMap>;
export type AccountTagsUpdateResultCwsPolicySetType = "cws_policy_set";
export declare const AccountTagsUpdateResultCwsPolicySetType: any;
export interface AccountTagsUpdateResultCwsPolicySet {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCwsPolicySetTagsMap;
    type: AccountTagsUpdateResultCwsPolicySetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCwsPolicySet: S.Schema<AccountTagsUpdateResultCwsPolicySet>;
export type AccountTagsUpdateResultCwsWorkloadTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultCwsWorkloadTagsMap: S.Schema<AccountTagsUpdateResultCwsWorkloadTagsMap>;
export type AccountTagsUpdateResultCwsWorkloadType = "cws_workload";
export declare const AccountTagsUpdateResultCwsWorkloadType: any;
export interface AccountTagsUpdateResultCwsWorkload {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultCwsWorkloadTagsMap;
    type: AccountTagsUpdateResultCwsWorkloadType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultCwsWorkload: S.Schema<AccountTagsUpdateResultCwsWorkload>;
export type AccountTagsUpdateResultD1DatabaseTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultD1DatabaseTagsMap: S.Schema<AccountTagsUpdateResultD1DatabaseTagsMap>;
export type AccountTagsUpdateResultD1DatabaseType = "d1_database";
export declare const AccountTagsUpdateResultD1DatabaseType: any;
export interface AccountTagsUpdateResultD1Database {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultD1DatabaseTagsMap;
    type: AccountTagsUpdateResultD1DatabaseType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultD1Database: S.Schema<AccountTagsUpdateResultD1Database>;
export type AccountTagsUpdateResultDNSRecordTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultDNSRecordTagsMap: S.Schema<AccountTagsUpdateResultDNSRecordTagsMap>;
export type AccountTagsUpdateResultDNSRecordType = "dns_record";
export declare const AccountTagsUpdateResultDNSRecordType: any;
export interface AccountTagsUpdateResultDNSRecord {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultDNSRecordTagsMap;
    type: AccountTagsUpdateResultDNSRecordType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultDNSRecord: S.Schema<AccountTagsUpdateResultDNSRecord>;
export type AccountTagsUpdateResultDurableObjectNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultDurableObjectNamespaceTagsMap: S.Schema<AccountTagsUpdateResultDurableObjectNamespaceTagsMap>;
export type AccountTagsUpdateResultDurableObjectNamespaceType = "durable_object_namespace";
export declare const AccountTagsUpdateResultDurableObjectNamespaceType: any;
export interface AccountTagsUpdateResultDurableObjectNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultDurableObjectNamespaceTagsMap;
    type: AccountTagsUpdateResultDurableObjectNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultDurableObjectNamespace: S.Schema<AccountTagsUpdateResultDurableObjectNamespace>;
export type AccountTagsUpdateResultGatewayListTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultGatewayListTagsMap: S.Schema<AccountTagsUpdateResultGatewayListTagsMap>;
export type AccountTagsUpdateResultGatewayListType = "gateway_list";
export declare const AccountTagsUpdateResultGatewayListType: any;
export interface AccountTagsUpdateResultGatewayList {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultGatewayListTagsMap;
    type: AccountTagsUpdateResultGatewayListType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultGatewayList: S.Schema<AccountTagsUpdateResultGatewayList>;
export type AccountTagsUpdateResultGatewayRuleTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultGatewayRuleTagsMap: S.Schema<AccountTagsUpdateResultGatewayRuleTagsMap>;
export type AccountTagsUpdateResultGatewayRuleType = "gateway_rule";
export declare const AccountTagsUpdateResultGatewayRuleType: any;
export interface AccountTagsUpdateResultGatewayRule {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultGatewayRuleTagsMap;
    type: AccountTagsUpdateResultGatewayRuleType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultGatewayRule: S.Schema<AccountTagsUpdateResultGatewayRule>;
export type AccountTagsUpdateResultHealthcheckTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultHealthcheckTagsMap: S.Schema<AccountTagsUpdateResultHealthcheckTagsMap>;
export type AccountTagsUpdateResultHealthcheckType = "healthcheck";
export declare const AccountTagsUpdateResultHealthcheckType: any;
export interface AccountTagsUpdateResultHealthcheck {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultHealthcheckTagsMap;
    type: AccountTagsUpdateResultHealthcheckType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultHealthcheck: S.Schema<AccountTagsUpdateResultHealthcheck>;
export type AccountTagsUpdateResultImageTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultImageTagsMap: S.Schema<AccountTagsUpdateResultImageTagsMap>;
export type AccountTagsUpdateResultImageType = "image";
export declare const AccountTagsUpdateResultImageType: any;
export interface AccountTagsUpdateResultImage {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultImageTagsMap;
    type: AccountTagsUpdateResultImageType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultImage: S.Schema<AccountTagsUpdateResultImage>;
export type AccountTagsUpdateResultInfrastructureTargetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultInfrastructureTargetTagsMap: S.Schema<AccountTagsUpdateResultInfrastructureTargetTagsMap>;
export type AccountTagsUpdateResultInfrastructureTargetType = "infrastructure_target";
export declare const AccountTagsUpdateResultInfrastructureTargetType: any;
export interface AccountTagsUpdateResultInfrastructureTarget {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultInfrastructureTargetTagsMap;
    type: AccountTagsUpdateResultInfrastructureTargetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultInfrastructureTarget: S.Schema<AccountTagsUpdateResultInfrastructureTarget>;
export type AccountTagsUpdateResultKVNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultKVNamespaceTagsMap: S.Schema<AccountTagsUpdateResultKVNamespaceTagsMap>;
export type AccountTagsUpdateResultKVNamespaceType = "kv_namespace";
export declare const AccountTagsUpdateResultKVNamespaceType: any;
export interface AccountTagsUpdateResultKVNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultKVNamespaceTagsMap;
    type: AccountTagsUpdateResultKVNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultKVNamespace: S.Schema<AccountTagsUpdateResultKVNamespace>;
export type AccountTagsUpdateResultLoadBalancerTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultLoadBalancerTagsMap: S.Schema<AccountTagsUpdateResultLoadBalancerTagsMap>;
export type AccountTagsUpdateResultLoadBalancerType = "load_balancer";
export declare const AccountTagsUpdateResultLoadBalancerType: any;
export interface AccountTagsUpdateResultLoadBalancer {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultLoadBalancerTagsMap;
    type: AccountTagsUpdateResultLoadBalancerType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultLoadBalancer: S.Schema<AccountTagsUpdateResultLoadBalancer>;
export type AccountTagsUpdateResultLoadBalancerMonitorTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultLoadBalancerMonitorTagsMap: S.Schema<AccountTagsUpdateResultLoadBalancerMonitorTagsMap>;
export type AccountTagsUpdateResultLoadBalancerMonitorType = "load_balancer_monitor";
export declare const AccountTagsUpdateResultLoadBalancerMonitorType: any;
export interface AccountTagsUpdateResultLoadBalancerMonitor {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultLoadBalancerMonitorTagsMap;
    type: AccountTagsUpdateResultLoadBalancerMonitorType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultLoadBalancerMonitor: S.Schema<AccountTagsUpdateResultLoadBalancerMonitor>;
export type AccountTagsUpdateResultLoadBalancerPoolTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultLoadBalancerPoolTagsMap: S.Schema<AccountTagsUpdateResultLoadBalancerPoolTagsMap>;
export type AccountTagsUpdateResultLoadBalancerPoolType = "load_balancer_pool";
export declare const AccountTagsUpdateResultLoadBalancerPoolType: any;
export interface AccountTagsUpdateResultLoadBalancerPool {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultLoadBalancerPoolTagsMap;
    type: AccountTagsUpdateResultLoadBalancerPoolType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultLoadBalancerPool: S.Schema<AccountTagsUpdateResultLoadBalancerPool>;
export type AccountTagsUpdateResultManagedClientCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultManagedClientCertificateTagsMap: S.Schema<AccountTagsUpdateResultManagedClientCertificateTagsMap>;
export type AccountTagsUpdateResultManagedClientCertificateType = "managed_client_certificate";
export declare const AccountTagsUpdateResultManagedClientCertificateType: any;
export interface AccountTagsUpdateResultManagedClientCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultManagedClientCertificateTagsMap;
    type: AccountTagsUpdateResultManagedClientCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultManagedClientCertificate: S.Schema<AccountTagsUpdateResultManagedClientCertificate>;
export type AccountTagsUpdateResultPagesProjectTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultPagesProjectTagsMap: S.Schema<AccountTagsUpdateResultPagesProjectTagsMap>;
export type AccountTagsUpdateResultPagesProjectType = "pages_project";
export declare const AccountTagsUpdateResultPagesProjectType: any;
export interface AccountTagsUpdateResultPagesProject {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultPagesProjectTagsMap;
    type: AccountTagsUpdateResultPagesProjectType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultPagesProject: S.Schema<AccountTagsUpdateResultPagesProject>;
export type AccountTagsUpdateResultQueueTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultQueueTagsMap: S.Schema<AccountTagsUpdateResultQueueTagsMap>;
export type AccountTagsUpdateResultQueueType = "queue";
export declare const AccountTagsUpdateResultQueueType: any;
export interface AccountTagsUpdateResultQueue {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultQueueTagsMap;
    type: AccountTagsUpdateResultQueueType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultQueue: S.Schema<AccountTagsUpdateResultQueue>;
export type AccountTagsUpdateResultR2BucketTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultR2BucketTagsMap: S.Schema<AccountTagsUpdateResultR2BucketTagsMap>;
export type AccountTagsUpdateResultR2BucketType = "r2_bucket";
export declare const AccountTagsUpdateResultR2BucketType: any;
export interface AccountTagsUpdateResultR2Bucket {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultR2BucketTagsMap;
    type: AccountTagsUpdateResultR2BucketType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultR2Bucket: S.Schema<AccountTagsUpdateResultR2Bucket>;
export type AccountTagsUpdateResultResourceShareTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultResourceShareTagsMap: S.Schema<AccountTagsUpdateResultResourceShareTagsMap>;
export type AccountTagsUpdateResultResourceShareType = "resource_share";
export declare const AccountTagsUpdateResultResourceShareType: any;
export interface AccountTagsUpdateResultResourceShare {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultResourceShareTagsMap;
    type: AccountTagsUpdateResultResourceShareType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultResourceShare: S.Schema<AccountTagsUpdateResultResourceShare>;
export type AccountTagsUpdateResultStreamLiveInputTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultStreamLiveInputTagsMap: S.Schema<AccountTagsUpdateResultStreamLiveInputTagsMap>;
export type AccountTagsUpdateResultStreamLiveInputType = "stream_live_input";
export declare const AccountTagsUpdateResultStreamLiveInputType: any;
export interface AccountTagsUpdateResultStreamLiveInput {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultStreamLiveInputTagsMap;
    type: AccountTagsUpdateResultStreamLiveInputType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultStreamLiveInput: S.Schema<AccountTagsUpdateResultStreamLiveInput>;
export type AccountTagsUpdateResultStreamVideoTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultStreamVideoTagsMap: S.Schema<AccountTagsUpdateResultStreamVideoTagsMap>;
export type AccountTagsUpdateResultStreamVideoType = "stream_video";
export declare const AccountTagsUpdateResultStreamVideoType: any;
export interface AccountTagsUpdateResultStreamVideo {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultStreamVideoTagsMap;
    type: AccountTagsUpdateResultStreamVideoType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultStreamVideo: S.Schema<AccountTagsUpdateResultStreamVideo>;
export type AccountTagsUpdateResultVectorizeIndexTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultVectorizeIndexTagsMap: S.Schema<AccountTagsUpdateResultVectorizeIndexTagsMap>;
export type AccountTagsUpdateResultVectorizeIndexType = "vectorize_index";
export declare const AccountTagsUpdateResultVectorizeIndexType: any;
export interface AccountTagsUpdateResultVectorizeIndex {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultVectorizeIndexTagsMap;
    type: AccountTagsUpdateResultVectorizeIndexType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultVectorizeIndex: S.Schema<AccountTagsUpdateResultVectorizeIndex>;
export type AccountTagsUpdateResultWorkerTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultWorkerTagsMap: S.Schema<AccountTagsUpdateResultWorkerTagsMap>;
export type AccountTagsUpdateResultWorkerType = "worker";
export declare const AccountTagsUpdateResultWorkerType: any;
export interface AccountTagsUpdateResultWorker {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultWorkerTagsMap;
    type: AccountTagsUpdateResultWorkerType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultWorker: S.Schema<AccountTagsUpdateResultWorker>;
export type AccountTagsUpdateResultWorkerRouteTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultWorkerRouteTagsMap: S.Schema<AccountTagsUpdateResultWorkerRouteTagsMap>;
export type AccountTagsUpdateResultWorkerRouteType = "worker_route";
export declare const AccountTagsUpdateResultWorkerRouteType: any;
export interface AccountTagsUpdateResultWorkerRoute {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultWorkerRouteTagsMap;
    type: AccountTagsUpdateResultWorkerRouteType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultWorkerRoute: S.Schema<AccountTagsUpdateResultWorkerRoute>;
export type AccountTagsUpdateResultWorkerVersionTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultWorkerVersionTagsMap: S.Schema<AccountTagsUpdateResultWorkerVersionTagsMap>;
export type AccountTagsUpdateResultWorkerVersionType = "worker_version";
export declare const AccountTagsUpdateResultWorkerVersionType: any;
export interface AccountTagsUpdateResultWorkerVersion {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultWorkerVersionTagsMap;
    type: AccountTagsUpdateResultWorkerVersionType;
    /** Worker ID is required only for worker_version resources */
    workerId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultWorkerVersion: S.Schema<AccountTagsUpdateResultWorkerVersion>;
export type AccountTagsUpdateResultZoneTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultZoneTagsMap: S.Schema<AccountTagsUpdateResultZoneTagsMap>;
export type AccountTagsUpdateResultZoneType = "zone";
export declare const AccountTagsUpdateResultZoneType: any;
export interface AccountTagsUpdateResultZone {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultZoneTagsMap;
    type: AccountTagsUpdateResultZoneType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultZone: S.Schema<AccountTagsUpdateResultZone>;
export type AccountTagsUpdateResultZoneRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const AccountTagsUpdateResultZoneRulesetTagsMap: S.Schema<AccountTagsUpdateResultZoneRulesetTagsMap>;
export type AccountTagsUpdateResultZoneRulesetType = "zone_ruleset";
export declare const AccountTagsUpdateResultZoneRulesetType: any;
export interface AccountTagsUpdateResultZoneRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: AccountTagsUpdateResultZoneRulesetTagsMap;
    type: AccountTagsUpdateResultZoneRulesetType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const AccountTagsUpdateResultZoneRuleset: S.Schema<AccountTagsUpdateResultZoneRuleset>;
export type AccountTagsUpdateResult = AccountTagsUpdateResultAccessApplication | AccountTagsUpdateResultAccessApplicationPolicy | AccountTagsUpdateResultAccessGroup | AccountTagsUpdateResultAccount | AccountTagsUpdateResultAccountRuleset | AccountTagsUpdateResultAIGateway | AccountTagsUpdateResultAlertingPolicy | AccountTagsUpdateResultAlertingWebhook | AccountTagsUpdateResultAPIGatewayOperation | AccountTagsUpdateResultCloudflaredTunnel | AccountTagsUpdateResultCustomCertificate | AccountTagsUpdateResultCustomHostname | AccountTagsUpdateResultCwsDeployment | AccountTagsUpdateResultCwsPolicy | AccountTagsUpdateResultCwsPolicySet | AccountTagsUpdateResultCwsWorkload | AccountTagsUpdateResultD1Database | AccountTagsUpdateResultDNSRecord | AccountTagsUpdateResultDurableObjectNamespace | AccountTagsUpdateResultGatewayList | AccountTagsUpdateResultGatewayRule | AccountTagsUpdateResultHealthcheck | AccountTagsUpdateResultImage | AccountTagsUpdateResultInfrastructureTarget | AccountTagsUpdateResultKVNamespace | AccountTagsUpdateResultLoadBalancer | AccountTagsUpdateResultLoadBalancerMonitor | AccountTagsUpdateResultLoadBalancerPool | AccountTagsUpdateResultManagedClientCertificate | AccountTagsUpdateResultPagesProject | AccountTagsUpdateResultQueue | AccountTagsUpdateResultR2Bucket | AccountTagsUpdateResultResourceShare | AccountTagsUpdateResultStreamLiveInput | AccountTagsUpdateResultStreamVideo | AccountTagsUpdateResultVectorizeIndex | AccountTagsUpdateResultWorker | AccountTagsUpdateResultWorkerRoute | AccountTagsUpdateResultWorkerVersion | AccountTagsUpdateResultZone | AccountTagsUpdateResultZoneRuleset;
export declare const AccountTagsUpdateResult: any;
export type PutAccountTagResponse = AccountTagsUpdateResult;
export declare const PutAccountTagResponse: S.Schema<PutAccountTagResponse>;
export type ZoneTagsUpdateRequestResourceType = "api_gateway_operation" | "custom_certificate" | "custom_hostname" | "dns_record" | "healthcheck" | "load_balancer" | "managed_client_certificate" | "worker_route" | "zone" | "zone_ruleset" | "access_application_policy";
export declare const ZoneTagsUpdateRequestResourceType: any;
export type ZoneTagsUpdateRequestTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateRequestTagsMap: S.Schema<ZoneTagsUpdateRequestTagsMap>;
export interface PutZoneTagRequest {
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Identifies the unique resource. */
    resourceId: string;
    /** Enum for base zone-level resource types (those with no extra required fields). */
    resourceType: ZoneTagsUpdateRequestResourceType | (string & {});
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags?: ZoneTagsUpdateRequestTagsMap;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId?: string;
    ifMatch?: string;
}
export declare const PutZoneTagRequest: S.Schema<PutZoneTagRequest>;
export type ZoneTagsUpdateResultAccessApplicationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAccessApplicationTagsMap: S.Schema<ZoneTagsUpdateResultAccessApplicationTagsMap>;
export type ZoneTagsUpdateResultAccessApplicationType = "access_application";
export declare const ZoneTagsUpdateResultAccessApplicationType: any;
export interface ZoneTagsUpdateResultAccessApplication {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAccessApplicationTagsMap;
    type: ZoneTagsUpdateResultAccessApplicationType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAccessApplication: S.Schema<ZoneTagsUpdateResultAccessApplication>;
export type ZoneTagsUpdateResultAccessApplicationPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAccessApplicationPolicyTagsMap: S.Schema<ZoneTagsUpdateResultAccessApplicationPolicyTagsMap>;
export type ZoneTagsUpdateResultAccessApplicationPolicyType = "access_application_policy";
export declare const ZoneTagsUpdateResultAccessApplicationPolicyType: any;
export interface ZoneTagsUpdateResultAccessApplicationPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** Access application ID is required only for access_application_policy resources */
    accessApplicationId: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAccessApplicationPolicyTagsMap;
    type: ZoneTagsUpdateResultAccessApplicationPolicyType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAccessApplicationPolicy: S.Schema<ZoneTagsUpdateResultAccessApplicationPolicy>;
export type ZoneTagsUpdateResultAccessGroupTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAccessGroupTagsMap: S.Schema<ZoneTagsUpdateResultAccessGroupTagsMap>;
export type ZoneTagsUpdateResultAccessGroupType = "access_group";
export declare const ZoneTagsUpdateResultAccessGroupType: any;
export interface ZoneTagsUpdateResultAccessGroup {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAccessGroupTagsMap;
    type: ZoneTagsUpdateResultAccessGroupType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAccessGroup: S.Schema<ZoneTagsUpdateResultAccessGroup>;
export type ZoneTagsUpdateResultAccountTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAccountTagsMap: S.Schema<ZoneTagsUpdateResultAccountTagsMap>;
export type ZoneTagsUpdateResultAccountType = "account";
export declare const ZoneTagsUpdateResultAccountType: any;
export interface ZoneTagsUpdateResultAccount {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAccountTagsMap;
    type: ZoneTagsUpdateResultAccountType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAccount: S.Schema<ZoneTagsUpdateResultAccount>;
export type ZoneTagsUpdateResultAccountRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAccountRulesetTagsMap: S.Schema<ZoneTagsUpdateResultAccountRulesetTagsMap>;
export type ZoneTagsUpdateResultAccountRulesetType = "account_ruleset";
export declare const ZoneTagsUpdateResultAccountRulesetType: any;
export interface ZoneTagsUpdateResultAccountRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAccountRulesetTagsMap;
    type: ZoneTagsUpdateResultAccountRulesetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAccountRuleset: S.Schema<ZoneTagsUpdateResultAccountRuleset>;
export type ZoneTagsUpdateResultAIGatewayTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAIGatewayTagsMap: S.Schema<ZoneTagsUpdateResultAIGatewayTagsMap>;
export type ZoneTagsUpdateResultAIGatewayType = "ai_gateway";
export declare const ZoneTagsUpdateResultAIGatewayType: any;
export interface ZoneTagsUpdateResultAIGateway {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAIGatewayTagsMap;
    type: ZoneTagsUpdateResultAIGatewayType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAIGateway: S.Schema<ZoneTagsUpdateResultAIGateway>;
export type ZoneTagsUpdateResultAlertingPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAlertingPolicyTagsMap: S.Schema<ZoneTagsUpdateResultAlertingPolicyTagsMap>;
export type ZoneTagsUpdateResultAlertingPolicyType = "alerting_policy";
export declare const ZoneTagsUpdateResultAlertingPolicyType: any;
export interface ZoneTagsUpdateResultAlertingPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAlertingPolicyTagsMap;
    type: ZoneTagsUpdateResultAlertingPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAlertingPolicy: S.Schema<ZoneTagsUpdateResultAlertingPolicy>;
export type ZoneTagsUpdateResultAlertingWebhookTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAlertingWebhookTagsMap: S.Schema<ZoneTagsUpdateResultAlertingWebhookTagsMap>;
export type ZoneTagsUpdateResultAlertingWebhookType = "alerting_webhook";
export declare const ZoneTagsUpdateResultAlertingWebhookType: any;
export interface ZoneTagsUpdateResultAlertingWebhook {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAlertingWebhookTagsMap;
    type: ZoneTagsUpdateResultAlertingWebhookType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAlertingWebhook: S.Schema<ZoneTagsUpdateResultAlertingWebhook>;
export type ZoneTagsUpdateResultAPIGatewayOperationTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultAPIGatewayOperationTagsMap: S.Schema<ZoneTagsUpdateResultAPIGatewayOperationTagsMap>;
export type ZoneTagsUpdateResultAPIGatewayOperationType = "api_gateway_operation";
export declare const ZoneTagsUpdateResultAPIGatewayOperationType: any;
export interface ZoneTagsUpdateResultAPIGatewayOperation {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultAPIGatewayOperationTagsMap;
    type: ZoneTagsUpdateResultAPIGatewayOperationType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultAPIGatewayOperation: S.Schema<ZoneTagsUpdateResultAPIGatewayOperation>;
export type ZoneTagsUpdateResultCloudflaredTunnelTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCloudflaredTunnelTagsMap: S.Schema<ZoneTagsUpdateResultCloudflaredTunnelTagsMap>;
export type ZoneTagsUpdateResultCloudflaredTunnelType = "cloudflared_tunnel";
export declare const ZoneTagsUpdateResultCloudflaredTunnelType: any;
export interface ZoneTagsUpdateResultCloudflaredTunnel {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCloudflaredTunnelTagsMap;
    type: ZoneTagsUpdateResultCloudflaredTunnelType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCloudflaredTunnel: S.Schema<ZoneTagsUpdateResultCloudflaredTunnel>;
export type ZoneTagsUpdateResultCustomCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCustomCertificateTagsMap: S.Schema<ZoneTagsUpdateResultCustomCertificateTagsMap>;
export type ZoneTagsUpdateResultCustomCertificateType = "custom_certificate";
export declare const ZoneTagsUpdateResultCustomCertificateType: any;
export interface ZoneTagsUpdateResultCustomCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCustomCertificateTagsMap;
    type: ZoneTagsUpdateResultCustomCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCustomCertificate: S.Schema<ZoneTagsUpdateResultCustomCertificate>;
export type ZoneTagsUpdateResultCustomHostnameTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCustomHostnameTagsMap: S.Schema<ZoneTagsUpdateResultCustomHostnameTagsMap>;
export type ZoneTagsUpdateResultCustomHostnameType = "custom_hostname";
export declare const ZoneTagsUpdateResultCustomHostnameType: any;
export interface ZoneTagsUpdateResultCustomHostname {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCustomHostnameTagsMap;
    type: ZoneTagsUpdateResultCustomHostnameType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCustomHostname: S.Schema<ZoneTagsUpdateResultCustomHostname>;
export type ZoneTagsUpdateResultCwsDeploymentTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCwsDeploymentTagsMap: S.Schema<ZoneTagsUpdateResultCwsDeploymentTagsMap>;
export type ZoneTagsUpdateResultCwsDeploymentType = "cws_deployment";
export declare const ZoneTagsUpdateResultCwsDeploymentType: any;
export interface ZoneTagsUpdateResultCwsDeployment {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCwsDeploymentTagsMap;
    type: ZoneTagsUpdateResultCwsDeploymentType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCwsDeployment: S.Schema<ZoneTagsUpdateResultCwsDeployment>;
export type ZoneTagsUpdateResultCwsPolicyTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCwsPolicyTagsMap: S.Schema<ZoneTagsUpdateResultCwsPolicyTagsMap>;
export type ZoneTagsUpdateResultCwsPolicyType = "cws_policy";
export declare const ZoneTagsUpdateResultCwsPolicyType: any;
export interface ZoneTagsUpdateResultCwsPolicy {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCwsPolicyTagsMap;
    type: ZoneTagsUpdateResultCwsPolicyType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCwsPolicy: S.Schema<ZoneTagsUpdateResultCwsPolicy>;
export type ZoneTagsUpdateResultCwsPolicySetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCwsPolicySetTagsMap: S.Schema<ZoneTagsUpdateResultCwsPolicySetTagsMap>;
export type ZoneTagsUpdateResultCwsPolicySetType = "cws_policy_set";
export declare const ZoneTagsUpdateResultCwsPolicySetType: any;
export interface ZoneTagsUpdateResultCwsPolicySet {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCwsPolicySetTagsMap;
    type: ZoneTagsUpdateResultCwsPolicySetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCwsPolicySet: S.Schema<ZoneTagsUpdateResultCwsPolicySet>;
export type ZoneTagsUpdateResultCwsWorkloadTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultCwsWorkloadTagsMap: S.Schema<ZoneTagsUpdateResultCwsWorkloadTagsMap>;
export type ZoneTagsUpdateResultCwsWorkloadType = "cws_workload";
export declare const ZoneTagsUpdateResultCwsWorkloadType: any;
export interface ZoneTagsUpdateResultCwsWorkload {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultCwsWorkloadTagsMap;
    type: ZoneTagsUpdateResultCwsWorkloadType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultCwsWorkload: S.Schema<ZoneTagsUpdateResultCwsWorkload>;
export type ZoneTagsUpdateResultD1DatabaseTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultD1DatabaseTagsMap: S.Schema<ZoneTagsUpdateResultD1DatabaseTagsMap>;
export type ZoneTagsUpdateResultD1DatabaseType = "d1_database";
export declare const ZoneTagsUpdateResultD1DatabaseType: any;
export interface ZoneTagsUpdateResultD1Database {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultD1DatabaseTagsMap;
    type: ZoneTagsUpdateResultD1DatabaseType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultD1Database: S.Schema<ZoneTagsUpdateResultD1Database>;
export type ZoneTagsUpdateResultDNSRecordTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultDNSRecordTagsMap: S.Schema<ZoneTagsUpdateResultDNSRecordTagsMap>;
export type ZoneTagsUpdateResultDNSRecordType = "dns_record";
export declare const ZoneTagsUpdateResultDNSRecordType: any;
export interface ZoneTagsUpdateResultDNSRecord {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultDNSRecordTagsMap;
    type: ZoneTagsUpdateResultDNSRecordType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultDNSRecord: S.Schema<ZoneTagsUpdateResultDNSRecord>;
export type ZoneTagsUpdateResultDurableObjectNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultDurableObjectNamespaceTagsMap: S.Schema<ZoneTagsUpdateResultDurableObjectNamespaceTagsMap>;
export type ZoneTagsUpdateResultDurableObjectNamespaceType = "durable_object_namespace";
export declare const ZoneTagsUpdateResultDurableObjectNamespaceType: any;
export interface ZoneTagsUpdateResultDurableObjectNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultDurableObjectNamespaceTagsMap;
    type: ZoneTagsUpdateResultDurableObjectNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultDurableObjectNamespace: S.Schema<ZoneTagsUpdateResultDurableObjectNamespace>;
export type ZoneTagsUpdateResultGatewayListTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultGatewayListTagsMap: S.Schema<ZoneTagsUpdateResultGatewayListTagsMap>;
export type ZoneTagsUpdateResultGatewayListType = "gateway_list";
export declare const ZoneTagsUpdateResultGatewayListType: any;
export interface ZoneTagsUpdateResultGatewayList {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultGatewayListTagsMap;
    type: ZoneTagsUpdateResultGatewayListType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultGatewayList: S.Schema<ZoneTagsUpdateResultGatewayList>;
export type ZoneTagsUpdateResultGatewayRuleTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultGatewayRuleTagsMap: S.Schema<ZoneTagsUpdateResultGatewayRuleTagsMap>;
export type ZoneTagsUpdateResultGatewayRuleType = "gateway_rule";
export declare const ZoneTagsUpdateResultGatewayRuleType: any;
export interface ZoneTagsUpdateResultGatewayRule {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultGatewayRuleTagsMap;
    type: ZoneTagsUpdateResultGatewayRuleType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultGatewayRule: S.Schema<ZoneTagsUpdateResultGatewayRule>;
export type ZoneTagsUpdateResultHealthcheckTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultHealthcheckTagsMap: S.Schema<ZoneTagsUpdateResultHealthcheckTagsMap>;
export type ZoneTagsUpdateResultHealthcheckType = "healthcheck";
export declare const ZoneTagsUpdateResultHealthcheckType: any;
export interface ZoneTagsUpdateResultHealthcheck {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultHealthcheckTagsMap;
    type: ZoneTagsUpdateResultHealthcheckType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultHealthcheck: S.Schema<ZoneTagsUpdateResultHealthcheck>;
export type ZoneTagsUpdateResultImageTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultImageTagsMap: S.Schema<ZoneTagsUpdateResultImageTagsMap>;
export type ZoneTagsUpdateResultImageType = "image";
export declare const ZoneTagsUpdateResultImageType: any;
export interface ZoneTagsUpdateResultImage {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultImageTagsMap;
    type: ZoneTagsUpdateResultImageType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultImage: S.Schema<ZoneTagsUpdateResultImage>;
export type ZoneTagsUpdateResultInfrastructureTargetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultInfrastructureTargetTagsMap: S.Schema<ZoneTagsUpdateResultInfrastructureTargetTagsMap>;
export type ZoneTagsUpdateResultInfrastructureTargetType = "infrastructure_target";
export declare const ZoneTagsUpdateResultInfrastructureTargetType: any;
export interface ZoneTagsUpdateResultInfrastructureTarget {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultInfrastructureTargetTagsMap;
    type: ZoneTagsUpdateResultInfrastructureTargetType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultInfrastructureTarget: S.Schema<ZoneTagsUpdateResultInfrastructureTarget>;
export type ZoneTagsUpdateResultKVNamespaceTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultKVNamespaceTagsMap: S.Schema<ZoneTagsUpdateResultKVNamespaceTagsMap>;
export type ZoneTagsUpdateResultKVNamespaceType = "kv_namespace";
export declare const ZoneTagsUpdateResultKVNamespaceType: any;
export interface ZoneTagsUpdateResultKVNamespace {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultKVNamespaceTagsMap;
    type: ZoneTagsUpdateResultKVNamespaceType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultKVNamespace: S.Schema<ZoneTagsUpdateResultKVNamespace>;
export type ZoneTagsUpdateResultLoadBalancerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultLoadBalancerTagsMap: S.Schema<ZoneTagsUpdateResultLoadBalancerTagsMap>;
export type ZoneTagsUpdateResultLoadBalancerType = "load_balancer";
export declare const ZoneTagsUpdateResultLoadBalancerType: any;
export interface ZoneTagsUpdateResultLoadBalancer {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultLoadBalancerTagsMap;
    type: ZoneTagsUpdateResultLoadBalancerType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultLoadBalancer: S.Schema<ZoneTagsUpdateResultLoadBalancer>;
export type ZoneTagsUpdateResultLoadBalancerMonitorTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultLoadBalancerMonitorTagsMap: S.Schema<ZoneTagsUpdateResultLoadBalancerMonitorTagsMap>;
export type ZoneTagsUpdateResultLoadBalancerMonitorType = "load_balancer_monitor";
export declare const ZoneTagsUpdateResultLoadBalancerMonitorType: any;
export interface ZoneTagsUpdateResultLoadBalancerMonitor {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultLoadBalancerMonitorTagsMap;
    type: ZoneTagsUpdateResultLoadBalancerMonitorType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultLoadBalancerMonitor: S.Schema<ZoneTagsUpdateResultLoadBalancerMonitor>;
export type ZoneTagsUpdateResultLoadBalancerPoolTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultLoadBalancerPoolTagsMap: S.Schema<ZoneTagsUpdateResultLoadBalancerPoolTagsMap>;
export type ZoneTagsUpdateResultLoadBalancerPoolType = "load_balancer_pool";
export declare const ZoneTagsUpdateResultLoadBalancerPoolType: any;
export interface ZoneTagsUpdateResultLoadBalancerPool {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultLoadBalancerPoolTagsMap;
    type: ZoneTagsUpdateResultLoadBalancerPoolType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultLoadBalancerPool: S.Schema<ZoneTagsUpdateResultLoadBalancerPool>;
export type ZoneTagsUpdateResultManagedClientCertificateTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultManagedClientCertificateTagsMap: S.Schema<ZoneTagsUpdateResultManagedClientCertificateTagsMap>;
export type ZoneTagsUpdateResultManagedClientCertificateType = "managed_client_certificate";
export declare const ZoneTagsUpdateResultManagedClientCertificateType: any;
export interface ZoneTagsUpdateResultManagedClientCertificate {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultManagedClientCertificateTagsMap;
    type: ZoneTagsUpdateResultManagedClientCertificateType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultManagedClientCertificate: S.Schema<ZoneTagsUpdateResultManagedClientCertificate>;
export type ZoneTagsUpdateResultPagesProjectTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultPagesProjectTagsMap: S.Schema<ZoneTagsUpdateResultPagesProjectTagsMap>;
export type ZoneTagsUpdateResultPagesProjectType = "pages_project";
export declare const ZoneTagsUpdateResultPagesProjectType: any;
export interface ZoneTagsUpdateResultPagesProject {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultPagesProjectTagsMap;
    type: ZoneTagsUpdateResultPagesProjectType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultPagesProject: S.Schema<ZoneTagsUpdateResultPagesProject>;
export type ZoneTagsUpdateResultQueueTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultQueueTagsMap: S.Schema<ZoneTagsUpdateResultQueueTagsMap>;
export type ZoneTagsUpdateResultQueueType = "queue";
export declare const ZoneTagsUpdateResultQueueType: any;
export interface ZoneTagsUpdateResultQueue {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultQueueTagsMap;
    type: ZoneTagsUpdateResultQueueType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultQueue: S.Schema<ZoneTagsUpdateResultQueue>;
export type ZoneTagsUpdateResultR2BucketTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultR2BucketTagsMap: S.Schema<ZoneTagsUpdateResultR2BucketTagsMap>;
export type ZoneTagsUpdateResultR2BucketType = "r2_bucket";
export declare const ZoneTagsUpdateResultR2BucketType: any;
export interface ZoneTagsUpdateResultR2Bucket {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultR2BucketTagsMap;
    type: ZoneTagsUpdateResultR2BucketType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultR2Bucket: S.Schema<ZoneTagsUpdateResultR2Bucket>;
export type ZoneTagsUpdateResultResourceShareTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultResourceShareTagsMap: S.Schema<ZoneTagsUpdateResultResourceShareTagsMap>;
export type ZoneTagsUpdateResultResourceShareType = "resource_share";
export declare const ZoneTagsUpdateResultResourceShareType: any;
export interface ZoneTagsUpdateResultResourceShare {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultResourceShareTagsMap;
    type: ZoneTagsUpdateResultResourceShareType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultResourceShare: S.Schema<ZoneTagsUpdateResultResourceShare>;
export type ZoneTagsUpdateResultStreamLiveInputTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultStreamLiveInputTagsMap: S.Schema<ZoneTagsUpdateResultStreamLiveInputTagsMap>;
export type ZoneTagsUpdateResultStreamLiveInputType = "stream_live_input";
export declare const ZoneTagsUpdateResultStreamLiveInputType: any;
export interface ZoneTagsUpdateResultStreamLiveInput {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultStreamLiveInputTagsMap;
    type: ZoneTagsUpdateResultStreamLiveInputType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultStreamLiveInput: S.Schema<ZoneTagsUpdateResultStreamLiveInput>;
export type ZoneTagsUpdateResultStreamVideoTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultStreamVideoTagsMap: S.Schema<ZoneTagsUpdateResultStreamVideoTagsMap>;
export type ZoneTagsUpdateResultStreamVideoType = "stream_video";
export declare const ZoneTagsUpdateResultStreamVideoType: any;
export interface ZoneTagsUpdateResultStreamVideo {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultStreamVideoTagsMap;
    type: ZoneTagsUpdateResultStreamVideoType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultStreamVideo: S.Schema<ZoneTagsUpdateResultStreamVideo>;
export type ZoneTagsUpdateResultVectorizeIndexTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultVectorizeIndexTagsMap: S.Schema<ZoneTagsUpdateResultVectorizeIndexTagsMap>;
export type ZoneTagsUpdateResultVectorizeIndexType = "vectorize_index";
export declare const ZoneTagsUpdateResultVectorizeIndexType: any;
export interface ZoneTagsUpdateResultVectorizeIndex {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultVectorizeIndexTagsMap;
    type: ZoneTagsUpdateResultVectorizeIndexType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultVectorizeIndex: S.Schema<ZoneTagsUpdateResultVectorizeIndex>;
export type ZoneTagsUpdateResultWorkerTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultWorkerTagsMap: S.Schema<ZoneTagsUpdateResultWorkerTagsMap>;
export type ZoneTagsUpdateResultWorkerType = "worker";
export declare const ZoneTagsUpdateResultWorkerType: any;
export interface ZoneTagsUpdateResultWorker {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultWorkerTagsMap;
    type: ZoneTagsUpdateResultWorkerType;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultWorker: S.Schema<ZoneTagsUpdateResultWorker>;
export type ZoneTagsUpdateResultWorkerRouteTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultWorkerRouteTagsMap: S.Schema<ZoneTagsUpdateResultWorkerRouteTagsMap>;
export type ZoneTagsUpdateResultWorkerRouteType = "worker_route";
export declare const ZoneTagsUpdateResultWorkerRouteType: any;
export interface ZoneTagsUpdateResultWorkerRoute {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultWorkerRouteTagsMap;
    type: ZoneTagsUpdateResultWorkerRouteType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultWorkerRoute: S.Schema<ZoneTagsUpdateResultWorkerRoute>;
export type ZoneTagsUpdateResultWorkerVersionTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultWorkerVersionTagsMap: S.Schema<ZoneTagsUpdateResultWorkerVersionTagsMap>;
export type ZoneTagsUpdateResultWorkerVersionType = "worker_version";
export declare const ZoneTagsUpdateResultWorkerVersionType: any;
export interface ZoneTagsUpdateResultWorkerVersion {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultWorkerVersionTagsMap;
    type: ZoneTagsUpdateResultWorkerVersionType;
    /** Worker ID is required only for worker_version resources */
    workerId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultWorkerVersion: S.Schema<ZoneTagsUpdateResultWorkerVersion>;
export type ZoneTagsUpdateResultZoneTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultZoneTagsMap: S.Schema<ZoneTagsUpdateResultZoneTagsMap>;
export type ZoneTagsUpdateResultZoneType = "zone";
export declare const ZoneTagsUpdateResultZoneType: any;
export interface ZoneTagsUpdateResultZone {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultZoneTagsMap;
    type: ZoneTagsUpdateResultZoneType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultZone: S.Schema<ZoneTagsUpdateResultZone>;
export type ZoneTagsUpdateResultZoneRulesetTagsMap = {
    [key: string]: string | undefined;
};
export declare const ZoneTagsUpdateResultZoneRulesetTagsMap: S.Schema<ZoneTagsUpdateResultZoneRulesetTagsMap>;
export type ZoneTagsUpdateResultZoneRulesetType = "zone_ruleset";
export declare const ZoneTagsUpdateResultZoneRulesetType: any;
export interface ZoneTagsUpdateResultZoneRuleset {
    /** Identifies the unique resource. */
    id: string;
    /** ETag identifier for optimistic concurrency control. Formatted as "v1:" where the hash is the base64url-encoded SHA-256 (truncated to 128 bits) of the tags map canonicalized using RFC 8785 (JSON Canonicalization Scheme). Clients should treat ETags as opaque strings and pass them back via the If-Match header on write operations. */
    etag: string;
    /** Human-readable name of the resource. */
    name: string;
    /** Contains key-value pairs of tags. Keys may contain at most 256 characters. Values may contain at most 1024 characters and may be empty for key-only tags. */
    tags: ZoneTagsUpdateResultZoneRulesetTagsMap;
    type: ZoneTagsUpdateResultZoneRulesetType;
    /** Zone ID is required only for zone-level resources */
    zoneId: string;
    /** Monotonic version of the resource's tags: the timestamp assigned when the tags were last written. Returned by read endpoints, by 2PC prepare (the version that will be assigned on commit, unless a concurrent write lands first, in which case a newer version is assigned), and by 2PC commit (the authoritative committed version). Omitted for untagged resources and delete commits: a deleted resource has no current version, and deletions are ordered by event order rather than by version. */
    tagsUpdatedAt?: string | null;
}
export declare const ZoneTagsUpdateResultZoneRuleset: S.Schema<ZoneTagsUpdateResultZoneRuleset>;
export type ZoneTagsUpdateResult = ZoneTagsUpdateResultAccessApplication | ZoneTagsUpdateResultAccessApplicationPolicy | ZoneTagsUpdateResultAccessGroup | ZoneTagsUpdateResultAccount | ZoneTagsUpdateResultAccountRuleset | ZoneTagsUpdateResultAIGateway | ZoneTagsUpdateResultAlertingPolicy | ZoneTagsUpdateResultAlertingWebhook | ZoneTagsUpdateResultAPIGatewayOperation | ZoneTagsUpdateResultCloudflaredTunnel | ZoneTagsUpdateResultCustomCertificate | ZoneTagsUpdateResultCustomHostname | ZoneTagsUpdateResultCwsDeployment | ZoneTagsUpdateResultCwsPolicy | ZoneTagsUpdateResultCwsPolicySet | ZoneTagsUpdateResultCwsWorkload | ZoneTagsUpdateResultD1Database | ZoneTagsUpdateResultDNSRecord | ZoneTagsUpdateResultDurableObjectNamespace | ZoneTagsUpdateResultGatewayList | ZoneTagsUpdateResultGatewayRule | ZoneTagsUpdateResultHealthcheck | ZoneTagsUpdateResultImage | ZoneTagsUpdateResultInfrastructureTarget | ZoneTagsUpdateResultKVNamespace | ZoneTagsUpdateResultLoadBalancer | ZoneTagsUpdateResultLoadBalancerMonitor | ZoneTagsUpdateResultLoadBalancerPool | ZoneTagsUpdateResultManagedClientCertificate | ZoneTagsUpdateResultPagesProject | ZoneTagsUpdateResultQueue | ZoneTagsUpdateResultR2Bucket | ZoneTagsUpdateResultResourceShare | ZoneTagsUpdateResultStreamLiveInput | ZoneTagsUpdateResultStreamVideo | ZoneTagsUpdateResultVectorizeIndex | ZoneTagsUpdateResultWorker | ZoneTagsUpdateResultWorkerRoute | ZoneTagsUpdateResultWorkerVersion | ZoneTagsUpdateResultZone | ZoneTagsUpdateResultZoneRuleset;
export declare const ZoneTagsUpdateResult: any;
export type PutZoneTagResponse = ZoneTagsUpdateResult;
export declare const PutZoneTagResponse: S.Schema<PutZoneTagResponse>;
export type DeleteAccountTagError = Forbidden | CloudflareOpError;
/** Removes all tags from a specific account-level resource. */
export declare const deleteAccountTag: API.OperationMethod<DeleteAccountTagRequest, DeleteAccountTagResponse, DeleteAccountTagError, CloudflareOpContext>;
export type DeleteZoneTagError = Forbidden | CloudflareOpError;
/** Removes all tags from a specific zone-level resource. */
export declare const deleteZoneTag: API.OperationMethod<DeleteZoneTagRequest, DeleteZoneTagResponse, DeleteZoneTagError, CloudflareOpContext>;
export type GetAccountTagError = Forbidden | CloudflareOpError;
/** Retrieves tags for a specific account-level resource. */
export declare const getAccountTag: API.OperationMethod<GetAccountTagRequest, GetAccountTagResponse, GetAccountTagError, CloudflareOpContext>;
export type GetSummaryError = CloudflareOpError;
/** Lists all distinct tag keys and their distinct values across resources in an account. */
export declare const getSummary: API.OperationMethod<GetSummaryRequest, GetSummaryResponse, GetSummaryError, CloudflareOpContext>;
export type GetZoneTagError = ZoneTagResourceNotFound | Forbidden | CloudflareOpError;
/** Retrieves tags for a specific zone-level resource. */
export declare const getZoneTag: API.OperationMethod<GetZoneTagRequest, GetZoneTagResponse, GetZoneTagError, CloudflareOpContext>;
export type ListKeysError = CloudflareOpError;
/** Lists all distinct tag keys used across resources in an account. */
export declare const listKeys: API.PaginatedOperationMethod<ListKeysRequest, ListKeysResponse, ListKeysError, CloudflareOpContext, string>;
export type ListResourceTaggingsError = CloudflareOpError;
/** Lists all tagged resources for an account. */
export declare const listResourceTaggings: API.PaginatedOperationMethod<ListResourceTaggingsRequest, ListResourceTaggingsResponse, ListResourceTaggingsError, CloudflareOpContext, ListResultItem>;
export type ListValuesError = CloudflareOpError;
/** Lists all distinct values for a given tag key, optionally filtered by resource type. */
export declare const listValues: API.PaginatedOperationMethod<ListValuesRequest, ListValuesResponse, ListValuesError, CloudflareOpContext, string>;
export type PutAccountTagError = Forbidden | TagPreconditionFailed | CloudflareOpError;
/** Creates or updates tags for a specific account-level resource. */
export declare const putAccountTag: API.OperationMethod<PutAccountTagRequest, PutAccountTagResponse, PutAccountTagError, CloudflareOpContext>;
export type PutZoneTagError = ZoneTagResourceNotFound | Forbidden | TagPreconditionFailed | CloudflareOpError;
/** Creates or updates tags for a specific zone-level resource. Replaces all existing tags for the resource. */
export declare const putZoneTag: API.OperationMethod<PutZoneTagRequest, PutZoneTagResponse, PutZoneTagError, CloudflareOpContext>;
//# sourceMappingURL=resource_tagging.d.ts.map