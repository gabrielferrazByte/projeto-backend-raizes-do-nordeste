/**
 * Stripe SDK trait surface — hand-written.
 *
 * Re-exports the generic protocol traits from core (so generated operations
 * import everything from one place). The only Stripe-specific piece is the
 * widened `Http` trait: nearly every Stripe operation's request body is
 * `application/x-www-form-urlencoded` (with Stripe's deep bracket
 * expansion), which core's `HttpTrait` doesn't model — the Stripe protocol
 * keys its body encoding on `contentType: "form-urlencoded"`.
 */
import { Http as CoreHttp, } from "@distilled.cloud/core/trait";
export { Body, Header, Query, Label, ResponseCode, HttpBody, FormDataFile, KeyDictionary, UnionCases, applyErrorMatchers, getErrorMatchers, bodySymbol, headerSymbol, querySymbol, labelSymbol, httpSymbol, responseCodeSymbol, httpBodySymbol, formDataFileSymbol, keyDictionarySymbol, unionCasesSymbol, } from "@distilled.cloud/core/trait";
export { RawResponse, RawResponseRoot, SensitiveValue, rawResponseSymbol, rawResponseRootSymbol, sensitiveValueSymbol, } from "@distilled.cloud/core/protocol-rest";
/** Operation-level HTTP binding (see {@link HttpTrait}). */
export const Http = (trait) => CoreHttp(trait);
//# sourceMappingURL=traits.js.map